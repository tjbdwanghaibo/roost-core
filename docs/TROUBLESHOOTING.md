# 问题快速定位

按**你看到的症状**索引。每行四列：最可能的原因、看哪里、怎么处理。找不到症状时先看 [OBSERVABILITY.md](../OBSERVABILITY.md) 的指标清单，再到 [USER_GUIDE.md](USER_GUIDE.md) 第 12 章。

这份文档与 bug 收敛绑定：每个[工作单元](history/ledger.md)必须为它加一行（编号 `T-xx`，单元日志里回填）。所以它随收敛增长，不是一次写完就过期的文件。"看哪里"引用的指标名、错误值、命令都必须真实存在——文档漂移检查会核对。

| 编号 | 症状（你看到的） | 最可能的原因 | 看哪里 | 怎么处理 |
| --- | --- | --- | --- | --- |
| T-01 | 进程启动后立刻熔断退出，日志有 `ErrCommitIndeterminate` | 上次 fsync 结果不确定，框架按不变量②（结果不确定时 fence，不猜）主动 fail-stop，避免内存回滚制造与 WAL 冲突的第二条历史 | 启动日志里的 WAL replay 记录；`OBSERVABILITY.md` "调度与事务"节的 fence / pipelined verdict 指标 | 让新进程完成 WAL replay 判定权威结果。**不要**手动删 WAL、不要改内存状态后重启；如果 fail-stop 反复发生，查磁盘（写满、只读、fsync 延迟） |
| T-02 | Remote Entity 写被拒，错误含 fence / marker epoch / route epoch 不匹配 | 旧 owner 的延迟写，或租约过期后恢复的进程仍持有旧 fence；存储端按 (marker, route, version) 三元 CAS 拒绝 | `OBSERVABILITY.md` "跨服实体"节的 remote write gate 指标；ownership marker 当前值 | 这是正确行为。从当前 owner 重新发起操作，不要绕过重试；如果是刚迁移完 ownership 的实体，等路由 epoch 传播 |
| T-03 | `roost generate` 报 `unknown kit mod "xxx"` | `roost.yaml` 用了当前 codegen 版本 catalog 不含的名字，通常是升级后名字变了 | `roost.yaml` 的 `mods` / `shared_mods`；`roost version` | `roost project upgrade`；旧名（`sync`、`replication-*`、`checkpoint`）会在加载时归一化并打弃用告警，其余名字按 codegen README 的生成器总览改 |
| T-04 | Mongo Mod 启动预检失败，提示部署无逻辑会话 / 需要 replica set | 接的是单机 `mongod`；kit 的 mongo Mod 写关注硬编码 majority + journal，事务需要副本集 | kit `mongo` 配置段；`require_replica_set` 开关 | 用 replica set（`make dev-up` 起的就是）；生产环境按 `DEPLOYMENT.md` 部署副本集或分片集 |
| T-05 | `nats.reliable.enabled: true` 后 Provide 阶段报找不到 redis capability | 装配顺序：可靠 Bus 的 inbox 去重依赖 redis Mod，redis 必须在 nats 之前注册 | bootstrap 里 `app.Use(...)` 的 Mod 顺序 | 把 redis Mod 移到 nats 之前；codegen 生成的 bootstrap 已按 catalog 依赖排序，手写装配时对照 kit README §3.3 |
| T-06 | entity 数量只增不减（statslog 的 `entity.count` gauge / 记录里的 `Entity.Total` 单调上升） | `Destroy` 未被调用，或持久删除的 admission 被延迟 / 拒绝 | statslog 的 `entity.count_by_category{category}` / `entity.count_by_kind{kind}` gauge（或记录里的 `Entity.ByCategory`）；nest 的 delayed admission 指标；`EntityManager.CountByCategory` | 按 category 定位到实体类型，检查其销毁路径是否走了 `EntityManager.Destroy`；查 delete admitter 的拒绝原因 |
| T-07 | goroutine 数持续增长，但 nest / bus 队列指标平稳 | 业务侧泄漏：框架 goroutine 都经 `goroutine` / `worker` 包产生并有上界，增长在框架之外 | statslog `runtime.goroutines`；运行时观察交付后看 `roost_goroutines{owner}` 的残差；pprof goroutine profile | 抓 goroutine profile，按业务包分组看阻塞点；常见是 handler 里 `go func()` 等一个永远不来的 channel |
| T-08 | kit CI 的基准步骤失败：`no Go files in .../sync` 或 `directory not found`，而 `go test ./...` 绿 | 工作流里的包路径字面量没有跟着包改名（v1.10.0 `sync` → `room`） | `.github/workflows/ci.yml` 里所有 `./pkg` 参数；kit 根测试 `TestCIWorkflowPackagePathsExist` 的输出 | 改成新包名；该测试会在普通 `go test` 里报出不存在的路径，改名时先跑它 |
| T-09 | codegen release 工作流的 `framework verify` 一直通过，但生成工程实际用的是更新的 core / kit | `ci/framework-release.yaml` 落后于已发布 tag；门禁校验的是清单里的旧组合，不是你以为的当前组合 | `ci/framework-release.yaml` 的 `framework` 段 vs 五仓 `git describe --tags` | 每次按 core → kit → skill → service → codegen 发布后更新清单；缺 `service` 字段的清单自 codegen 下一版起直接不通过校验 |
| T-10 | kit 集成套件里**第一个**测试失败：`start data engine mod: dataengine mod: ensure effect stream: context deadline exceeded`，后面的测试都过 | NATS 三节点刚起来，JetStream 元集群还没选出 leader，建流请求一直等；`dataengine.startup_timeout` 默认 30s 先到期。只打第一个夹具，因为等它超时时 leader 已经选出来了 | `scripts/integration/dataengine-env.sh status` 每行末尾的 `meta_leader=`；任一监控端口的 `/jsz` 里 `.meta_cluster.leader` | 环境脚本自 U-0003 起把 meta leader 纳入就绪判定。手工搭的环境要等 `/jsz .meta_cluster.leader` 非空再启动进程；生产里如果 NATS 集群整体重启，Data Engine 的 startup_timeout 要覆盖元选举时间 |
| T-11 | 排行榜提交在高并发下偶发返回 `CodeInternal` / "server error"，但 Redis 正常、日志里是 `submit lost 8 compare-and-swaps` | 同一 owner 的 CAS 重试耗尽是业务层面的冲突，不是服务故障；rank v1.4.0 及之前把它当无码错误返回，信封只能填 `CodeInternal` | `rank` 的 `conflict:submit` 指标计数；错误链里是否有 `rank.ErrConflict`（旧名 `ErrConflictSentinel`） | 升级到含 U-0004 的 rank：客户端收到 `CodeConflict 540108` 后按业务重试或提示；服务端不要把它计入故障告警 |
| T-12 | 客户端更新角色 profile 收到 `CodeConflict 560113`，重试仍然失败 | profile 超过 `MaxProfileBytes`（4096）；account v1.4.0 及之前把这种大小违规错报成"冲突"，客户端按冲突语义重试自然永远失败 | 错误 reason 里是否有 `profile is N bytes, limit 4096` | 升级到含 U-0005 的 account 后该失败改报 `CodeRangeInvalid 560112`；客户端对 560112 不要重试，裁剪 profile |
| T-13 | 全服广播邮件发送接口返回成功，但没有任何玩家收到；重发同一个 request id 仍返回成功 | 第一次 fanout 失败后账本已记下这次发送，mail v1.4.0 及之前的回放路径不再投递 | `refused:send:delivery_failed` 是否在首次发送时出现过；账本记录 `delivered_at_unix` 是否为 0（含 U-0006 的版本） | 升级到含 U-0006 的 mail：回放会补投递。旧版本只能换一个 request id 重发，并接受可能的重复 |
| T-14 | 客户端翻邮箱列表时中途提前结束，`Summary` 的未读数却比列过的多 | 上一页最后一封邮件在翻页间隙被删除，旧游标按 id 定位失败，直接返回空页无游标 | 游标格式：含 `|` 的是位置游标（U-0006 起），纯 id 是旧格式 | 升级 mail；客户端对空页且无游标的结果不要缓存为"已读完"，重新从头列一次 |
| T-15 | 玩家反馈"发出去的聊天消息没出现"，服务端却记录了成功；偶发、多人同频道时更多 | 两个客户端在同一频道用了相同的幂等键（客户端本地计数器、短随机串），chat v1.4.0 及之前把后来者当回放，返回前者的消息并报成功 | `refused:publish.<kind>:key_reused` 计数（含 U-0007 的版本）；客户端幂等键的生成方式 | 升级到含 U-0007 的 chat：撞键返回 `CodeConflict 580111`，客户端换键重发。根治是客户端用全局唯一键（UUID 或 roleID 前缀） |
| T-16 | 匹配队列"卡住"：`Candidates` 返回同一张票两次，`Commit` 报 `ticket appears twice`，直到有新玩家入队才恢复 | 只在内存后端（测试、单进程演示、`examples/`）出现：match v1.4.0 及之前的 Mutate 回调就地修改存储值后又放弃保存，`Waiting` 尾部被复制。Redis 后端每次重新解码，不受影响 | 是否在用 `versionstore.NewMemoryStore`；`Candidates` 结果里是否有重复 id | 升级到含 U-0008 的 match。生产环境用 Redis 后端本就不受影响；如果生产也看到重复票，那是别的问题 |
| T-17 | JetStream 上的状态流 / 副本同步偶发少一条：发布方进程刚重启，或 delete 紧跟同版本 upsert，消费者没收到；`nats` 端无报错 | 消息没有独立投递身份时，JetStream 以 `(topic,key,version,sid,part)` 元组去重：重启后重发的同序号帧、同 key 同 version 的 upsert 与 delete 都会被当成重复而丢弃 | 发布出的 `SyncMsg.MessageID` 是否为空；JetStream stream 的 `duplicates` 窗口内是否有同名 MsgId | 升级到含 U-0009 的 core/kit：三条发布路径都经 `syncbus.DeliveryIDs` 生成身份。自写发布者要填 `MessageID`，不要依赖元组回退 |
| T-18 | kit CI 的 `integration` job 在环境脚本一开始就失败：`require_commands` / `command not found: mongod …`，或 `mkdir: cannot create directory '/private': Permission denied`、`failed to initialize build cache at /private/tmp/…` | 前者：runner 上安装步骤没装齐，或 MongoDB apt 源对当前 ubuntu 代号（`lsb_release -cs`）没有发行；后者：脚本里残留 macOS 专属的 `/private/tmp` 路径（U-0010 已把根目录改为 `$(cd /tmp && pwd -P)` 派生，新加路径要沿用） | job 里两步安装的输出；`grep -rn /private scripts/` | 版本对照本地 `mongod --version` / `nats-server --version` 调整；路径一律从 `ROOST_IT_CANONICAL_ROOT` 派生，不要再写字面量 |
| T-19 | Remote Entity 的每一次共享（marked）操作都被 `ErrRemoteFenced: shared lock did not allocate fence` 拒绝，但 Redis 正常、无 ownership 变更 | 装配了一个不实现 `redis.IFencedVersionedLock` 的锁工厂（自写或旧版本），锁拿不到 fence；U-0011 之前这在运行时逐次暴露 | `RemoteEntityMod.Provide` 的返回（含 U-0011 的版本会在这里就停）；日志 `remote_entity: refusing to create wrappers` | 用 kit 自带的 `versionedLockFactory`，或让自写工厂返回带 `Fence() uint64` 的锁。不要绕过：fence 是 Remote commit 三元 CAS 的一维 |
| T-20 | 某个 `IDistLock` 对象此后每次 `Acquire` 都返回 `ErrDistLockStateUncertain`，Redis 里该 key 可能有也可能没有 | 上一次 `Acquire` 的 SETNX 或上一次 `Release` 的回复丢失了；锁对象不知道服务端是否已应用，按设计停在不确定态，直到一次值守卫的 `Release` 对账（U-0012 起有测试守住这条状态机） | 错误链里的 `ErrDistLockStateUncertain`；Redis 该 key 的值是否等于本对象的 owner token | 对同一对象调一次 `Release`：服务端有租约则释放成功，没有则返回 `ErrLockNotHeld`，两种结果之后都可以重新 `Acquire`。不要新建对象绕过——那正是把第二个租约叠到第一个上面的路径 |
| T-21 | nestwal 的 `applied` 计数偶尔高于实际提交的 mutation 数；或 `TestCommitterEnqueueHoldsReplayUntilReleased` 在慢机器上报 `apply calls=2` | kit ≤ v1.12.1 的重放循环与 `Flush` 两条 pass 可能在"读完、未 ack"窗口重叠，同一记录被 apply 两次。应用侧按契约幂等，所以不丢不坏，只是重复工作 | `CommitterStats.Applied` 与提交数的差；是否同时有主动 `Flush` 调用 | 升级到含 U-0013 的 kit（`replayPass` 整体串行）。自写 `MutationApplier` 无论如何都要幂等——契约就是 at-least-once |
| T-22 | 通用服务（原 roost-service，现 kit/service）的 Redis 集成测试在本地或某条流水线上全部 `--- SKIP`（`set REDIS_ADDR to run …`），结果却是绿的 | 三处套件带 `//go:build integration` 且以 `REDIS_ADDR` 门控；不设变量就整体 skip，`go test` 仍报 ok | `go test -tags integration -json ./... \| jq 'select(.Action=="skip")'` 的条数；CI 的 integration 段是否设置了 `REDIS_ADDR` | 本地起 Redis 后 `REDIS_ADDR=127.0.0.1:6379 go test -tags integration -p 1 ./...`；CI 用 `.github/workflows/ci.yml` 的 integration 段（U-0014），它把含 `REDIS_ADDR` 字样的 skip 判为失败 |
| T-23 | 生成工程的 `docker compose -f deploy/docker/docker-compose.prod.yaml config` 报 `service "game" refers to undefined volume configs/service/config.game.yaml: invalid compose project` | codegen ≤ v1.13.0 用短语法挂配置文件；compose 把不以 `/`、`./`、`../` 开头的 source 当命名卷。`ROOST_CONFIG_ROOT` 传相对路径就撞上 | compose 文件里 `volumes:` 是短语法还是 `type: bind`；`ROOST_CONFIG_ROOT` 的值 | 升级 codegen 并 `make sync`（U-0015：长语法显式 bind，Makefile / CI 传绝对路径）。临时绕：`ROOST_CONFIG_ROOT` 用绝对路径。注意相对 bind source 是相对 compose **文件**目录解析的，`up` 会挂错目录 |
| T-24 | 生成工程 `kubectl kustomize deploy/k8s/overlays/staging` 报 `cycle detected: candidate root …/deploy/k8s contains visited root …/deploy/k8s/overlays/staging`；或 `deploy/k8s/deploy.sh` 一个对象都渲染不出 | kustomize v5.7+（kubectl 1.34+）拒绝 base 目录是 overlay 祖先的布局；codegen ≤ v1.13.0 的 base 就在 `deploy/k8s/` | `kubectl version --client` 的 Kustomize 版本；`deploy/k8s/` 下是否直接有 `kustomization.yaml` | 升级 codegen 并 `make sync`：base 移到 `deploy/k8s/base/`，旧位置六类受控清单会被 sync 删除（`removed=6`）；自己的 `secret.<svc>.local.yaml` 手动移到 base/。sync 后 `removed=0` 且旧文件仍在 → codegen 版本还没含 U-0015 的迁移识别 |
| T-25 | 生成工程 `docker build` 在 `RUN go mod download` 失败：`go.mod requires go >= 1.27.0 (running go 1.25.14; GOTOOLCHAIN=local)` | Dockerfile 的 `ARG GO_VERSION=1.25`（codegen ≤ v1.13.0）落后于框架要求；生成的 go.mod 在 `go get` 框架后被抬到 1.27.0 | Dockerfile 第 2 行的 `ARG GO_VERSION`；`go.mod` 的 `go` 指令 | 升级 codegen 并 `make sync`（三处共用 `generatedGoVersion`）。临时绕：`docker build --build-arg GO_VERSION=1.27 …` |
| T-26 | kit/service 集成测试（原 roost-service） `TestEveryModWritesUnderItsConfiguredPrefix` 报 `N of M new keys were written outside the configured prefix … e.g. "mailtest:…"`，单独跑该包又过 | 多个测试包并行（`go test` 默认 `-p` = CPU 数）共用同一个 Redis；该测试对整个 keyspace 做前后差集，把并行包写的键当成越界 | 越界键的前缀是不是别的测试包（`mailtest:` / `ranktest:`）；命令有没有 `-p 1` | 加 `-p 1` 逐包运行（CI 已改）。不要把差集改成按模式扫描——那正是该测试注释里记录的上一个假绿 |
| T-27 | `accepted:reserve` / `accepted:commit`（directory）等计数明显高于实际成功次数，负载越高越离谱 | service ≤ v1.5.0 在 `versionstore.Update` 回调内上报；kit 契约允许回调因 CAS 冲突重跑，计的是尝试数 | 对比 `accepted:*` 与存储里真实条目数；是否只在多副本竞争时出现 | 升级到含 U-0016 的 service。自写服务遵循同一规则：回调只做决定，Update 返回后按结果上报一次；`directory/contention_test.go` 的 `contendedStore` 可直接借用来验证 |
| T-28 | 支付回调驱动的投递里，`conflict:deliver` 指标涨了，但回调方拿到的 Receipt 从不带 `Replayed=true`；或同一订单的货被发了两次而回调日志看不出 | service ≤ v1.5.1 的 `HandleCallback` 首次路径构造 Receipt 时只抄 `Order` / `Delivered`，`AttemptDelivery` 置位的 `Replayed` 被丢掉。触发场景：一次慢投递超过退避期，sweep 或 provider 重试超车先提交 | `conflict:deliver` 与回调侧 `Replayed` 的对照；投递方（Deliverer）耗时是否接近退避期 | 升级到含 U-0018 的 service。投递方自身仍应按订单幂等——超车是设计上可达的竞态，不是 bug；`race_test.go` 的 `blockingDeliverer` 可用来复现 |
| T-29 | chat 频道的 `Retained` 统计只涨不降、`retention_age` 像没生效；或 match 的 `ticket.expired` 计数只在有人读写队列时才动 | service ≤ v1.5.1 的两个 Server run 钩子遍历一个返回硬编码 `nil` 的方法，后台 `Prune` / `Sweep` 从未被调用。升级后若仍如此，是没配置枚举：启动日志会有一条 `no prune channels configured` / `no sweep queues configured` 告警 | 启动日志的这两条 WARN；`chat.prune_channels` / `match.sweep_queues` 是否为空 | 配置 `chat.prune_channels: ["world:1", "group:42"]`（共享频道，`kind:target`；私聊等 pair 频道用 `Mod.WithPruneChannels` 提供在线玩家集合）与 `match.sweep_queues: ["ranked:2:asia"]`（`mode:group_size[:partition]`）。配置项写错会在启动时失败而不是被跳过（U-0022） |
| T-30 | 进程启动即退：`mod service.<xxx> depends on bus: unknown mod dependency "bus"` | 该进程装配了某个 kit/service 服务（原 roost-service）的 `NewClientMod()`；service ≤ v1.5.1 / codegen ≤ v1.13.2 生成的 ClientMod 把总线 capability 名 `bus` 写成了依赖，而 app 按 Mod 名解析依赖，没有 Mod 叫 `bus` | 报错里的 Mod 名（`service.account` 等）；`*_rpc_gen.go` 里 `ClientMod.DependsOn` 返回的是 `mods.ModBus` 还是 `mods.ModNats` | 升级到含 U-0024 的 roost-service（八个包已重生成），或用新 codegen 的 servicerpc 重生成自己的传输层；进程里必须有 kit 的 nats Mod（生成器对 `uses` 会自动补） |
| T-31 | 进程启动即退：`mod dataengine depends on health: unknown mod dependency "health"`（或 saga / remote_entity 报 `nats.jetstream` / `health`） | kit ≤ v1.12.1 三个 Mod 的 `DependsOn` 写了 Registry 内建项 / capability 名，app 按 Mod 名解析。默认生成的工程（含 nest）全部命中 | 报错里的依赖名是否是某个 Mod 的 `Name()`；kit 版本 | 升级到含 U-0025 的 kit（`go get github.com/tjbdwanghaibo/roost-kit@latest` 或 `roost project deps`）。自写 Mod 遵循同一规则：`DependsOn` / `OptionalDependsOn` 只写 Mod 名；需要 capability 的在 Provide 里 `app.Lookup`，需要顺序就依赖发布它的 Mod |
| T-32 | 在开发机上起生成工程的进程，`mod mongo start: server selection error … ReplicaSetNoPrimary … lookup mongo … server misbehaving` | codegen ≤ v1.13.4 的 `deploy/dev/docker-compose.yaml` 用 `mongo:27017` 初始化副本集，驱动发现成员地址后在宿主机解析不了 `mongo` | `docker compose exec mongo mongosh --eval 'rs.conf().members'` 里的 host；服务配置拨的是 `127.0.0.1:27017` | 升级 codegen 并 `make sync`，然后 `docker compose -f deploy/dev/docker-compose.yaml down -v` 重建卷（旧卷里的副本集配置仍指向 `mongo`）；临时绕：`rs.reconfig` 把成员改成 `127.0.0.1:27017` |
| T-33 | tag CI 的 unit 段在 `git diff --exit-code -- go.mod go.sum` 失败，diff 里是几行被删掉的旧版本校验和 | 升级 `tool` 指令或依赖后没跑 `go mod tidy`；`go get` 不清理 go.sum 的旧行。本地 `pretag.sh` 以前不查 tidy，所以放行了 | tag 对应提交的 go.sum 是否比 `go mod tidy` 后多行 | 五仓 `scripts/pretag.sh` 现在都先 `go mod tidy` 再比对；红了的 tag 不要改写，补 tidy 后打下一个补丁版并对齐发布清单 |
| T-34 | kit 集成套件里 `TestToxic*` 显示 SKIP：`toxiproxy-server not installed` | 网络故障测试依赖 toxiproxy；普通 CI 的 integration job 不装它，只有 nightly 装并以 `ROOST_IT_TOXIPROXY=1` 强制 | `scripts/integration/dataengine-env.sh status` 是否有 `toxiproxy=RUNNING`；env.sh 是否导出 `ROOST_DATAENGINE_IT_TOXIPROXY_URL` | 本地 `brew install toxiproxy`（或下载 release 二进制）后重新 `up`；要把 skip 变成失败就设 `ROOST_IT_TOXIPROXY=1` |
| T-35 | 配置表里两行同一个 id，游戏里只看到后一行；或声明了 `unique="true"` / `min=` 的列出现重复 / 越界值却没有任何报错 | codegen ≤ v1.13.7 的 tablegen 只把这些约束印进 CSV 规则行，不执行 | `roost generate` 的输出有没有 `key field … repeats value` / `below min=`；`configs/generated` 的 JSON 里同 key 是否只剩一行 | 升级 codegen 并重跑 `roost generate`：现在生成期就报文件、字段、行号。跨表 `ref=` 仍不在这一层校验 |
| T-36 | 同一条 JetStream 消息被处理器反复收到（`NumDelivered` 递增），处理器日志却显示每次都成功 | `Ack` 在发回 broker 时失败（连接已关 / 消费者被重建），broker 到 AckWait 后重投。kit ≤ v1.12.3 把这个失败静默丢掉 | `nats.jetstream.settle_failures.total{op="ack"}` 是否在涨；Warn 日志 `nats jetstream: settling delivery failed` | 处理器保持幂等（本就要求）；查 NATS 连接 / 消费者是否被外部删除；AckWait 是否短于处理时长 |
| T-37 | outbox 积压（`outbox_pending` 涨、`outbox_oldest` 变长）但 `publish_failures=0`，日志安静 | 是 store 侧（Mongo 认领 / ack / nack）在失败，不是总线。kit ≤ v1.12.3 只加计数不出声，health 行也不报 | health 行 `store_failures=`；Warn `dataengine: outbox claim loop failing`（连败只打一次，看**首条**时间）；恢复时 Info `outbox claim loop recovered` | 修 Mongo 连接 / 副本集；worker 不用重启，store 恢复后自动继续认领 |
| T-38 | 某个 `DealEventXxx` 从来收不到事件，`<recv>_event_gen.go` 里没有它的 `case`；或 `go build` 在 `*_event_gen.go` 报 `undefined: event.EventXxx` | 前者：codegen ≤ v1.13.8 的 eventgen 遇到解析不了的源文件会整文件跳过、不报错；后者：处理器引用的事件在定义目录里没有对应 `EventXxx` 结构体 | 升级后重跑 `roost generate`：前者报 `parse <file>: …`，后者报 `<file>: (<recv>).DealEventXxx has no event struct EventXxx` | 修源文件语法 / 在事件定义目录补 `type EventXxx struct` 再生成；生成文件不要手改 |
| T-39 | 写了 `//roost:entity` 却没有生成 `<entity>_gen_wire.go`；或明明写了 `remote=managed` / `sync=true`，生成的却是本地 / 不同步实体 | codegen ≤ v1.13.8 的 entity 生成器：标记与 `type X struct` 之间隔了两行以上（多行文档注释）或标记放在 interface 上时整条标记静默消失；键名拼错、裸词、非法值都按"没写"处理 | 升级后重跑 `roost generate`：会在 `文件:行` 处报 `marker is not followed by a struct` / `unknown parameter` / `remote="…" is not one of …` | 把文档注释移到标记上方（标记紧贴 `type` 行）；按报错列出的合法键 / 值改标记 |
| T-40 | `roost generate` 对某个 `//roost:web` 处理器报 `unsupported method ""`，但标记里明明写了 method | codegen ≤ v1.13.8 的 webroute 接受任意键名：`methd=` 之类拼错键被吞掉、随后按"没写 method"报错 | 看标记的键名拼写；升级后重跑会直接报 `unknown marker option "methd" (known: method, path, body)` 或 `missing marker option "…"` | 改标记键名；三项 method / path / body 都是必填 |
| T-41 | tag CI 的 `integration` job 或 `vet integration-tagged tests` 步骤红：`xxx redeclared in this block`，另一处声明在 `*_integration_test.go`；本地 `go test ./...` 全绿 | 新增的普通测试文件与 `//go:build integration` 文件声明了同名辅助函数——两份构建各自能过，只有带 tag 的构建同时看见二者。看 CI 时若用 `gh run list --limit 1` 取"最新一次运行"，可能取到 `codeql` 而不是 `ci`，红了也不知道 | `go vet -tags integration ./...`；`gh run list --workflow ci --branch <分支或 tag>` 看 ci 工作流本身的结论 | 改名其一；kit 的 pretag 现在带 tag vet 一遍。红了的 tag 不改写，补丁版 + 对齐发布清单 |
| T-42 | 故障注入 / 代理部署下 NATS 客户端日志出现 `reconnected url=nats://<成员真实地址>`，代理上的 toxic 或访问控制"失效" | nats.go 默认从 INFO gossip 学到全部成员的广告地址，重连时随便挑一个——绕开代理 / NAT 映射 | 日志里 `reconnected url=` 的端口是不是配置的那个 | 配置 `nats.ignore_discovered_servers: true`（kit ≥ v1.12.5）；平坦网络里保持默认，靠 discovery 做故障转移 |
| T-50 | 两个玩家几乎同时进入，都收到成功，其中一个之后重试同一请求被拒 `request "…" belongs to owner N`，且他手里那个 run 找不到了 | kit ≤ v1.14.2 的 session `Enter` 丢弃了账本 Create 的 created=false：不同 owner 撞同一个 RequestID 时两边都成功，账本只记胜者 | 指标 `refused:enter:request_owner_collision`（新）；旧版本无信号，只能从账本 owner 与 run owner 不一致看出 | 升级 kit；客户端 RequestID 应全局唯一（UUID），跨 owner 撞键是客户端缺陷 |
| T-51 | 读穿透缓存在后端变慢期间持续报 `cache: load waiter limit exceeded`，即使并发请求数远低于 `MaxWaitersPerKey` | core ≤ v1.15.1 的 `ReadThroughStore.load` 在跟随者取消 / 超时离开时不归还等待名额，限制变成"本次 load 累计入场次数" | 首个 load 结束后自行恢复；`Coalesced` 持续增长而拒绝不减 | 升级 core；临时调大 `MaxWaitersPerKey` 或拉长调用方超时 |
| T-53 | 一个玩家同时有两个 open 的 run；`Current` 只看见后一个，`Enter` 在已有 run 时又成功 | kit ≤ v1.14.3 的 session `Enter` 撞 RequestID 落败后先删 run 再删 claim：run 一没，同 owner 的新 Enter 把孤立 claim 清掉建了新 claim，而删后重建的键版本回到 1，落败者按版本删 claim 把新的删了（ABA） | 指标 `dropped:claim.orphaned` 与 `refused:enter:request_owner_collision` 相邻出现 | 升级 kit；旧版本把多出来的 run `Release` 掉即可恢复 |
| T-54 | 停服时 dataengine 第一次 `Stop` 超时报错，紧接着重试立刻成功，但进程退出时仍有 outbox worker 在跑 / Mongo 连接被拔时报错 | core ≤ v1.15.2 的 `Assembly.Shutdown` 无论结果都置空 runtime，重试看到 nil 直接答成功 | 第一次 Shutdown 的错误是 `context deadline exceeded` / `context canceled` | 升级 core；旧版本给停机 context 足够的时限，不要以重试成功作为释放底层资源的依据 |
| T-56 | `roost entity -dir X -output Y.go` 对有两个实体的包 exit 0 并两次打印同一路径,随后 `go build` 报 `undefined: RegisterEntity` | codegen ≤ v1.15.4 的生成循环对每个实体都用同一个 `-output`,后者覆盖前者,包级 `RegisterEntity` 随被覆盖的文件消失 | 输出里同一路径出现两次 | 升级 codegen(现在会在写文件前拒绝并说明);拿掉 `-output` 让每个实体各占一个文件 |
| T-57 | RoomManager 构造与 Start 都成功,随后后台 goroutine panic `non-positive interval for NewTicker` | core ≤ v1.15.2 的 `SweepInterval` 默认值按 `IdleTTL/2` 整数除法推导,任何小于 2ns 的有效 `IdleTTL` 都得 0 | `IdleTTL` 配得极小且 `SweepInterval` 留空 | 升级 core(派生值现在有下限);旧版本显式给一个正的 `SweepInterval` |
| T-58 | 两个玩家在同一队列用了同一个 requestID,后一个拿到前一个的 ticket 与分数,而且自己没进队 | kit ≤ v1.14.3 的 `Enqueue` 幂等重放只按 requestID 查,不校验票的归属;读取侧的 `Ticket` 一直校验 | 指标 `refused:enqueue:request_subject_mismatch`(新);旧版本只能从票的 Subject 与调用方不一致看出 | 升级 kit;requestID 应按玩家或全局唯一生成,同队列撞键是客户端缺陷 |
| T-60 | 升级后 `roost generate` 成功,业务工程编译报 `undefined: view`(或别的 category 常量所在包) | codegen v1.15.4 之后的 `category=` 标记支持里,`collectEntityImports` 没有收集 category 的限定包;Go 的 import 作用域是单文件,源文件导入不算 | 只在 category 表达式里用到该包的实体才会犯 | 升级 codegen 并重新生成;旧版本临时把 category 换成 `entity.EntityCategory*`,或在实体文件里让该包被别的字段也引用 |
| T-61 | 生成的 `registry` 聚合编译报 `entity redeclared in this block` 与 `undefined: entity.RegisterEntity` | 某个注册包路径以 `/entity` 结尾,被别名分配器分到 `entity`,与模板无条件导入的 core entity 包撞名 | 生成物里有两条 import 的名字都是 entity | 升级 codegen 并重新生成;旧版本把该包改名或换路径 |
| T-62 | 高并发提交下 `FlushRemoteTransaction` 所在 goroutine panic `invalid memory address or nil pointer dereference`,随后所有事务跟踪调用挂住 | core ≤ v1.15.2 的等待者唤醒后重新查 map 取状态,而准入的容量淘汰恰好挑"最旧的已关闭"记录,也就是刚把它唤醒的那一条;panic 发生在 txMu 临界区,Unlock 未执行 | `TransactionTrackLimit` 偏小且提交并发高;挂住的调用栈停在 `txMu.Lock` | 升级 core;旧版本调大 `TransactionTrackLimit` 降低淘汰概率 |
| T-64 | 读了一次 `Service.Mailbox` 并改了返回值之后,存储里的领取记录跟着变了 | kit v1.14.3 之后的 `Mailbox.clone` 没有拷贝 U-0165 新增的 `SettledClaims`,`out := m` 只复制 map 头 | 只在进程内 MemoryStore 上确认;Redis 每次解码新对象 | 升级 kit;在此之前不要修改 `Mailbox` 读取结果 |
| T-65 | 一封早已领取的附件邮件重新可领并拿到新 token,而信封还没过期 | kit v1.14.3 的墓碑按条数收界,计数保证不了时间期限:同一信封生命周期内有 `MaxSettledClaims` 条更新的已领取邮件被淘汰,它就被挤掉了 | 邮箱 `Evicted` 很大而墓碑数恰好在上限 | 升级 kit(墓碑改为按信封可领取窗口保留,挤不下时以 `ErrClaimHistoryFull` 拒绝投递);发奖侧不要只靠 token 去重 |
| T-66 | 停机期间带 delay 的同步 Request 既没有结果也没有错误,最后报成取消或超时 | core ≤ v1.15.2 的 `Dispatcher.OnDestroyWithContext` 回收延迟队列时不向 `RetChan` 发终态 | Shutdown 返回成功,而调用方在自己的超时后才失败 | 升级 core;旧版本停机前先等延迟队列排空 |
| T-68 | 权威快照后端变慢时,读请求持续报 `ErrRemoteOverloaded`,而并发数远低于 MaxWaiters | core ≤ v1.15.2 的 `loadMonotonic` 在跟随者取消时不归还名额,上限变成"本次加载累计入场次数" | 首个加载结束后自行恢复 | 升级 core;临时调大 MaxWaiters 或拉长调用方超时 |
| T-69 | 读到的远程快照 `Expired(now)` 为真却被当作命中返回 | core ≤ v1.15.2 的 `Get` 只看容器 TTL,不看信封自己的 `ExpiresAt` | 只影响显式设置了 `ExpiresAt` 的快照 | 升级 core(过期即视为未命中,Monotonic 会回权威) |
| T-70 | 同一版本的快照在不同进程解码结果不同 | core ≤ v1.15.2 的 L2 CAS 只比 checksum,而 checksum 只覆盖 payload 字节,schema / codec 可以被同版本改掉 | 本地缓存拒绝而 L2 接受同一次发布 | 升级 core 并让所有发布方同时升级 |
| T-71 | 版本号超过 2^53 之后,较旧的快照写入被接受、版本回退;或真正更新的版本被判为同版本冲突 | core ≤ v1.15.2 的 L2 脚本用 Lua 的 `tonumber` 比较,Lua 数值是 float64 | 版本量级在 9007199254740992 附近 | 升级 core;迁移或导入大版本号前先升级 |
| T-72 | `GetOwnership` 报 `invalid marker lease "shared:1001:1e+14:1"`,且此后一直失败 | core ≤ v1.15.2 的 marker 脚本拼接 Lua 数值,Lua 5.1 用 `%.14g` 渲染,10^14 变成科学计数法并已写入 Redis | lease 字符串里出现 `e+` | 升级 core;已损坏的记录要人工改回十进制或重新 claim |
| T-74 | 同一快照版本,本进程 L1 与 Redis L2 内容不同,而发布返回成功 | core ≤ v1.15.2 对 L2 的 `ErrRemoteVersionConflict` 与网络错误一视同仁地降级 | 冷 L1 进程发布时 L2 已有同版本不同内容 | 升级 core(冷 L1 发布先问 L2 一次,冲突即报错);排查两边发布方谁写了不同内容;复核补修后 L2 CAS 的冲突回答不再被 IgnoreRemoteError 降级 |
| T-75 | 刚发布的快照被"读一次"后变回旧内容 | core ≤ v1.15.2 的 L2 回填直接覆盖 L1,不做同版本内容校验 | 读与发布并发、L2 仍是旧内容 | 升级 core(`Conflict` 钩子在分片锁下把关,回填不覆盖) |
| T-76 | MemoryStore 上被拒的投递之后,邮箱 `Unread` 与实际未读条目数不一致,墓碑越过上限 | kit ≤ v1.14.3 的 mail Update 回调直接改共享 map 再返回 save=false | 仅 MemoryStore;Redis 每次解码新对象 | 升级 kit(回调先 clone) |
| T-77 | 事务已持久化(WAL LSN 前进、状态已变)但调用方收到超时/取消;或 completion 饱和时进程直接退出 | core ≤ v1.15.2 的 `Commit` 一个业务 AfterCommit panic 就跳过后续回调与回复;饱和内联回退无恢复边界 | 日志有 `after-commit callback panic`;`async_total{result="completion_failed"}`(新) | 升级 core;业务按幂等重试;回复里的 `ErrAfterCommitFailed` 表示"已提交但收尾失败",不要当回滚处理 |
| T-78 | 某消费者续租了兴趣,却收不到后续快照更新,本地又认为兴趣有效 | core ≤ v1.15.2 的兴趣 release 按 key/SID 无条件删,迟到的旧 release 取消了新 renewal | 网络重排或重放时出现;下一次成功续租可恢复 | 升级 core(renew/release 带只增代际);混合部署期旧版本消息代际为 0,偏向保留兴趣 |
| T-79 | `WAL.Sync` / `Committer.Flush` 成功返回后,刚 Enqueue 的 ticket 仍未 Done、`DurableLSN` 没前进 | core ≤ v1.15.2 的 `Sync` 只 fsync 当前文件,不等 writer 写完队列里已准入的记录 | 记录还在 appendCh 或 BatchDelay 收集窗口内;`Stats().Queued > 0` 而 Sync 已返回 | 升级 core(Sync 先过写屏障再 fsync) |
| T-80 | 带超时的 `Committer.Shutdown` / `Flush` 超时后仍不返回,直到后台 replay 的 applier 结束 | core ≤ v1.15.2 用 `sync.Mutex` 等 replay / flush 所有权,截止时间管不到等锁 | goroutine dump 里 Flush 停在 `replayPass` 的 Lock;applier 在等下游 | 升级 core(信号量等待随 ctx 取消;超时返回 ctx 错误且已触发 Close,再调一次不带超时的 Close 等后台退出) |
| T-81 | Remote 快照缓存里已删的实体又能读到旧快照,或刚发布的新快照被一条迟到的删除清掉 | core ≤ v1.15.2 的复制侧删除不看版本、不留墓碑 | 消息乱序 / outbox 重发 / 历史流恢复时出现;真实 broker 的发生频率未测 | 升级 core(`DeleteAtVersion` + `TombstoneTTL` 内的墓碑,默认等于缓存 TTL);墓碑过期后极晚的旧快照仍可复活,属协议边界;复核补修后墓碑对 L2 回填同样生效,L2 删除按版本(`DeleteAtVersion` 脚本) |
| T-82 | Transfer 返回 I/O 错误后旧 owner 仍能 `PrepareRemoteWriteBatch`,而 Redis 里 owner 已是新节点 | core ≤ v1.15.2 把 `TransferExpected` 的任何错误当"没执行",恢复 local_owned 且热 marker 继续被信任 | 错误发生在 Lua 执行之后(回复丢失 / 超时);live 为 local_owned 而权威 owner 不同 | 升级 core(失效 marker、独立重查权威:已转移→fenced 并按成功返回,查不到→recovering 冻结直到权威可读);EnterShared / LeaveShared 的同类分支尚未改 |
| T-83 | `EnterRemoteSharedMode` / `LeaveRemoteSharedMode` 返回 I/O 错误后,本地模式与 Redis 里的 shared 标志相反(如本地 local_owned 而权威 shared),独占写照常放行 | core ≤ v1.15.2 对 Enter/Leave 的任何存储错误恢复旧模式,不清 marker、不重查 | 错误发生在 Lua 执行之后;live 状态与 `GetOwnership` 的 Shared 不一致 | 升级 core(与 Transfer 同一套收尾:权威确认已切→按成功同步,查不到→recovering 冻结);发送前失败仍恢复原模式 |
| T-84 | 停机时 `Flush` / `Sync` 立刻成功,但随后 `Close` 还在跑、刚 Enqueue 的 ticket 未 Done | core ≤ v1.15.2(含 U-0185 之后、v1.15.3 之前)把 Close 发起当成排空完成 | 只在 Close 与 Sync 并发、writer 恰好持有未写批次时出现 | 升级 core(关闭进行中的 Sync 等 writer 排空,按 ctx 超时;关闭完成后仍幂等) |
| T-85 | 跨服活动成功打开、首个 game 已通知,宽限期过了却一直 collecting,`PendingActivities` 里没有它 | kit ≤ v1.14.3 的窗口条目先进后建记录,sweep 把"有条目无记录"一律当死条目删,与 OpenActivity 交错时删掉了正在建的 | 只在 sweep 恰好落在 admit 与 Create 之间时出现;活动记录存在但窗口没有 key | 升级 kit(窗口条目分 opening / 确认,回收只删超过 `OpeningGrace` 且仍在 opening 的);临时:管理端重新 notify 不会修,需要把 key 补回窗口 |
| T-86 | 活动已完成,game 的 dispatch 长期 pending 且 due,Attempts 停在 0 或 1 不再增长 | kit ≤ v1.14.3 的 sweep 只对本轮刚到期完成的活动派发,完成后出窗口就再也不扫 | 由最后一个 notify 完成、或首次派发 Create 失败、或退避到期的重试,都命中 | 升级 kit(`Window.Delivering` 持久索引,每轮 sweep 从它重试,全部终态才退出);临时:显式调 `DueDispatches` → `AttemptDispatch` |
| T-87 | lockstep 里一次性操作(开火 / 技能)被执行两次,两帧都带同一个玩家的同一输入 | core ≤ v1.15.2 的 Sequencer 只在目标帧内去重,切帧后重传按迟到折入下一帧 | 客户端 datagram 冗余重传跨过了切帧;帧 N 与 N+1 有同一玩家相同 payload | 升级 core(每玩家 64 帧内的原始帧号身份表,重传幂等) |
| T-88 | 重连的会话一直在追帧,`CatchingUp` 不结束,收不到 live 帧 | core ≤ v1.15.2 接受 `CatchupBatchFrames=1`,每 tick 产一帧补一帧,积压不变 | 追帧页数持续增长而 next 与 head 差值恒定 | 升级 core(NewRoom 拒绝 1);配置改为 >= 2 |
| T-89 | 同一个 session 既是座位又是旁观者,广播发两份,旁观者 detach 清掉了座位的追帧 | core ≤ v1.15.2 允许座位号 -1,与旁观者哨兵撞值 | 房间配置里出现负座位 | 升级 core(负座位在创建时拒绝);座位号用非负值 |
| T-90 | 房间正常 Tick,客户端解码报 `frame encoding corrupt: input count N`(N > 256) | core ≤ v1.15.2 只按字节预算校验座位数,没对齐 wire 的 MaxFrameInputs | 座位数超过 256 且 datagram 上限调大 | 升级 core(创建时拒绝);超过 256 座位需要协议升级 |
| T-91 | lockstep 房间内存随比赛时长增长,Tick 耗时越来越高,且异常客户端能加速这一过程 | core v1.15.2+U-0193 的去重身份表准入无上限,只有 Advance 按期限清理 | 客户端在两次 tick 之间提交大量不同的旧帧号;`RoomTickTenPlayers` 这类基准随运行时长变慢 | 升级 core(每座位固定 129 槽的环,准入即有界,取消每 tick 的清理遍历) |
| T-92 | 压测机器人 / 客户端在一次回调出错后继续跑,hash 对不上或负载结果失真,模拟少了几帧 | core ≤ v1.15.2 的 LockstepBot 把 Assembler 的接收游标当成应用游标,apply 中途失败时批次尾帧被丢弃 | 日志里有一次 `submit input` / `report hash` / `simulate` 错误,之后调用都成功;`Stats().FramesApplied` 少于已收帧数 | 升级 core(出站失败保留未完成的帧与步骤并续做;Simulate 失败转 terminal,`Terminal()` 可查,需重建 Bot) |
| T-93 | 32 位构建(GOARCH=386/arm)的 lockstep 房间在收到某个客户端输入后 panic `index out of range`;或房间内存里出现明明被拒绝的座位的身份环 | core v1.15.2+U-0193 的 `SubmitInput` 先按客户端给的帧号索引身份环、再做窗口校验,`int(uint32)` 在 32 位上溢出为负下标 | 客户端提交了一个极大的帧号(> next+window);64 位平台只表现为多余的内存分配,不 panic | 升级 core(校验前移、下标在 FrameID 域取模);未升级前在接入层先拒绝 `frame > NextFrame()+SubmitWindow` |
| T-94 | statesync 客户端 `ApplyDelta` 一直成功,但某个对象永远缺失;服务端 delta 变化数为 0 | core ≤ v1.15.2 允许同一 tick 被重新投影并覆盖 `sent[tick]`,迟到的旧 ACK 绑到新视图 | 同 tick 内有兴趣变化(投影结果不同)且第二次发送丢包 | 升级 core(一个 tick 一个视图:同 tick 再准备复用已发送视图,不同视图的提交按 stale 拒绝);兴趣变化最多延迟一个 tick 生效 |
| T-95 | 调了 `ForceFull` / 收到 Resync,下一帧却还是 delta | core ≤ v1.15.2 的两个 ACK 入口在推进水位时把 forceFull 清掉,ForceFull 之前发出的帧的 ACK 迟到即取消恢复 | ACK 到达时间晚于 ForceFull 调用 | 升级 core(ACK 只推进水位,恢复意图由当前 generation 的全量帧提交释放) |
| T-96 | 接收端 `MaxFrameBytes` 调小后,单片帧仍被 Reassembler 放行,只有分片帧被拒 | core ≤ v1.15.2 的单片快路径不做帧大小检查 | 仅 `MaxFrameBytes` 小于单片可容纳载荷时出现;后续 `DecodeFrame` 仍会拒绝 | 升级 core(单片与多片同一判据) |
| T-97 | 限频(LOD MaxRateHz)组件在某些会话上长期不更新,ACK 正常,权威值早已变化 | core ≤ v1.15.2 按绝对 tick 取模采样,会话的发送相位与采样点错开就永远不刷新 | 会话不是每个 tick 都发送(降频 / 错开批次 / 跳过采样点) | 升级 core(按"自上次已提交发送是否跨过采样点"刷新,与相位无关) |
| T-98 | 客户端 `ApplyDelta` 报 `object limit exceeded` / `component limit exceeded`,但前后两份权威快照都在上限之内;换一个 ID / TypeID 就不报 | core ≤ v1.15.2 的 ApplyDelta 在每一步操作后用最终存量上限检查暂存集合,满容量替换先 Create 后 Remove 即被拒 | 对象数或组件数恰好在上限,且替换进来的标识比替换出去的小(先创建后删除) | 升级 core(上限只约束最终集合,过程上界 2×);未升级前调大上限或避免满容量替换 |
| T-99 | 自定义异步运输下,某会话的客户端比服务端多(或少)一个对象,之后 delta 全部成功且零变化,永不收敛 | core ≤ v1.15.2(含 U-0200)同一 tick 两次 PrepareLatest 都早于第一次 Commit 时视图可以不同,第二份被交付后 Commit 才拒 | 同 tick 内有兴趣 / 质量档变化,且运输在 Commit 之前交付 | 升级 core(视图在第一次投影时钉住,同 tick 所有准备同一视图;分叉提交被拒即进入全量恢复);未升级前遇到时对该会话 ForceFull |
| T-100 | 开了持久化水位门槛(`SetDurableWatermark`),进程故障恢复后客户端见过的实体状态在 WAL 里找不到;`entitysync_flush_gate_deferred_total` 在涨但状态还是出去了 | core ≤ v1.15.2 只在 `FlushSubject` 查水位,新订阅 / profile 切换 / `Distribute` 直接外发 | 故障前有新进入兴趣集、profile 变化或自定义 tick 分发 | 升级 core(门槛跟着被捕获内容的 `CommitLSN` 走,三个入口同一判据;`Subscribe` 推迟返回 `ErrDurabilityDeferred`,调用方重试) |
| T-101 | 慢连接被剔除后(`SlowConsumerEvictions` 在涨、`DeadSessions` 非零),room 里它的订阅还在,`ActiveSubscribers` 不减,继续为它做 profile / 信封 | core ≤ v1.15.2 的 `RoomTransportSink` 只在整批成功时派发剔除通知,剔除后剩余路由再失败就把通知丢了 | 剔除紧接着一次 transport 临时错误或取消;之后的重试不会补通知 | 升级 core(通知随错误一起带回,两条出口都派发);未升级前对该 session 手动 `ReleaseSession` + 退订 |
| T-102 | 调过 `RoomBroadcaster.SetDownstream` 之后,新 sink 的 `SlowConsumerEvictions` 在涨、`OnSlowConsumer` 也跑了,room 的订阅和名额却不释放 | core ≤ v1.15.2 的 `SetDownstream` 只换指针,room 的慢消费者回调仍注册在旧 sink 上 | 只在替换过 downstream 的 room 出现;旧 sink 的 handlers 里还有该 room | 升级 core(替换即交接:新 sink 先注册、再切换、再解除旧注册);未升级前替换后自行 `RegisterRoomSlowConsumerHandler` |
| T-103 | 切到 JetStream 总线后,同一进程里同 topic 的多个同步器各自只收到一部分消息;分片流谁都重组不出 | core ≤ v1.15.2 每次 Subscribe 用同名持久消费者各自 Consume,服务端按工作队列分摊 | 同 bus 同 topic 注册了多个 handler;普通 NATS 总线下正常 | 升级 core(一个 topic 一个底层订阅,本地扇出) |
| T-104 | 两个用不同 Prefix 的 JetStream 总线共用一个 Stream,消费者相互覆盖 / 被服务端拒绝 | core ≤ v1.15.2 的 durable 名不含 Prefix | 同 Stream、同 LocalSid、同 topic、不同 Prefix | 升级 core(非默认 Prefix 进入身份);仍建议不同 Prefix 各用一个 Stream(Stream 的 Subjects 所有权未解决) |
| T-105 | syncstream History 第二次启动报 `decode WAL: invalid character '{' after object key:value pair`,而上一次启动是成功的 | core ≤ v1.15.2 恢复时忽略了半条尾记录却没截掉,续写拼在其后 | 上上次运行在写 WAL 时被中断 | 升级 core(首次续写前截断到最后完整记录);未升级前手工删掉 WAL 末尾的半行;Windows 上若见 "append truncate …: Access is denied",是 09-16 之前的补修版本,升级 |
| T-106 | syncstream 重启报 `ErrInvalidSnapshot`(WAL 里同一序号两条),或重启后丢了已成功的追加 | core ≤ v1.15.2 把 fsync / 发布报错当成没写,继续用旧内存状态写 | 磁盘 / 目录同步报错之后进程还在继续 Append 或 Checkpoint | 升级 core(fail-stop:`ErrHistoryJournalFailed` 后必须重开 journal);未升级前一遇 journal 错误就重启 |
| T-107 | 对绑定 journal 的 History 调过 Import / Restore,重启后又回到旧快照,或 `ErrInvalidSnapshot` | core ≤ v1.15.2 的 Import 只换内存不发布 | 运行时替换过快照且之后有追加 | 升级 core(Import 先经 Checkpoint 发布);未升级前 Import 后立刻 `Checkpoint()` |
| T-108 | 接收方按更高序号应用了一个 Full,内容却比之前的旧;或旧内容出现在新 epoch 里 | core ≤ v1.15.2 的 Recover 不核对 provider 捕获期间的变化 | Recover 与同流 Append / RotateEpoch 并发 | 升级 core(提交前核对,`ErrRecoverStale` 时重试) |
| T-109 | 删流 / 删 observer / IdleTTL 清理后同一身份重建,旧连接的 ACK 把新快照裁掉,Resync 说无需发送 | core ≤ v1.15.2 重建的流从序号 1 重来 | 同 Observer(含 Session)/Stream 身份在同一 epoch 内复用 | 升级 core(epoch 内序号地板,新建流首序号不再是 1——消费方若假设首序号为 1 需核对) |
| T-110 | Recover 返回的 Full 内容比之前的更旧,且序号更高;日志里同时能看到期间的 DeleteStream / DeleteObserver / Import | core ≤ v1.15.2 的 Recover 只比较 epoch / 存在性 / latest,删后重建或同位置导入后位置相同、核对通过 | provider 运行期间有删除或 Import;`ErrRecoverStale` 从不出现 | 升级 core(修改代数 `revision`);调用方对 `ErrRecoverStale` 做有限重试而不是重新读一次就提交 |
| T-111 | 给 `match.NewMod` / `Config.Grouping` 注入了自定义匹配策略,匹配结果却完全按先来先到 | kit ≤ v1.14.3 保存该参数但没有任何执行路径;成组由调用方 Candidates → Commit 驱动 | 搜索 store 里对 `Grouping` 的调用为空;自定义策略的计数器不增长 | 升级 kit(参数已删,`NewMod(reporter)`),把策略放进应用的 matchmaker(见 codegen game-demo `internal/service/<game>/matchmaker.go`) |
| T-112 | 生成的工程 `go build` 报 `internal/service/match/collaborators.go: "…/roost-kit/service/match" imported and not used` | codegen v1.15.5 的 collaborators 渲染无条件 import 服务包,match 正文自 U-0217 起不再引用它 | 只出现在托管 match 服务的工程,文件里没有 `match.` 选择表达式 | 升级 codegen ≥ v1.15.6 重新生成,或手删那一行 import(业务文件,不回写) |
| T-113 | 进程关停后某个 manager 的 goroutine / 监听仍在，日志里它是最后一个 `manager started`，之后没有 `manager stop` | core ≤ v1.15.5 的 Engine 只在下一轮循环顶部检查关停，Stop 与最后一个 Start 交错时没人负责清理 | Start 返回 nil 而 Stop 已先返回；再补一次 Stop 才停 | 升级 core（U-0219）：Start 成功后在锁下交接，Stop 已生效则 Start 自己停掉并报 `start aborted by shutdown` |
| T-114 | `Manager(name)` 能看到一个从未 `manager start` 的对象，用它的资源报 nil | core ≤ v1.15.5 的 Register 在首个管理器慢 Start 期间仍接纳（`started` 尚为 nil） | Register 返回 nil 而该对象不在 Start 的快照里 | 升级 core（U-0220）：Start 取快照即关闭注册，返回 `ErrRegisterAfterStart`；条件装配要在 Start 之前完成 |
| T-115 | 匹配票出现在别的模式 / 分区的 Candidates 里，或二人队列 Commit 了三人队列的票 | core ≤ v1.15.5 的 `Queue.Key` 冒号拼接非单射，Mode / Partition 含 `:` 的两个队列共用存储与 affinity | 两个 Queue 的 `Key()` 相等 | 升级 core（U-0221）：Key 转义自由字段（不含 `:` `%` 的键不变），Candidates / Commit 校验归属；曾含 `%` 的 Mode / Partition 键会变，需迁移 |
| T-116 | ScoreWindow 把分差极大的两人配成一组，或窗口配置很大时明明在 cap 内却不成组 | core ≤ v1.15.5 的距离 / 窗口用 int64 有符号算术，回绕 | 分数含 MinInt64 / MaxInt64 量级，或 InitialWindow + Widen*waited 超 MaxInt64 | 升级 core（U-0222）：uint64 无溢出距离与饱和窗口；负窗口配置现在报 `ErrQueueInvalid` |
| T-117 | 单进程 / 测试里改了 Enqueue 返回票的 Payload 或 Match 的 Members，之后从内存 Store 读回的也变了 | core ≤ v1.15.5 内存 Store 浅复制切片字段 | 只在 `NewMemoryStore` 出现，Redis 后端不复现 | 升级 core（U-0223）：边界深复制；调用方仍不应把返回值当共享状态改 |
| T-127 | pipelined 提交的部署里，房间把尚未持久的实体内容发给了客户端（断电 / 崩溃后客户端看到过不存在的状态） | core ≤ v1.15.6 的 RoomBroadcaster 私有持有 coordinator，房间与管理器的配置都没有水位源入口 | 只在 pipelined committer 下成立；同步持久提交不受影响 | 升级 core（U-0233）：`RoomManagerConfig.DurableWatermark` / `RoomBroadcasterConfig.DurableWatermark` 填 `PipelinedTransactionCommitter.DurableLSN` |
| T-126 | 改了嵌套结构里更深一层的字段（`equip.GetGems(1).SetLevel(...)`），内存里生效但落库没有、回滚快照与同步也看不见 | codegen ≤ v1.15.9 的嵌套模板三个装载入口都不把 child 接到父级的 Mark | 直接改父级字段会落库，改 child 不会 | 升级 codegen（U-0232）重新 `make generate`：setter / raw 恢复 / BSON 恢复都接线，替换时解绑旧 child |
| T-125 | 用原生 Nest 步骤（`SubscribeDataEngineStep` + `EmitCompletion`）的 saga 永远停在 waiting，步骤按超时反复重发但业务不重跑 | core ≤ v1.15.6 的 Assembly 不订阅效果流上的 `saga.result.*`，原生完成没有任何消费者 | `gm.saga.get` 看 status 不动、attempt 递增；步骤的 inbox 里有回执；用 `SubscribeMongoStep` 的 saga 正常 | 升级 core（U-0231）：Assembly 自带第三条消费者；kit 可用 `saga.result_effect_*` 调它的 durable 与限额 |
| T-124 | 写了 `//roost:attribute` 的 profile 之后，`game/gameplay/attribute` 包编译不过：`undefined: AttrID` / `AttributeProfile` / `Snapshot` / `Container` … | codegen ≤ v1.15.9 只有生成器，框架半没有落地，脚手架只产出空包 | 空 feature 能编译、写了 profile 就不能；报错都是"未定义的类型"而不是语法错误 | 升级 core（≥ v1.15.7，新增 `roost-core/attribute`）与 codegen（U-0230）后 `roost project sync`：脚手架会写 `game/gameplay/attribute/runtime.go`；profile 结构体要自带 `dirtyMask uint64` |
| T-123 | 给实体加 `sync=true` 后工程编译不过：`unknown field FlushPolicy` / `undefined: entity.SyncFlushOnEntityRelease` / `unknown field SubjectPackerFactory` | codegen ≤ v1.15.9 的实体生成器与 Core 的 `EntitySyncBuilderParam` 漂移 | 只在带 `sync=true` 的实体上出现；`sync=false` 正常 | 升级 codegen（U-0229）重新 `make generate`；marker 里两种 packer 拼写只留一个（`subjectPacker` 为现行） |
| T-122 | game-demo 的一场战斗里没人发输入时，房间一帧都不切、日志只有 `room open` 然后超时退出 | codegen ≤ v1.15.9 的 battle 房间只有收到 command 才开帧，宽限期常量没有事件源 | 无输入的房间 `frames_cut=0`；有输入的房间正常推进 | 升级 codegen（U-0228）重新生成 demo：宽限期是独立计时器，到点开帧；绝对上限改为 `battleMaxLifetime` |
| T-121 | 生成工程编译报 `game/handler/<x>_nest_gen.go: "<module>/<pkg>" imported and not used` | codegen ≤ v1.15.9 的 nest 生成器给 handler 半也收集返回类型的包，而 handler 半不写出返回类型名 | 该 handler 的返回类型来自既非实体参数也非普通参数的第三个包 | 升级 codegen（U-0227）重新 `make generate`；临时可把返回类型挪进已被参数引用的包 |
| T-120 | 玩家反复发同一条 FinishDungeon 就能不断拿到经验；或 run 已 failed / expired，响应里却带 `ExpGranted` | game-demo ≤ codegen v1.15.9 的端点按请求里的 `Success` 判定，而 `session.Finish` 对终态 run 幂等返回 | 响应同时出现 `State:failed/expired` 与非零 `ExpGranted`；或重放后 Mongo 里 level 继续上涨 | 升级 codegen（U-0226）重新生成 demo：按 `run.State` 判定并经 ClaimDungeon 事务的 run id 账本发奖；已发多的经验按运营口径回收 |
| T-119 | saga 步骤返回不可重试失败（或重试用尽）后记录一直 `waiting` / `pending`，`last_error` 反复是 `step result timeout`、`attempt` 递增；进程日志 `saga: consumer processing failed … kind=completion … err="saga: invalid record"` | core ≤ v1.15.5 的 applyCompletion / retryOrCompensate 与 beginCompensation 各加一次记录版本，MongoStore.Apply 只接受 expected+1 | `gm.saga.get` / 查 saga 集合：status 不动、attempt 递增；日志 kind=completion 的 invalid record；内存 Store 不复现 | 升级 core（U-0225）；已卡住的记录用 `Engine.Compensate`（只加一次版本，一直可用）推进或等 deadline 路径 |
| T-118 | Mongo 里 DAO 文档的嵌套字段是 `{"dirtyhook": {}}`，重启后嵌套 struct 全是零值；`map[K]*Nested` 每个值也是空子文档 | codegen ≤ v1.15.8 的嵌套 struct 无 MarshalBSON，字段未导出，反射只编码内嵌 DirtyHook | `db.<coll>.findOne()` 看嵌套字段内容；回滚快照与同步同丢 | 升级 codegen（U-0224）重生成 `gen_*_nested.go` 与 `gen_*_dao.go`（补修后父 DAO 文档直接放 wire 形式，多出一份 `gen_dao_bson_helpers.go`）；历史文档里的嵌套数据本来就没存过，无迁移可做 |
| T-73 | 一条快照消息按某个 scope 路由,却改动了另一个 scope 的缓存 | core ≤ v1.15.2 的 `ApplyReplica` 不校验 payload 里的 key / version 与信封是否一致 | 需要发布方有缺陷或能写内部 topic | 升级 core(不一致即拒绝,不落写) |
| T-67 | `StopFinalizer` 成功返回后再 `Close` 批次,`Close` 返回 nil 但 writeGate、ownership 读锁与 finalize slot 没有释放 | core ≤ v1.15.2 的 `deferRemoteClose` 靠 select 分支退出,而队列可写与 ctx 已关闭同时就绪时 Go 随机选,交接给了没人排空的队列 | 停止后遗留的 slot 数不固定;`finalizeOnce` 使重启无法收拾 | 升级 core;旧版本先排空所有 batch 再停 finalizer |
| T-63 | 从 checkpoint 恢复后,`InspectCast` 能查到的已完成技能与恢复前不是同一批 | core ≤ v1.15.2 的快照按 ID 序列化 casts,恢复时按这个顺序重建完成队列,而淘汰是从"最旧完成"的队头走 | 创建顺序与完成顺序交错、且完成数超过 `CompletedCastLimit` 时才出现 | 升级 core 后重新生成 checkpoint;旧快照恢复后退化为按 ID 顺序,与旧行为一致 |
| T-59 | 已领取过的有附件邮件重新变成未读并可再次领取,`ReserveClaim` 返回一个新 token | kit ≤ v1.14.3 的容量淘汰把终态 Entry 连同 claim token 整条删除,而重复投递把"条目不在"当成"从未投递" | 指标 `refused:reserve_claim:already_claimed`(新);邮箱 `Evicted` 在增长 | 升级 kit(淘汰改留极小墓碑,墓碑有自己的上限);发奖侧不要只依赖 token 去重,同时校验业务幂等键 |
| T-55 | 一个包里有两个 `//roost:entity` 实体，`roost entity` 生成成功，`go build` 报 `registerEntityOnce redeclared in this block` / `RegisterEntity redeclared` | codegen ≤ v1.15.4 每个生成文件都声明包级 `registerEntityOnce` 与 `RegisterEntity` | 报错的两个文件都是 `*_gen_wire.go` | 升级 codegen 后重新生成（每实体 `register<Name>Entity`，包级 `RegisterEntity` 只在按名排序的第一个实体文件里）；旧版本只能一包一实体 |
| T-52 | `roost project upgrade --consolidate` 报 `rewritten source does not format: N:2: expected declaration, found coreredis`，该文件未改写 | codegen ≤ v1.15.3 对只有单行 `import "…"` 且混用 kit / core 符号的文件，把新 import 直接塞到顶层 | 报错文件的 import 是单行形式 | 升级 codegen；旧版本可先把该文件的 import 改成括号块再升级 |
| T-49 | 开了流水线提交（`DurabilityPipelined`）后，写完最后一笔常要等约 1 秒 Mongo 里才看得到；严格提交没有这个延迟 | core ≤ v1.15.0 的 `Projector.Enqueue` 落盘完成时不唤醒投影循环，尾部记录只能等 `IdlePoll`（默认 1s）轮询 | `dataengine` 指标里 `WALUnacked` 在一批写入后停在非零约 1 秒再归零；只发生在流水线提交 | 升级 core（票完成即唤醒）；旧版本可临时把 `ProjectorOptions.IdlePoll` 调小 |
| T-48 | 某封邮件在列表页读取时整页报 `decode envelope <id>` 失败，但单独读这封邮件却是"不存在" | kit ≤ v1.14.1 的 `redisEnvelopes.Get` 把"键存在但值为空"当作不存在，`GetMany` 对同一个值报解码错误；两条读路径对同一条损坏数据答案不同 | Redis 里该键 `GET` 返回空串（`STRLEN` 为 0）而 `EXISTS` 为 1 | 升级 kit（两条路径同报解码错误）；删掉该键或按 send 账本重投；查写入侧为何落了空值 |
| T-47 | match 的过期票或 activity 的宽限窗口长期不被后台推进，日志里每个 tick 一条 `sweep failed` / `advancing expired activities failed`，但指标面板看不出异常 | kit ≤ v1.14.0 的后台 sweep 失败只打日志不计数，与"无事可做"在指标上无法区分 | `dropped:sweep.failed`（match）、`dropped:sweep.advance_failed` / `sweep.due_read_failed` / `dispatch.attempt_failed`（activity）、`dropped:prune.failed`（chat）是否持续增长 | 升级 kit；把这几个计数接进告警 |
| T-46 | 跨服活动在某个游戏服沉默后一直停在 collecting，宽限窗口（`activity.grace_window`）过了也不完成，只有再有人 notify 时才被推进 | kit ≤ v1.14.0 的 activity `Server` 后台 sweep 遍历的是一个返回 nil 的桩，没有配置入口，`AdvanceExpired` 从未被后台调用 | activity 进程启动日志里是否有 `no sweep groups configured`；`PendingActivities(group)` 是否堆积 | 升级 kit；在拥有 activity 服务的进程配置 `activity.sweep_groups: [<group_id>...]`（组是部署事实，不做 keyspace 扫描） |
| T-45 | `roost project upgrade --consolidate` 之后编译报 `undefined: natsdriver.Permanent`（或 `xxx.Permanent`，xxx 指向 `roost-core/nats/driver`）、`undefined: syncstream.HealthOptions` / `syncstream.HealthStatus` | 升级器只改包路径，不维护符号改名表（codegen ≥ v1.15.2）。`Permanent` / `IsPermanent` 是契约级符号，收敛后在 `roost-core/nats`（不在 driver 子包）；kit 的 `syncstream.HealthOptions` / `HealthStatus` 并入 core 时因与 core 已有的同名契约类型冲突，改名 `PublisherHealthOptions` / `PublisherHealthStatus` | 编译错误指出的文件与符号 | `Permanent` 改 import `github.com/tjbdwanghaibo/roost-core/nats`；两个 Health 类型改用 `Publisher` 前缀的名字。都是一次性手改，业务工程里通常只有装配文件涉及 |
| T-44 | Saga 步骤收件箱把同一个命令 ID 的两次不同内容当成重投递（`duplicate=true`，返回上一次的完成结果）而不是 `ErrIdentityConflict`；或 `Handle` / `Reserve` / `Apply` 返回 `saga: invalid record: command digest: json: unsupported value` | 命令 / 回执的身份摘要靠 `json.Marshal`；core ≤ v1.14.0 丢弃了它的错误，无法序列化的输入（如年份超出 [0,9999] 的 `DeadlineAt`）全部共享 sha256(nil) 一个摘要 | 命令的 `DeadlineAt` / `CreatedAt` 是否越界；日志里的 `command digest` / `completion digest` 错误 | 升级 core（摘要函数返回错误、入口拒绝）；修正产生越界时间的调用方。`Bus.RequeueDeadLetters` 同一修复：条目无法序列化时报错而不是发布 |
| T-43 | Redis 慢或网络延迟时，带短超时 ctx 的调用（分布式锁 `Acquire`、任何 `IRedis` 方法）返回时间远超 ctx 截止期，稳定等于 `redis.read_timeout` | go-redis 默认 `ContextTimeoutEnabled=false`：命令等回复只看 ReadTimeout，不看 ctx。kit ≤ v1.12.5 未打开 | 看调用耗时是否恰好等于 read_timeout（默认 2–3s）而不是 ctx 预算 | 升级 kit（客户端已启用 ContextTimeoutEnabled）；没给 ctx 截止期的调用仍由 read_timeout 兜底 |
