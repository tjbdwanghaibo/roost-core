# 核心模块优化合并验收（2026-09-25）

范围：`feature/arch-10-sync-policy` 上累计完成的 Sync、Nest、DataEngine、Remote 及必要 kit/codegen/模板、测试、压测入口和问题记录。main 基线 `358c89f`，功能分支已提交基线 `967bc69`；用户已授权提交、合并和 push。未提交改动与历史 8 个功能提交一起进入 main，不包含忽略的本地性能产物、临时环境或个人 skill。

实现边界：业务执行与 Entity local 锁归快池，慢池负责前置/后置 I/O；Remote 写预算与慢池并发分离；Sync 周期默认，变化触发可配置，setter 只标脏，在 Guard 锁释放前捕获；DataEngine 保持 WAL 准入、投影与持久确认语义。各批具体问题和验证见 docs/bug、docs/bugfix 以及各 feature 记录。

## 合并前发现与修复

全模块回归发现通用故障矩阵漏列 Remote 包，覆盖检查失败。已修复 [RR-20260925-11](../bugfix/RR-20260925-11.md)，入口加入 Remote 并按包串行，原覆盖用例通过。未放宽测试。

本地忽略目录 `artifacts/perf/remote/` 留有历史 .go 源码快照，Go 的 `./...` 不读取 gitignore，因此直接在工作目录运行会将其当成包，产生混合 package / internal 导入错误。这些证据保持原样，不加入提交。实际提交验证在 `/tmp/roost-merge-check-aq1vj9qs` 干净源码副本完成：从同一 Git 基线克隆，将 git 跟踪文件和待加入文件覆盖，排除忽略产物；源文件 SHA256 清单留在本机验收目录。

## 本次验证

- 全模块 `go test -race -p 4 ./...`：通过（完整命令退出 0）。
- 全模块 `go vet ./...`、`go vet -tags integration ./...`：通过。
- `go run ./cmd/glsvet ./...`：通过。
- 正式生成 DAO/Entity → Nest → Sync 双模式 `scripts/test-sync-modes-generated.sh`（race）：通过。
- 全部本批 Go 文件 gofmt 检查、15 个变更 shell 脚本 bash 语法、Git whitespace 检查：通过。

Go 1.27.0 / darwin arm64，GOWORK=off，GOMAXPROCS=4。完整日志保存在本机 `artifacts/perf/remote/merge-20260925/`，不作为源码提交。未在此次提交收尾重复 30 分钟/24 小时长稳、完整集群故障矩阵或其他平台 CI；integration vet 是构建检查，不等价于真实故障运行。

## 性能边界继续保留

最新[资源预算与会话恢复验收](../feature/REFACTOR-2026-09-25-resource-budgets-and-session-recovery.md)：1024 慢 worker + 128 写预算下，80 TPS 输入的 9600 笔全部成功，实测 79.74 TPS 并全量校验；100 TPS 档存在容量拒绝，不认定达标。Sync 1% 变化 + 1000 会话集中恢复的单轮 50ms 门禁通过；5% 变化 p99 12.933ms，但仍有极少长尾，最大 915.231ms，不能宣称所有样本低于 50ms。预算分摊恢复和后续长尾定位未被此提交隐去。

提交/合并/push 仅发布源码，不等于生产部署或完成剩余性能目标。
