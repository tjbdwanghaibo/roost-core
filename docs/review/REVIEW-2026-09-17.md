# 09-17：匹配新边界与 demo 调用链审查

本轮从 09-16 开始，09-17 恢复完成。用户明确“没有修复”，因此跳过旧 RR 修复核验，不读取旧 bugfix/ledger 进行验收、不重跑 manager 等旧复现。只查进度/问题摘要避免重复登记。新增 [RR-20260917-01/02/03](../bug/REVIEW-2026-09-17.md)，两个 P2、一个仅内存范围的 P3。

## 基线与同步

两次同步三仓 main 均 Already up to date，起始工作树干净，更新前后 SHA 相同。远端为 tjbdwanghaibo/roost-{core,kit,codegen} 的 origin/main。

| 仓库 | 本轮实际源码 SHA |
| --- | --- |
| Core | 9f31436932bd29b625413f1758d0072111eb405b |
| Kit | 48301507d06343f0de788de38f8a670f45adcf49 |
| Codegen | 242b4384d4d96ad616b4197396c55972835cd741 |

未发现额外 AGENTS.md；技能仍按三个活动仓库执行，迁入 Core 的 match 以当前源码位置为准。本轮仅提交 docs，不修改源码、依赖或模板，不以审查报告作为修复。

## 新增范围与结论

1. Core match Queue.Validate/Key → queueStore Enqueue/Candidates/Commit → Redis key：合法复合键碰撞导致混池，新 RR-01。
2. ScoreWindowGrouping → 距离筛选/排序、等待窗口增长/cap：完整 int64 边界不安全，新 RR-02；普通分数对照通过。
3. NewMemoryStore → 输入 Subject/输出 Ticket/Match 的引用所有权：七个切片修改入口可改存储，新 RR-03。泛型 MemoryStore 不负责任意 T 深复制，类型化 match 边界需补齐隔离。
4. queueState 终态保留、clone/Sweep：一年后仍保留 128 场完整历史，作为容量设计观察，不新增确认 bug。
5. Codegen demo JoinQueue/PollMatch/matchmaker 源码已读；实际生成物 formMatches 的候选错误、策略返回、Commit 冲突和失败、提交后通知不可用/奇数余票已验证。没有把未运行的 TCP/RPC PollMatch 端到端说成通过。

固定 demo 使用 FirstCome、duel/default，不直接触发本轮分数极值或键碰撞。生成的 matchmaker 正文与当前模板替换 MODULE/GAME_SERVICE_PKG 后逐字一致（规范化换行）；所以复用前轮生成工程只作为新场景运行载体，不是旧修复验收。

## 验证记录

Go 1.27.0，仓库外 go.work/overlay；没有改用户 Go module。09-16 原临时目录 roost-review-0916f 在恢复时已不存在，执行结果在会话记录中保留；09-17 新增最小复现已重新执行并保存到 [REPRO](../bug/REPRO-2026-09-17.md)。不虚称交付了丢失的完整 34 场景代码。

| 新场景组 | 叶子数 | 实际结果 |
| --- | --- | --- |
| QueueIdentity | 5 | 2 过、3 失败：键相等、候选混池、跨队列 Commit；普通隔离与正常 Commit 对照过 |
| ScoreArithmetic | 9 | 5 过、4 失败：MinInt64 距离、两端极值距离、窗口加法/乘法回绕 |
| MemoryOwnership | 7 | 全部失败：输入/Enqueue/Ticket/Candidates Payload，Commit/Match Members，Match TicketIDs |
| TerminalRetentionObservation | 1 | 观察测试过：Waiting 0、Tickets 256、Matches 128、Requests 256，未设置人为容量断言 |
| demo MatchmakerFailureBoundaries | 9 | 全过：read_error、empty、insufficient、policy_error、not_formed、commit_missing/matched/conflict/unexpected |
| demo CommitWithUnavailablePush | 3 | 全过：2/4/5 票，计数和推送不可用，比赛仍可查询，余票数量正确 |
| **原新矩阵合计** | **34** | **20 过、14 个行为失败，归为三个根因** |

Core 原有 `go test -race -count=1 ./service/match` 通过。此包测试只用于本轮新范围的基准，不据此更新旧 RR 状态。独立 Core 矩阵命令 `go test -race -count=1 -overlay <review.json> ./service/match -run TestReview -v` 失败符合上表；生成 demo `./internal/service/game -run TestReviewMatchmaker -v` 的 12 叶子全部通过。

恢复收尾另运行三个最小复现：QueueCollision、ScoreOverflow、MemoryResultOwnership 均按预期红，确认报告所附代码可运行，不计入上述 34 个新场景。最终分数最小样例补齐合法 Queue、Waiting 与未过期元数据后仍复现。最初 demo 测试误用小写 game 包名，改为生成物实际 Game 后才运行成功；该准备错误不是源码故障。

本轮未运行真实 Redis/Mongo/NATS，未测网络通知成功、Linux 专项、长期容量压测或吞吐 benchmark。内存结果共享只在顺序操作下证明，不能把 race 模式执行解释成已经触发数据竞争告警。

## 图谱证据与限制

使用 codebase-memory Verify；list_projects 确认 D-whb_s 根 D:/whb_s，index_status ready，coverage generation 仍为 2026-09-08T14:39:26Z。模板和新迁入 Core 的服务路径 not_tracked；versionstore 旧文件 metadata_changed，已使用当前源码补证。

match 结构查询有 64 行，两页 60+4 已取完；结果夹带历史 roost-service 节点，排除后只将活动 Kit 旧节点作为迁移前结构线索。旧 Group 双向 trace 没有当前消费者，get_code_snippet unavailable；不据此声称 Group 没有调用，当前 demo 的显式 Group 调用由源码验证。demo 查询零结果与 not_tracked 一致，不能当作实现不存在。

覆盖检查含 Core grouping/types/queue_store/store/redis_store/match_test、versionstore memory 实现，以及 Codegen matchmaker/JoinQueue/PollMatch 模板；core/service/match、codegen/demo scopes 无更多记录，但不证明穷尽。没有中断或重建共享索引。

## 下一步

先由用户实施三个新问题；后续新 review 可继续 RPC 身份/序列化、匹配幂等重放与终态保留协议、demo 断线后的真实查询恢复。旧 RR-20260916-06/07 与此前升级待办保持原状态，本轮用户声明未修复，跳过复核。

机制见 [匹配身份、所有权与配对](IMPLEMENTATION-MATCH-IDENTITY-OWNERSHIP-AND-GROUPING.md)，覆盖状态见 [PROGRESS](PROGRESS.md)。本轮没有把匹配包或 demo 标为 review 完成。
