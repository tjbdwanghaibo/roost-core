# 2026-09-13 第十轮：进度盘点与 LeaveShared 恢复

收尾独立验收：合并作者修复 `885ff4f585508b7f157b89419f83d05bf0a7b5d8`（U-0189，Enter/Leave 共用未知结果恢复），解决文档索引冲突时保留两个 RR 与作者说明。原触发按新契约适配：权威确认切换成功允许返回 nil，发送前失败仍必须报错；核心 live 模式和写准入断言保留。四个模式场景及 Leave 连续三次写准入均 PASS（overlay 1.656s）。RR-12/13 现均已独立验收，旧失败证据保留；未验证真实 Redis/跨进程。最终统计：24 轮、16 篇机制文档、36 RR，索引 36 已修复、0 未修复；不是全仓审完。

先完成[进度统计](PROGRESS-SNAPSHOT-2026-09-13.md)：开始时 23 轮、16 篇机制文档、35 RR（34 标修复、1 未修复）。本轮完成后 24 轮、36 RR（34 标修复、2 未修复），不是全仓覆盖百分比。

三仓起始干净、main，pull --ff-only 均无变化。Core `7be357c1cae69b18bca2c6a87ef918f130c60121`，Kit `ac1a8801604bde4b7ec5ed60104502c542602ed0`，Codegen `cacd627b70991c5d0e38545866610db78e695b53`。核对 bug 主表、bugfix 最新残余补修及 ledger U-0188，RR-12 无新修复；不重复运行它的旧失败断言。

本轮新增 LeaveShared 回复丢失后的后续普通写准入。初版两次尝试失败；扩展到三次，第一次刷新后拒绝，第二、三次因 Shared→LocalOwned 非法转换持续失败。临时 overlay `go test -race -count=1 -overlay <remote.json> ./remoteentity -run '^TestReviewLeaveReplyLossSecondWrite$' -v` FAIL 0.609s；函数名沿用初版，最终源代码实际循环三次。[新增 P2 RR-13](../bug/REVIEW-2026-09-13-10.md)。`go test -race -count=1 ./remoteentity` PASS 2.669s，不覆盖该故障。

使用 roost-review/codebase-memory Verify。list_projects 确认 D-whb_s；搜索 LeaveRemoteSharedMode/beginWrite 两项，无剩余分页；Leave 双向 trace 一页完成，零 caller 不作无使用证明。coverage 的 ownership.go、batch.go 均 metadata_changed，generation 2026-09-08T14:39:26Z，直接阅读错误恢复和 beginWrite 实际路径补证。图谱旧代，不提供全仓穷尽结论。

机制结论：未知结果处理不仅要阻止错误写入，还必须让权威 lease、本地 live 状态与准入状态机重新收敛。Leave 第一次拒绝是安全方向，却不代表下一次会成功；应该验证后续请求而非止于第一条 error。相关学习更新在[未知结果主题](IMPLEMENTATION-REMOTE-REDIS-UNCERTAIN-OUTCOMES.md)。

本轮仅可控存储，没有真实 Redis、Linux 强杀或新 WAL 交错测试。Kit/Codegen 仅同步及统计历史 RR，无新增源码覆盖。继续待办：RR-12/13 修复验收，WAL Sync/Close 受控 writer 屏障，共享模式真实 Redis。Mirror 仍为待实施方案，不改生产代码。

收尾新代码 remoteentity 完整 race 回归 PASS 2.733s。
