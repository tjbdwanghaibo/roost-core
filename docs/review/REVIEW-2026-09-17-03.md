# 2026-09-17 第三轮：续完上轮并审新增 Feature

## 任务与基线

按用户要求先补齐前轮，再做一次新 review，并分析其他 agent 新增功能。使用 roost-review 与 codebase-memory，默认 Verify，受旧索引限制以当前源码和实际运行补证；只改审查文档。

前轮结果已提交并推送 `1af903e`：[Wanted/五项修复验收与 platform 问题](REVIEW-2026-09-17-02.md)。其测试仍明确归属于旧基线，未混入本轮新增 feature 的验证。

2026-09-17 恢复时三仓干净、main 跟踪 origin/main；fetch 后快进：

| 仓库 | 更新前 | 本轮实际源码基线 | 同步增量 |
| --- | --- | --- | --- |
| Core | 6fb36e7 | dc1aab17e654078c5911d0b7ae3bb769ae5b64a2 | 13 提交；U-0225 Saga 与 feature/Wanted 文档等 |
| Kit | 4830150 | 55f3a35b32ea97ccac8459fd1efe2515049f91e3 | 1 提交，Core 依赖升级 |
| Codegen | 242b438 | 2e09c169a354b61edc357cb78665c5d18a1dc199 | 16 提交、108 个变更文件；非已审文件计数 |

Core 之后提交的 1af903e 只有上轮审查文档，源码与 dc1aab1 相同。本轮临时 go.work 引用三仓当前源码，未修改仓库 go.mod、模板或生成物；临时测试放在 `D:/whb_s/review-temp-0917c`，Saga 通过 overlay 注入。不是发布 tag 验证。

## 结论与新覆盖

新增 [五项 RR](../bug/REVIEW-2026-09-17-03.md)：RR-05 DAO 深层通知、RR-06 attribute 契约、RR-07 原生 Saga 完成订阅、RR-08 副本重复/错误发奖、RR-09 battle 宽限期。其中 RR-08 为 P1，RR-05/06/07 为 P2，RR-09 为 P3，全部未修复。

| 范围 | 本轮证据/状态 | 限制与后续入口 |
| --- | --- | --- |
| DAO U-0224 与 Wanted-02 | 当前 golden 实际 BSON 原 3 项通过；新增通知 4 叶子 1 过/3 失败 | 递归 struct/slice、替换解绑、完整事务 patch/rollback 待实施验证 |
| attribute Wanted-03 | 实际 CLI 生成成功，消费包编译失败；模板/参考契约已读 | 缺权威 runtime；未替使用者编造七类型实现 |
| Saga U-0225 | 版本只增长一次的改动已读，现有 saga race 测试通过 | 非独立真实 Mongo 故障/进程重启矩阵，不称全部 Saga 已验收 |
| Saga Wanted-04 | 实际 Assembly 订阅配置及 NewCompletionEffect；3 叶子 2 过/1 失败 | fake JS + mongotest；原生 inbox/outbox/真实 broker 尚未端到端运行 |
| dungeon demo | 实际生成 Controller + Core Session + sender；5 叶子 2 过/3 失败 | Nest RequestMulti 为记录替身；并发和真实奖品存储待查 |
| battle demo | 实际 run + Core Room；2 叶子 1 过/1 失败 | 未连 TCP，下游发送报错；只核验开帧状态 |
| StateSync Wanted-05 | EntityBase.Sync、RoomManager、RegisterSubject 与内部 coordinator 源码已核对 | 修正“没有公开路径”前提；真实生成实体接入仍再观察 |
| cfggen runtime 新门 | 实际生成、真实 configdata 加载/失败保留快照/重载 4 项通过 | 非整包完整审查或大配置性能验证 |
| mail / gift / skill catalog | 关键接入源码与声明边界抽样；生成工程编译通过 | 邮件领取、MongoStep/Nest 原子性已有明示限制；catalog 不等于执行引擎 |
| chat、GM、add-rpc、ranked、next/CRLF、live CI 等 | 增量盘点、相关生成器既有测试和生成工程编译覆盖 | 未逐个做网络/鉴权/重试行为专项，不能据此验收全部新增 feature |

Wanted-02/03/04 转 RR，原始文字归档在 [WANTED](../bug/WANTED.md)；Wanted-05 保留并注明所缺证据。Wanted-01 前轮已经转 ARCH-05，未在本轮实施迁移。

## 实际测试与环境

独立新增行为叶子 **14 个：6 通过、8 失败**，按四个根因归入 RR-05/07/08/09。attribute 是另外一次编译失败，不计 Go 行为叶子。DAO 原 runtime 3 项和 cfggen 4 项另计，不用于夸大新增矩阵。测试完整源码和命令保存在 [REPRO](../bug/REPRO-2026-09-17-03.md)。

| 命令/检查 | 结果 |
| --- | --- |
| Core `go test -race -count=1 ./saga` | 通过 |
| Codegen `go test -count=1 ./internal/dao ./internal/attribute ./internal/roost ./internal/servicerpc` | dao/attribute/servicerpc 通过；roost 唯一失败为系统找不到 sh |
| 将 Git for Windows 的 bin 加入本次进程 PATH 后，定向重跑 `TestDeployScriptsCarryNoKnownShellcheckFindings` | 通过；shellcheck 未安装的部分仍跳过，不冒称 shellcheck 验证成功 |
| 当前 CLI 创建 game-demo，再 `roost generate --root ...` | 271 个文件创建；首次 deps 下载 Core v1.15.6 遇 goproxy.cn INTERNAL_ERROR；随后用临时 go.work 引用当前三仓，generate 通过 |
| 生成 demo 中 `go build ./...`、`go test ./... -run '^$'` | 全部通过；后者仅编译，不执行行为测试 |
| 外部模块 DAO 原三项 BSON runtime | 通过 |
| 当前 CLI 生成 cfggen，并执行原四项真实 runtime | 通过 |
| 独立 DAO/Saga/dungeon/battle 与 attribute | 如上述矩阵；保留实际失败，不作为环境问题隐藏 |

早期临时夹具曾缺实体注册、battle 包名/传输 adapter；修正夹具后取得归档中的最终结果，不将夹具错误列产品 RR。battle 对照传输未连 runtime 的日志保留为限制。Go workspace 的 use 在此 Windows 环境采用本机路径 `D:\whb_s\...`，防止模块路径识别错误。

## 图谱与源码补证

会话及恢复后 list_projects/index_status 确认 `D-whb_s`、根 `D:/whb_s`、ready、26320 nodes / 195520 edges。coverage generation `2026-09-08T14:39:26Z`，与当前 Git 不新鲜，未重启/重建用户索引。

结构发现包括 Saga NewCompletionEffect/SubscribeCompletions/SubscribeDataEngineStep/SubscribeNestStarts（4 个相关结果）、DAO/attribute 生成入口（20 个相关结果）、同步构造入口（7 个相关结果）；相关页无剩余结果。NewCompletionEffect 和 RegisterSubject 做双向追踪。注册实体的大范围查询有额外页，但只用于定位已命中的正向符号，没有据其作零调用或全仓结论。

coverage 对 Saga assembly、迁入 session、新 demo 模板报 not_tracked；对已有 Saga、DAO、attribute、entity/room/entitysync、生成器路径报 metadata_changed。对应代码已直接读取，生成物按本轮源码实际运行。旧图谱 snippet 的偏移/调用启发式不能作当前源码依据。cfg runtime 文件最初误查 runtime_test.go 得 missing，已改核对实际 roundtrip_test.go；未把 missing 当源文件缺陷。

因此证据是“图谱定位 + 过期/未覆盖范围源码补证 + 有界动态检查”，不是当前代 Auditor 全审。真实 NATS/Mongo/Redis、Linux 部署和网络故障在本轮未新增验证。

## 学习、进度与下轮

新增 [跨层契约机制文档](IMPLEMENTATION-GENERATED-FEATURE-CONTRACTS.md)，解释 BSON/dirty/事务三层、Session 完成与奖励身份、两种 Saga 完成来源、attribute 类型归属和 statesync 装配所有权。各建议尚未实施。

累计 **48 份运行记录、29 篇机制文档、74 个不同 RR**，只是记录计数，不是审查完成率；更新 [PROGRESS](PROGRESS.md)。下一轮优先根据用户是否修复决定验收新 RR；未修复时直接推进 Wanted-05 的真实生成实体同步接入，然后 gift/mail 资产流程、GM 鉴权与多节点/重试专项。既有 lockstep/sync 未验证范围继续保留，不宣称整个框架已审完。

## 09-18 续跑收尾

09-17 文档导航与提交步骤被自动审批服务额度耗尽中断；此前行为测试与四份新文档已完成。09-18 恢复后确认 Core HEAD 与 origin/main 同为 1af903e，保留原审查 SHA 和测试日期，补导航及进度后提交推送。此次仅收尾，不重复计算测试或冒称完成新一轮源码审查。
