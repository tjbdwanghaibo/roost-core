# 09-16 第五轮：实施交接落实情况与迁移边界

结论：上次交接的 Recover/Grouping 修复已落实；match、mail、session 领域及 RPC 传输、manager 引擎已迁入 Core，Kit 主要保留对应装配与兼容别名。真实 game-demo 在发布依赖和当前源码两种模式均可构建测试。**不能将这等同于整个 Kit 已无核心实现，或交接全部完成。** 新确认 manager 两项生命周期问题；Kit 默认 durable 迁移仍待补，RPC 生成头部存在可重复性观察项。

## 1. 同步基线

2026-09-16，三仓起始干净，main 分支 pull --ff-only 成功，远端为 tjbdwanghaibo/roost-{core,kit,codegen} 的 origin/main。

| 仓库 | 更新前 SHA | 本轮审查 SHA |
| --- | --- | --- |
| Core | 31b4cc63ec4ddfbb903831d127bb3803a10735b5 | 41d49b982b0ec2c1b6c73bf6bfd8e4f8af15c54b |
| Kit | 527eecdb33e8725737981d02b1ebd8641d976951 | 48301507d06343f0de788de38f8a670f45adcf49 |
| Codegen | 678b2d97f5f56408571efb1f5e0594bb0a580e33 | 242b4384d4d96ad616b4197396c55972835cd741 |

只更新审查文档；没有改源码、生成文件、依赖、作者 bugfix 或 history 账本。三仓未发现另附 AGENTS.md，沿用会话提供的规则。本轮恢复旧问题验收，不适用“用户声明未修复时跳过”。

## 2. 对上次交接的逐项验收

| 上次交接项 | 当前判断 | 证据 / 剩余工作 |
| --- | --- | --- |
| RR-20260916-04 / U-0216 Recover 修改代数 | 原四叶子独立通过；修复有效 | Append、ACK、Import、Rotate、DeleteStream、DeleteObserver、SweepIdle 成功变更后推进 revision；Import 不导入 revision。仍为全局代数，其他流修改会保守拒绝；宿主重试策略未定义 |
| RR-20260916-05 / U-0217 Grouping 无效注入 | 按 B 方案移除入口已落实 | Kit NewMod(reporter)、Core Config 无 Grouping；策略作为调用方工具迁 Core；codegen 生成项目编译通过。旧“拒绝策略注入 Store”复现因合同删除不再适用，不强行恢复旧入口 |
| U-0218 collaborators 未使用 import | 生成消费者独立构建通过 | 发布模式及源码模式 game-demo 均通过；这是作者另外发现的修复，不冒用 RR 编号 |
| U-0211 Windows 半尾截断补修 | 原 20 场景全部通过，前轮失败已消失 | 当前先按路径 Truncate 再打开 APPEND 句柄；作者记录 Linux CI 通过，但本轮没有亲自运行 Linux 或读取 CI 作业日志，不能写成本轮独立 Linux 验收 |
| U-0210 Kit 默认 durable 迁移说明 | **未完成** | bugfix 仍写“默认部署零迁移”，未区分 Kit roost.room；仍需上轮方案中的旧游标/重放演练，本轮不重复原名称测试 |
| ARCH-01 match/mail/session | 三个指定样本的迁移落实 | M-06/07/08/11：领域与接口/传输在 Core；Kit alias/Mod/assembly 保留；Core 依赖边界测试通过。account/chat/global/activity/platform/rank 等仍有 Kit 领域代码，M-11 也明确未迁，不能扩写成全部服务已迁 |
| ARCH-02 manager | 结构迁移落实，行为仍有两项待修 | Kit 包装 Core Engine，原测试通过；独立交错确认 RR-06/07。它们为迁入的既有逻辑缺口，不是“迁移导致” |
| ARCH-03 已有 dataengine/saga 组装 | 延续既有样板 | 本轮没有新增专项验收，不把未改动样板当新增测试成果 |
| ARCH-04 README/生成器 | 主要拆分落实，仍有生成头部观察 | Kit README 已改为装配层；M-10/11 实现 transport/assembly 两半。六份生成正文一致，Core 三份命令头是实施机绝对路径，按 directive -check 会 stale |

作者 M-06/07/08 与 M-10 的阶段记录还会提到“RPC 留 Kit/下一步迁接口”；后续 M-11 已完成该步骤。这些按时间阅读即可，不将早期阶段描述当作当前缺失。bugfix/README 的 ARCH“完成”应限于该批指定范围，而非证明全 Kit 职责或所有生命周期边界正确。

## 3. 新问题与实施建议

[RR-20260916-06/07 完整证据与复现](../bug/REVIEW-2026-09-16-05.md)：

- 最后一个 manager 启动期间停止，Stop 取走空 started，随后 Start 成功追加并退出：starts=1/stops=0/startErr=nil。
- 第一个 manager 仍在 Start 时 Register 返回成功；晚加入者在 managers 可见，却不在 pending，最终 starts=0/stops=0。

优先以显式生命周期状态和在途启动交接一起修复，分别验收。原测试“至多停止一次”不足以证明“恰好清理一次”；有下一 manager 的控制组也不能代表最后一项。此前审查主要验证了搬迁边界及已有包测试，没有构造这两种位置/注册时序，故不能从上轮通过推出引擎无缺陷。

建议后续顺序：RR-06/07 → Kit durable 迁移说明与演练 → RPC 生成头部稳定性 → 其余 Kit 领域逐包迁移。功能修复和机械迁移分开验收，持久化键、wire 字段、错误身份继续保持。

## 4. 实际验证

临时目录 C:/Users/tjbdw/AppData/Local/Temp/roost-review-0916e，Go 1.27.0；源代码测试引用前轮仓库外 go.work，未修改任一仓库 go.mod。所有临时目录只作为执行位置；新问题完整测试已写入问题文档。

| 命令 / 范围 | 结果 |
| --- | --- |
| Core go test -race -count=1 ./syncstream ./skill/skillsync ./manager ./service/... ./servicemetrics | 七包通过 |
| Kit go test -race -count=1 ./manager ./service/match ./service/mail ./service/session | 四包通过 |
| Codegen go test -race -count=1 ./internal/servicerpc ./internal/roost | servicerpc 通过；roost 仅部署脚本测试因 PATH 缺 sh 失败 |
| 加入已安装 Git/bin 到本进程 PATH，重跑 TestDeployScriptsCarryNoKnownShellcheckFindings | 两叶子通过；shellcheck 未安装，因此未执行 shellcheck，不称完整静态检查通过 |
| Core 根 TestCoreDependencyBoundary / TestCoreContractsDoNotLinkDrivers | 通过，迁入代码不引入 Kit 反向依赖 |
| 前轮独立 RecoverReplacementBoundary / JournalFailureAdmission | 4+2 叶子全部通过 |
| 原 history_review.json 的 TestReview | 20 叶子全部通过，包括此前 Windows partial_tail |
| 新 manager overlay 的 TestReviewManager | 3 叶子 1 通过、2 失败，对应 RR-06/07；race 未报数据竞争 |
| servicerpc -check 与六份副本重新生成比较 | Core mail 的直接 -check 失败；六份排除换行/命令头后完全一致；Kit 三份仅规范化换行即一致 |
| 当前 roost CLI 生成 game-demo；GOWORK=off，Core v1.15.5 / Kit v1.14.6 / Codegen v1.15.8 固定依赖 | generate、go build ./...、go test -count=1 ./... 全通过 |
| 相同 demo 通过临时 workspace 接本轮三仓源码 | go build、go test、roost generate --check 全通过 |

生成命令：

```text
roost project new reviewdemo -module example.com/reviewdemo -out <temp>/reviewdemo
  -mods configdata,mongo,nats,dataengine,nest -services game -template game-demo
  -roost-core-version v1.15.5 -roost-kit-version v1.14.6 -codegen-version v1.15.8
```

发布依赖验证使用当前源码构建的 roost CLI，不声称下载的 v1.15.8 CLI 二进制也做了独立验收。demo 大部分包没有测试；build/test 证明集成可编译及已有测试通过，不等于 Redis/Mongo/NATS 业务端到端、生产部署或游戏压测通过。

临时 source-head go.work 初次手写路径不正确，Go 拒绝加载；修正为原生绝对路径后上述源码模式通过。没有改用户仓库路径/依赖来掩盖问题。

## 5. 图谱与覆盖限制

使用 codebase-memory Verify。list_projects 确认 D-whb_s 根 D:/whb_s，index_status ready；coverage generation 仍为 2026-09-08T14:39:26Z。manager/新服务路径 not_tracked，旧路径 metadata_changed。正确通配范围搜索 manager Start、servicerpc Run 有界结果分别 6/1，无更多页；初次使用不适配的 file_pattern 查询零结果已纠正，不作为缺失证据。Kit Start 双向旧边仍指向已迁走函数，源码片段 unavailable；均以当前源文件/diff/临时测试补证，未声称图谱已追上本轮迁移。

主要证据路径和 core/manager、core/service、kit/service/match 范围已 coverage；无记录缺口并不代表完备。没有重建/中断共享索引。生产 Linux、真实 durable 升级、RPC 运行时全协议互通、其余 Kit 领域完整清单和 Codegen 新 demo 全业务尚未收敛。

学习机制见 [Core/Kit 服务与 manager 边界](IMPLEMENTATION-CORE-KIT-SERVICE-AND-MANAGER.md)，进度见 [PROGRESS](PROGRESS.md)。本轮没有把包标成穷尽 review 完成。
