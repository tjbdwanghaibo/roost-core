# 2026-09-14 第五轮：lockstep 消费者与可靠追帧

按既定顺序继续 lockstep，尚未转入 sync 新源码审查。只写文档，不修源码。

## 基线与修复记录

三仓开始干净、main 执行 pull --ff-only；2026-09-14 14:13（Asia/Shanghai）核对记录：

| 仓库 | 更新前 | 更新后 |
| --- | --- | --- |
| Core | 07f4421a452eedacb412cab794eb738c578a2183 | a1245fda685496b077932854aa20d5d7eb97dc45 |
| Kit | 7030c5fd15701df9c5cc6c8c4bf62a8c91a5ba2a | 不变 |
| Codegen | cacd627b70991c5d0e38545866610db78e695b53 | 不变 |

已读 bugfix/RR-20260914-08、ledger U-0197、sequencer 环实现与 replay_window_promises_test。上一轮原上限测试无需改断言即可通过：map 改成固定槽 slice 后，原 len 边界仍适用；没有把替换数据结构本身当验收。

## 实际覆盖与测试

| 执行（均 -race -count=1） | 结果 |
| --- | --- |
| lockstep overlay：原身份上限、原期限边界、原 UDP 回环、新 TCP 重投、新取消恢复 | 五场景 PASS 1.639s |
| robot overlay：输入发送/hash 上报/模拟三处一次性失败后继续，正常对照 | 三失败、一通过，0.956s |
| go test -race -count=1 ./lockstep ./robot | lockstep PASS 1.617s；robot PASS 2.468s |

临时 go.work 引用当前三仓，Go 1.27.0，overlay 只追加临时测试。新增 [P2 RR-20260914-09](../bug/REVIEW-2026-09-14-05.md)：接收批次游标已推进、应用中断后未保存尾帧也未隔离实例。模拟/发送原错误可见，但后续成功处理跳过权威帧。复现源码见[附录](../bug/REPRO-2026-09-14-05.md)。

TCP 实验使用 127.0.0.1 临时 listener/client/server socket，长度前缀仅为测试 transport framing；真实写出后在适配器返回一次错误，接收两个重叠页并验证总应用 7 帧/重复 6 帧。socket 设置读写 deadline 并 defer 关闭，读取 goroutine 有限两页。它验证真实字节传输及 Room/Assembler 重投协作，不是 KCP/QUIC 故障验收。取消实验用尊重 context 的替身，不模拟阻塞内核 I/O。UDP 是已收到后丢弃第 5、6 包的原对照。

## 图谱与证据限制

Verify，list_projects 确认 D-whb_s。robot/lockstep.go 搜索 19 节点无剩余分页；HandleFrames 双向 depth=1 trace 未截断，找到 IngestFrames/apply/requestCatchup。入向零结果不是没有动态调用的证明；实际通过导出 HandleBroadcast/HandleFrames 验证。HandleBroadcast snippet 与当前文件一致。

coverage generation 仍 2026-09-08T14:39:26Z；sequencer/room/robot 源文件 metadata_changed、新回归文件 not_tracked，已读当前源码补证。没有索引重建或全图完备性声明。

## 进度与后续

累计 29 份运行记录、18 篇机制文档、45 RR（44 标修复、1 未修复）。RR-08 原触发通过，新消费者 RR-09 未修复；接收成功、应用成功、出站成功须分开跟踪。lockstep 仍未完整收敛，真实可靠链路增加了局部证据；尚未做生产 Linux、真实游戏模拟或回调部分副作用恢复验证。

下一轮先验收 RR-09，再按[覆盖清单](LOCKSTEP-AND-SYNC-COVERAGE.md)收束剩余可验证边界；随后 statesync → entitysync → syncstream/syncbus，不把本轮继续 lockstep 误报为 sync 已开始。
