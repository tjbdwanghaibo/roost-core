# 2026-09-19：新修复验收、K1 所有权与 platform 支付续审

本轮执行 roost-review。起始同步为 Core `a964af7`、Kit `45c035a`、Codegen `992cc23`；提交前 fetch 发现并行提交，
最终审查基线更新为 Core `1947faa`、Kit `5116f2a`、Codegen `7297f92`。
只提交审查文档，不改实现、依赖或正式测试；前三项问题在 `e12a4d3` 重跑，batch15 三项在最终 Codegen 基线上独立复现。

## 修复验收

| 项目 | 证据 | 结论与限制 |
| --- | --- | --- |
| RR-20260918-05 邮件寿命 | Core mail race；新 demo handler/dungeon 全包测试 | 原固定 31 天改为信封事实 `ExpiresAtUnix`；原根因通过，无真实持久后端/跨进程并发 |
| RR-20260918-06 断线清理 | 新 demo `internal/service/game` 通过；当前生成模板逐行核对 | **部分修复**：scene 已接线，chat presence 仍只靠 push 失败懒清理 |
| RR-20260918-07 syncTopic | `internal/entity` 通过；当前 demo 用显式字面量 | 歧义裸标识符已拒绝；一般 Go 表达式仍不支持，属已记录边界 |
| RR-20260918-08 AOI 预算 | Core spatial race 通过 | 最坏 block 预算、可配置值和溢出守卫通过；无指标和真实容量基准 |
| RR-20260918-09 / U-0244 运行期 ID | 新 demo runtimeid/spawner 测试与全包通过 | sid 改从 `Registry.Config()` 读取；仍依赖部署 sid 唯一，运行期 ID 不跨重启 |
| RR-20260918-10 DAO 顶层容器 | 官方 runtime script 通过；新增 11 叶子 | map/slice 原八叶子及多写、拒绝、panic、nil 均通过；发现 pointer/alias 两个新根因 |
| ARCH-06 同步字段词汇表 | Core dataengine、Codegen DAO runtime 通过 | 元数据能力可用；demo 尚无真实自定义协议消费者，nested 未覆盖 |
| U-0245 新 DAO nested 接线 | 独立 fresh DAO 测试、官方 runtime script、最新 demo 全包通过 | 构造即 `Init` 的原触发通过；跨进程 mirror 构造路径仍未覆盖 |

RR-06 的问题索引原来写“已修复”，本轮按作者 bugfix 的明确边界和当前源码更正为“部分修复”。其 scene 原触发已经通过，
未把 chat 未完成部分重复登记新 RR。

## 新问题与 K1 结果

[问题与实施方向](../bug/REVIEW-2026-09-19.md)记录六个新 P1，[独立复现](../bug/REPRO-2026-09-19.md)给出命令和行为输出：

1. RR-20260919-01：顶层 nested pointer 替换 / rollback 不解绑游离对象；
2. RR-20260919-02：nested 指针同时占两个 map key 时只更新最后绑定的 key；
3. RR-20260919-03：会话关闭订阅者 panic 穿出调度 goroutine。
4. RR-20260919-04：订单创建与待办 ZSet 分两次写，失败/崩溃后后台永远枚举不到订单；
5. RR-20260919-05：一条不可读订单让 `PendingOrders` 丢弃整页，健康订单饥饿；
6. RR-20260919-06：未领取付费 grant 固定 30 天后被拒绝并删除，平台却已标 delivered。

K1 本轮预定义 11 个所有权叶子，8 过、3 失败；失败归并为前两个根因。重复写三形状、提交拒绝、transaction panic、nil
这些 RR-10 记录为未覆盖的边界已经补齐并通过。K1 仍未整体完成：顶层 pointer/alias 要先修；实体加载/释放、串行 mailbox、
关闭结算与持久化完成交接仍未按当前 SHA 收敛。

## 并行新增 feature 审查

Codegen `e12a4d3` 新增装备栏、DAO `schema=N` 和 Player v1→v2 迁移。当前有界结论：

- 装备的背包扣除、替换件返还、装备字段和 Gear 属性刷新在同一个 strict/undo Nest handler 中；客户端不决定槽位；
- `schema` 省略保持 1，0/负数/文本拒绝；迁移变换与 `RestorePersisted` 实际接线均在新生成 demo 运行通过；
- 新 demo 第一次把 nested struct → pointer map 放进真实消费链，确实发现并修复 U-0245；
- 未发现除本轮 RR-01/02 之外的新确定功能根因。旧 `weapon_id` 不会由在线 patch unset、迁移 registry 是进程级、
  TCP Mod 在 Service.Init 前开始监听等边界仍需部署/启动专项，不能用本轮单进程测试宣称生产迁移完成。

随后到达的 Kit `5116f2a` / Codegen `7297f92` 新增 platform `RegistryBound`、托管服务可选 collaborator 接线和 game-demo 第十五批支付链。
有界审查覆盖订单创建/认领、pending ZSet、grant 写入/抽干、Nest claim 账本、登录补偿、配置与 bootstrap：

- 正向链设计成立：platform 进程把 grant 持久化后才报告 delivered；game 进程在 Nest 事务提交后才删 grant，重读靠 order id 去重；
- Kit 的 `RegistryBound` 在 Provide 阶段绑定，生成 bootstrap 确实调用 `WithPendingOrders(Pending())`，两个 secret 和 game 侧 Redis 依赖已生成；
- Kit platform race、Codegen 定向门、新生成 demo 全包通过，作者的分页、退避、终态退休和 grant 重放用例都执行；
- 正向测试没有覆盖三条失败契约，新增 RR-04/05/06。它们分别破坏“崩溃后可枚举”“坏单不阻塞健康单”“已付款最终履约”。

因此 U-0234 的“部署参考索引”只能判为**接线已完成、恢复契约未收敛**；ARCH-05 中 platform 的持久恢复入口不再是空白，
但还不能作为通用支付实现交付。机制与实施顺序已补到[生成 Feature 跨层契约](IMPLEMENTATION-GENERATED-FEATURE-CONTRACTS.md)。

## Wanted-11 / ARCH-07

图谱先定位 `SetRuntimeConfig` 的 app/configdata 写入者和 Nest/fctx 消费者；由于索引 generation 仍是
`2026-09-08T14:39:26Z`，相关路径均为 metadata_changed、not_tracked 或 missing，随后逐一回读当前源码。

结论是设计债而不是新的功能 RR：U-0244 已修复，当前生产代码中没有其他把该槽位断言为 viper 的消费者；槽位实际服务于
configdata 活动快照和请求捕获。W-11 转 ARCH-07，按[运行期配置所有权](IMPLEMENTATION-RUNTIME-CONFIG-OWNERSHIP.md)收敛。
本轮没有新增待判 Wanted；batch15 的三项都有确定触发和业务后果，直接登记 RR-20260919-04/05/06。

## 图谱覆盖与源码补证

本轮为 Verify 层级。`search_graph` 能找到旧代 `fctx.SetRuntimeConfig`，找不到新加入的 `deliverClosed` 与最新 DAO template 符号；
`check_index_coverage` 对 Core fctx/configdata/nest、Codegen DAO/player TCP/scene/chat，以及 Kit platform、Codegen platform/purchase/game 模板执行聚合检查，未报告 parse gap，
但所有关键新增路径都不是 current generation（batch15 为 `not_tracked`，Kit 修改为 `metadata_changed`）。已回读全部引用路径并运行新生成消费代码；
“无记录 gap”不被当作完整覆盖证明，也没有重建用户索引。

## 验证汇总

- 正常基线：Core 相关五包 race、Codegen entity/DAO、DAO runtime 脚本、`e12a4d3` 阶段的新生成 demo（施加独立红测前）均通过；最终 batch15 基线另见下一项。
- 新增行为：DAO 11 叶子 8 过 3 失败；生命周期 panic 1 叶子失败；支付失败面 2 条独立复现通过当前缺陷行为，另有 1 条由现成 Nest 测试与 drain 删除源码确认，归为六个根因。
- batch15 基线：Kit `go test -race ./service/platform`、Codegen 相关 `internal/roost`、source workfile 新生成 game-demo `go test ./...` 全过。
- `internal/roost` 全包只因宿主缺 `sh`/shellcheck 失败；五项 `TestDemo*` 通过。未执行真实 Mongo/Redis/NATS、网络断开、
  多进程 sid 冲突、AOI 压测、旧数据离线清理或完整发布部署；不据此宣称 K1 或三仓完成。

## 下一入口

先验收 RR-20260919-01..06 与 RR-06 chat 剩余部分；若用户说明没有修复，直接继续 K1 的实体加载/释放、串行调度和关闭结算。
K1 收敛后按既定顺序进入 K2 数据提交/恢复，再进入 K3 owner/fence/epoch、迁移、mirror 和防复活，不回退到低优先域。
