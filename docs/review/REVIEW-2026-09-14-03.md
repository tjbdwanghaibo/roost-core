# 2026-09-14 第三轮：lockstep 与 Activity 修复验收

按用户安排优先 lockstep，只更新文档。测试约 11:10（Asia/Shanghai）。

## 同步基线

三仓初始干净、main 执行 pull --ff-only：

| 仓库 | 更新前 | 更新后 |
| --- | --- | --- |
| Core | daa8fbe97f5f1ef9e616a373d9ac9fb2cd2684f8 | a1455fb63903e5f1e4d1e7741ed13d52533bebf9 |
| Kit | ac1a8801604bde4b7ec5ed60104502c542602ed0 | 7030c5fd15701df9c5cc6c8c4bf62a8c91a5ba2a |
| Codegen | cacd627b70991c5d0e38545866610db78e695b53 | 不变 |

Core 增量为 Activity 修复文档，Kit 为 U-0191/0192 opening 与 delivering 生命周期。已核对 bug/bugfix、两个修复说明、ledger U-0191/0192，以及 lockstep U-0137、09-09 历史 review。lockstep 无新代码仍按用户指定深入；Codegen 仅同步。

## 结果与测试

新增 RR-20260914-04/05（P2，上行重传重复入帧、batch=1 不收敛），RR-06/07（P3，-1 身份碰撞、超解码条数配置）。[问题](../bug/REVIEW-2026-09-14-03.md) · [复现](../bug/REPRO-2026-09-14-03.md) · [机制](IMPLEMENTATION-LOCKSTEP-INPUT-AND-CATCHUP.md)。

测试均 -race -count=1，临时 go.work 指向当前三仓，Go 1.27.0。overlay 只增加测试，不修改生产源码。

| 实际执行 | 结果 |
| --- | --- |
| lockstep overlay -run TestReview -v，加入生命周期测试之前 | FAIL 0.580s；七叶子、五失败两通过 |
| lockstep overlay -run TestReviewRoomLifecycleControls -v | PASS 1.644s；三个生命周期子例 |
| Core go test -race -count=1 ./lockstep | PASS 1.585s |
| Activity 上轮原 overlay -run TestReview -v | PASS 1.828s；五叶子 |
| Kit go test -race -count=1 ./service/global/activity | PASS 1.908s |

lockstep 共十叶子，综合对照中的多个断言不重复计数。追帧不收敛同时有 +1-1=0 的源码推导，不单凭二十次循环推断无限行为。记录型传输不是实际网络。

Activity 原测试原样复跑，开活动交错、grace/notify/heal 三条派发与正常对照全通过；已读 confirmWindow/retireFromWindow/DeliveringActivities/RetireDelivered 与 sweepGroup。RR-02/03 原触发验收通过；作者明示 reopen、实际投递、OpeningGrace 超期容量边界没有一起完成，本轮没有展开这些残余的新复现。

## 图谱、范围与限制

Verify：list_projects 确认 D-whb_s，coverage generation=2026-09-08T14:39:26Z。lockstep Method 搜索 38 条无更多分页；pumpCatchup 双向 depth=1 trace 无截断；Advance snippet 与源码一致。Latest 等同名跨包误连不作为证据。六个 lockstep 源文件、测试 helper、Activity 三个变化文件均 metadata_changed，读取当前源码补证；scope 无记录缺口不是完整保证，未重建索引。

重点覆盖输入/切帧、History、Assembler、wire、Room 绑定/广播/追帧/关闭，desync 是源码阅读与裁剪对照。未验证真实 UDP/KCP/QUIC、客户端模拟、快照恢复、压测、uint32 耗尽，不声称全包无其他问题。

## 进度与续跑

累计 27 份运行记录、18 篇机制文档、43 RR（39 标修复、4 未修复），不是代码覆盖率。见[进度](PROGRESS.md)；[原六项未收敛工作](OPEN-QUESTIONS.md)保留，Mirror 仍待实施。

测试完成后的首次文档写入被自动审批服务以额度限制拒绝，当时未生成本轮文档、未提交推送。用户要求继续后恢复文档收尾；没有把拒绝算作代码测试失败。

下一轮先验收 lockstep 四项，继续乱序身份、可靠发送过载与快照接入；Activity 明示残余及 Codegen 消费者轮转继续保留。
