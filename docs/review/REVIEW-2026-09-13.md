# 2026-09-13 扩展审查：Remote 接收链与加载取消

本轮延续尚未实施的 Mirror 方案，扩大到已经存在的 Remote 发布/接收、兴趣协议、缓存合并层和 NATS/JetStream wrapper。新增四项确认问题（五个失败场景），两个正向场景通过；没有修改源码。

## 同步与修复记录

三仓 main 起始工作树干净，`pull --ff-only` 均 Already up to date；更新前后相同：

| 仓库 / GitHub origin | SHA |
| --- | --- |
| core / tjbdwanghaibo/roost-core | 617738b1cb61f8a4f35f6d5e8365d2f525a08b0b |
| kit / tjbdwanghaibo/roost-kit | 3855c71f5aaaf5ca4b7091ae17bf8cb0e6943b79 |
| codegen / tjbdwanghaibo/roost-codegen | cacd627b70991c5d0e38545866610db78e695b53 |

已核对 bug/bugfix 索引、上轮记录及 ledger U-0009/U-0045。没有新修复记录；遵从用户说明继续往后审，不重复未改代码上的四项旧失败。读取 U-0155 说明后确认快照加载的等待计数是独立实现，新编号而非重开旧问题。Mirror 仍未实现，上一轮方案保留。

## 图谱与实际源码

使用 codebase-memory Verify。list_projects/index_status 确认 D-whb_s 根 D:/whb_s、ready、26320 nodes/195520 edges；coverage generation `2026-09-08T14:39:26Z`，关键源码均 metadata_changed。ApplyReplica 同名符号图谱只返回 interest 接收器，不能推断 snapshot 接收器不存在；已直接读取 syncer.go。

Remote 目标搜索 4 条、SyncBus 搜索 15 条、loadMonotonic/LoadAuthoritative 2 条均无分页。ReadRemoteSnapshot 双向 trace 返回 11 callees/0 callers，无分页；若干低置信边错误指向其他包的 Get/Now/WithTimeout，未用这些边作行为证据，实际接口回调与标准库调用以当前源码为准。

统一 coverage 包含 remoteentity syncer/interest/transaction_manager/manager/config/snapshot_l2/interest_test、entity remote_snapshot、mirror envelope、syncbus sync；补 room nats_syncbus/jetstream_syncbus、kit room_mod 与 U-0155 文档，相关 scope 无记录缺口/分页。旧图中 U-0155 文档 not_tracked，已读原文；ledger 第 311 行 partial 已读。图谱 ready 不等于当前代码已索引；本轮依赖当前源码和实测补证。

## 实验与结论

临时目录 `C:/Users/tjbdw/AppData/Local/Temp/roost-review-0913-007ecd198ae640cf8b3508b76a5db41a`，文件 remote_test.go/entity_test.go 与 overlay.json/entity.json。不写入源测试，不改依赖，不运行真实外部服务。[完整源码与重建方式](../bug/REPRO-2026-09-13.md)。

| 临时 race 场景 | 结果 | 结论 |
| --- | --- | --- |
| delete v2 在 full v3 后交付 | FAIL，v3 消失 | RR-20260913-01 |
| delete v2 后交付 full v1 | FAIL，v1 复活 | 同一 RR |
| renew/release/renew，旧 release 后到 | FAIL，新兴趣消失 | RR-20260913-02 |
| 外层 scope1、payload Update scope2 | FAIL，scope2 被写入且无错误 | RR-20260913-03 |
| upsert 版本乱序、正常删除/兴趣释放 | PASS | 不是测试总线完全无法工作 |
| 缺 base 的 delta v3 → 权威 full v3 | PASS，loader 一次 | 正常恢复路径成立 |
| 慢 loader + 跟随者取消 + 新跟随者 | FAIL，空闲仍 Overloaded | RR-20260913-04 |

```text
go test -race -count=1 -overlay <temp>/overlay.json ./remoteentity -run '^TestReview' -v
FAIL: 四条失败、两条通过，0.599s
go test -race -count=1 -overlay <temp>/entity.json ./entity -run '^TestReview' -v
FAIL: 等待名额断言失败，0.588s
go test -race -count=1 ./remoteentity ./entity ./mirror ./cache ./entitysync
PASS: 2.838s / 2.083s / 1.382s / 1.414s / 1.397s
go test -race -count=1 ./room ./syncbus
PASS: 1.699s / 1.304s
```

失败是行为断言，不是构建或环境失败，没有 race detector 数据竞争报告。已读取实际总线回调确认没有业务版本过滤；测试重排的是真实 API 产生的消息，但未启动真实 broker，不宣称所有部署必然重排。没有对 kit/codegen 新增包测试；kit 本轮额外确认 room Mod 使用 core 总线，codegen 无新增覆盖。

## 覆盖与续跑

[问题详情](../bug/REVIEW-2026-09-13.md)、[实现学习](IMPLEMENTATION-REMOTE-REPLICA-ORDERING-AND-RECOVERY.md)、[跨轮进度](PROGRESS.md)。本轮七个新场景覆盖发布→解码→应用与加载取消，不再止于上轮裸缓存 API。

尚未验证真实 L2 删除与并发 Set、broker 强杀/重投、gap 回填失败后的再同步、owner 迁移、同 ID 重建、Mirror 生成消费者写能力、性能基准。下一轮优先复核新增修复；无修复时从这些未查边界继续，避免反复报告同一根因。
