# 2026-09-09 第四轮：扩大覆盖与修复验收

本轮新增两个 P2，未改源码；三项旧修复独立验收通过。跨轮范围见[PROGRESS](PROGRESS.md)，新问题见[报告](../bug/REVIEW-2026-09-09-04.md)，实验见[复现附录](../bug/REPRO-2026-09-09-04.md)。

## 同步基线

三仓 origin/main 均 fetch；Core 从 b714085f56b266dba046fea550332bd31a39dcc6 快进至下表，其余两仓无新增。审查前工作树干净。

| 仓库 | 实际源码 SHA |
| --- | --- |
| Core | 1f7bb5425a8b82c774bac9bbf40048f44ea3de99 |
| Kit | c4e7cef1029fb6326fa88b84c622f059bc62c0c4 |
| Codegen | aa072edb35a0b97d234e0155142e36295c5f21d1 |

前次执行被工具审批服务额度中断，当时尚未完成的图谱与实验不计为验证。本轮恢复后重新核对远端，完成下面的实际检查。正式组合仍为 Core v1.15.2 / Kit v1.14.3 / Codegen v1.15.4；U-0158～U-0160 的验收是源码 HEAD，不是新 tag 发布证明。读取了 bug、bugfix、历史账本；U-0161 文档收尾见用户记录，未重跑完整快速开始部署。

## 图谱与补证

使用 codebase-memory MCP，Verify 层级。项目 D-whb_s，根 D:/whb_s，ready，26320 节点、195520 边；coverage generation 为 2026-09-08T14:39:26Z，早于最新提交，不能代表当前源码完整索引。

search_graph 定位 Guard、Cast、Mail Claim、Match Enqueue/ownership；trace_path 检查 validateOwnership 双向关系；get_code_snippet 阅读 CastMulti。Mail 初始广泛搜索返回 35/38，随后按 service.go/Claim 缩小；不作 Mail 全符号穷尽结论。旧图谱中的远程 helper 名与当前重命名不一致，启发式边也不能代替源码调用证据。

一次 coverage 批量检查 20 条证据路径：Core entity_guard、cast/cast_test、msg、group_transition、nest_dispatch、Assembly/Runtime/其回归测试；Kit Session/其回归测试、Mail service/mailbox、Match queue_store/store/match_test/match_rpc；Codegen Entity main/gen/多实体回归测试。没有记录解析缺口不等于完整；metadata_changed、not_tracked 及旧位置均用当前源码、diff、原文件补证。账本 311 行部分解析范围也直接读取。未重建或中断用户索引。

## 修复复核与新增覆盖

| 项目 | 本轮证据 | 结论 |
| --- | --- | --- |
| RR-20260909-02 Session ABA | 原第二轮临时 overlay，包 race | 原触发已通过 |
| RR-20260909-03 Assembly Shutdown | 原第三轮 overlay，包 race；检查 Runtime 分阶段关闭 | 原触发已通过 |
| RR-20260909-04 多 Entity 默认输出 | 最新 CLI 生成两个 remote Entity，最新 Core 消费者执行测试 | 默认输出修复通过 |
| Guard/Cast 增量 | 锁顺序、远程预声明、失败回滚源码及包 race | 未确认新缺陷；真实分布式故障未验 |
| Mail 领取链 | Reserve/Commit/Cancel、稳定 token/租约源码及包 race | 已读部分机制；未验证资产侧去重 |
| Match 队列 | Enqueue/Ticket/Cancel/Commit/Candidates/Sweep 相关源码及测试 | 新 RR-20260909-05 |
| Entity 显式输出 | 双实体真实 CLI + 最终消费者编译 | 新 RR-20260909-06 |

实现学习：[锁与生命周期](IMPLEMENTATION-STATE-AND-LIFECYCLE.md)、[领取/重放/生成](IMPLEMENTATION-SERVICE-REPLAY-AND-CODEGEN.md)。

## 实际测试

Windows，Go 1.27.0，CGO_ENABLED=1。临时目录 roost-review4-be759089d3294d8197b6954ae3945c34 位于用户 TEMP；临时 go.work 仅连接最新三仓和消费者，overlay 不写入仓库源文件。

- Core：`go test -race -overlay <overlay.json> ./dataengine/engine ./entity ./nest -count=1 -timeout=180s`：通过。
- Kit：`go test -race -overlay <overlay.json> ./service/session ./service/mail ./service/match -count=1 -timeout=180s`：通过。
- Codegen，GOWORK=off：`go test -race ./internal/entity ./internal/nest -count=1 -timeout=180s`：通过。
- 默认模式：`go run ./cmd/entity -dir <consumer>`，消费者 `go test ./... -count=1 -timeout=60s`：通过。
- Match 独立 overlay：`go test -race -overlay <match-overlay.json> ./service/match -run '^TestReviewEnqueueReplayKeepsSubjectOwnership$' -count=1 -timeout=30s`：行为断言失败；异归属重放返回 player:1，候选数 1，Ticket 对照拒绝。
- 显式输出：生成器退出 0；消费者 `go test -run='^$' ./... -timeout=60s` 退出 1，undefined: RegisterEntity。

“8 个相关包 race 通过”不表示全仓审计通过；后两项是预期揭示缺陷的失败，不是环境失败。没有运行真实 Redis/Mongo/网络分区、Linux 集成、生产负载或完整正式 tag 消费者。

## 工作流与续跑

个人 roost-review skill 已加入每轮维护 PROGRESS、实现主题文档、扩大风险轮转和中断续跑记录的要求，技能结构校验通过。个人 skill 在仓库外；本轮 Git 提交只包含 Core 审查文档与导航。

下轮从上表 SHA 增量，先读新增 bugfix，再沿 Match 请求过期、Mail 发奖回执、Remote Entity 预分派推进，并轮转 Room/Skill。不要把包测试通过标为整个模块完成。
