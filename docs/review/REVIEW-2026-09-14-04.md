# 2026-09-14 第四轮：继续 lockstep，盘点 sync

只审查与文档，用户要求 lockstep 优先，然后未完成的 sync。结论：lockstep 六个生产文件已有源码阅读，尚非全部风险验证完成；sync 历史只有部分覆盖。本轮新增一个 P2，原四项修复验收通过。

## 同步与修复

三仓开始干净、main 执行 pull --ff-only；2026-09-14 13:56（Asia/Shanghai）核对记录。

| 仓库 | 更新前 | 更新后 |
| --- | --- | --- |
| Core | aafe6a4baba29511a04e0368cab1aa516bd16e12 | 30a6b5bbdc9dc3df93ad16a1249d37e1b7c51995 |
| Kit | 7030c5fd15701df9c5cc6c8c4bf62a8c91a5ba2a | 不变 |
| Codegen | cacd627b70991c5d0e38545866610db78e695b53 | 不变 |

Core 增量为 U-0193..0196，已读四份 bugfix、ledger、sequencer/room diff，并原样执行上一轮完整 overlay。RR-04..07 原触发及正常对照十叶子全部通过。超 64 帧重放仍允许再次入帧是作者明示边界，本轮确认但不冒充其已修复。新资源边界另立 [RR-20260914-08](../bug/REVIEW-2026-09-14-04.md)。

## 实际验证

| 验证 | 结果 |
| --- | --- |
| 原 lockstep overlay，-race -count=1 -run TestReview -v | PASS 1.602s，十叶子 |
| 新 overlay：身份表上限、超期限重放、本机 UDP 冗余恢复 | FAIL 0.606s；上限失败、两个边界/对照通过 |
| go test -race -count=1 ./lockstep ./robot | PASS 1.687s / 2.491s |
| 现有两个微基准，100x，-benchmem，非 race | PASS 0.592s |

临时 go.work 指向当前三仓，Go 1.27.0，overlay 只增测试；未改源码/依赖。新 UDP 实验只监听 127.0.0.1 自动端口，两个 socket defer 关闭；主动丢弃已收到的两个数据报，不能称为真实跨主机丢包。客户端端点验证帧与输入顺序，没有运行战斗模拟。

微基准机器 Windows/amd64、Ryzen 7 5800H：RedundantEncodePush 8826 ns/op、2530 B/op、21 allocs/op；RoomTickTenPlayers 8426 ns/op、2911 B/op、26 allocs/op。两者都含十玩家 SubmitInput/Advance，前者并非纯编码；Room 使用 nullTransport，没有真实发送。仅 100 次迭代的本机快照，不推算生产吞吐、不比较两项优劣。可复跑命令：`go test ./lockstep -run '^$' -bench 'BenchmarkRoomTickTenPlayers|BenchmarkRedundantEncodePush' -benchmem -benchtime=100x -count=1`。

## 图谱与范围

Verify，list_projects 确认 D-whb_s；coverage generation 2026-09-08T14:39:26Z 仍旧代。lockstep Function 搜索 40 条无剩余分页；Lockstep 名称查询 15 条；Ingest 双向 depth=1 trace 定位 robot.HandleBroadcast，未截断。候选七个业务文件 coverage metadata_changed，实际源码补证；未将清洁 scope 当完整保证。

statesync 查询 15 条用于入口盘点，coverage 三路径 metadata_changed；没有读取这些实现作新正确性结论。sync 状态依据现有 review/ledger 和当前目录，不声称完整依赖图或简单迁移关系。详见[覆盖与优先级](LOCKSTEP-AND-SYNC-COVERAGE.md)。

## 进度与继续入口

累计 28 份运行记录、18 篇机制文档、44 RR（43 标修复、1 未修复），不是覆盖率。更新[进度](PROGRESS.md)及 lockstep 实现文档；下一轮先 RR-08 准入边界和消费者错误恢复，再按 sync 清单推进。当前不宣布 lockstep、sync 已全部完成，也不修改用户实现来消除待办。
