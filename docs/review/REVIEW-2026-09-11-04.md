# 2026-09-11 第四轮：completion 慢确认与回调异常

新增 P2 RR-20260911-06；上轮 Mail P3 仍复现。慢 ticket、慢回调下的重复 Shutdown 验证通过。仅更新文档。

## 同步与范围

三仓干净 main，pull --ff-only 均 Already up to date，更新前后 SHA 相同：Core `dd1f270c1f044022df37a887f6510e8e26b90dab`，Kit `3855c71f5aaaf5ca4b7091ae17bf8cb0e6943b79`，Codegen `cacd627b70991c5d0e38545866610db78e695b53`。读取 bug/bugfix 索引、第三轮进度和相关 history；RR-20260911-05 无新修复记录。Core 相对 4e8f5ec 的 nest/worker 无源码差异。

本轮从上轮明确未覆盖的慢 completion ticket/回调继续，不重复已验收的 Remote Close 与 Nest 延迟重排修复；Kit 只复测未关闭问题，Codegen 无新增源码审查。

## 图谱证据

采用 Verify。list_projects/index_status 确认 D-whb_s 对应 D:/whb_s，ready；generation 仍为 `2026-09-08T14:39:26Z`。search_graph 的 pipelined 范围 49 项、Rollback/AfterCommit 10 项、worker 执行 4 项、TransactionReleased 5 项、SafeFunc 1 项、nestwal hold/release/replay 4 项均取完。双向 trace prepareCompletion 定位 invokeWithTransaction，精确 snippet 与当前源码交叉核验。低置信度 Now 等误连不采用。

coverage 检查 nest 的 pipelined_completion、pipelined_async_test、pipelined_commit_test、rollback、transaction、client、nest；worker 的 worker/pool；goroutine/safe_func；nestwal/committer；dataengine/engine/projector；kit/dataengine/mod；Mail service 与 versionstore/memory_store。均 metadata_changed，按当前文件补证。history/ledger 的 partial 311 行亦已读取。不声称图谱已刷新或全仓完整覆盖。

## 执行结果

Windows amd64，Go 1.27.0，CGO_ENABLED=1，临时 go.work 与 overlay，不改模块和源码。

| 实际执行 | 结果 |
| --- | --- |
| Mail 原 TestReviewClaimHistoryRefusalIsAtomic，race | FAIL，1.007s；原 RR-20260911-05 仍复现 |
| 新 AfterCommit panic 用例，race | FAIL；已持久化、Shutdown 成功，但无回复且释放次数为 0 |
| 新重复 Shutdown：ticket 未完成 | PASS；连续两次 20ms 等待均超时，确认后最终 Shutdown/回复完成，释放恰好一次 |
| 新重复 Shutdown：AfterCommit 等待屏障 | PASS；同样等待回调真正结束，无提前停止完成 |
| 上述新 Nest overlay 合并执行 | 包 FAIL，0.761s；失败仅来自 panic 收尾用例 |
| 原 nest / worker 全包 race | PASS，2.774s / 1.472s |

完整命令与新测试见[复现附录](../bug/REPRO-2026-09-11-04.md)。可控 ticket 代表成功持久化契约，未执行真实 fsync/数据库实验；没有以故障注入测试推断真实磁盘数据已损坏。

## 结论与下一轮

Shutdown 调用方的 context 只限制等待，后台仍负责完成已接收工作；本轮两组慢完成测试支持该设计。异常回调则暴露了另一条边界：worker 捕获 panic 能保住 worker，却没有履行具体事务的回复和释放责任，见[RR-20260911-06](../bug/REVIEW-2026-09-11-04.md)。

下一轮先复测 RR-20260911-05/06 的修复；继续审查饱和回退的异常一致性及真实 WAL held/release 链路。未覆盖 Linux 压力、真实 projector 阻塞恢复、跨实体回调重入，不对整个 Nest 作完成审计结论。[进度](PROGRESS.md)与[实现学习](IMPLEMENTATION-COMPLETION-FAILURE-AND-SHUTDOWN.md)同步更新。
