# 2026-09-18 Roost Review：Wanted-05 接入与当前进度

用户明确没有修复，本轮不读取旧 bugfix/ledger 做验收、不重跑旧独立复现。沿上一轮未审的生成实体→房间→传输路径继续。使用 roost-review、codebase-memory（Verify，旧索引以源码补证）。

## 基线与实际工作

09-18 三仓起始干净、main；fetch origin + merge --ff-only origin/main 均 Already up to date。Core `6e09124aa3ea4036c43592be5b6893bb61cdfaf0`，Kit `55f3a35b32ea97ccac8459fd1efe2515049f91e3`，Codegen `2e09c169a354b61edc357cb78665c5d18a1dc199`，更新前后相同。

外部消费模块 `D:/whb_s/review-temp-0918`，Go 1.27.0，临时 go.work 引用三仓当前源码。实际运行当前 entity CLI 分别生成 sync=false、sync=true、sync=true+subjectPacker；两个同步开启包编译失败，对照通过。没有为绕过错误修改模板或生成文件；接入行为矩阵使用 sync=false 生成类，通过公开 EnableSync 手动装配。

## 本轮结果

新增 [两个 P2](../bug/REVIEW-2026-09-18.md)：RR-20260918-01 生成配置与 Core 不兼容，RR-20260918-02 房间封装未接入 durable 水位，均未修复。Wanted-05 已分流为明确缺陷和设计建议，不再停留于“再观察”。[完整源码/命令](../bug/REPRO-2026-09-18.md)。

| 本轮检查 | 结果 | 证据范围 |
| --- | --- | --- |
| 生成消费者三组编译 | 1 过、2 失败 | 无 sync 对照通过；带/不带 subjectPacker 的 sync=true 都失败 |
| 手动组合八场景 | 8 通过 | 快照、增量、批量、profile、退订、退休、关闭、准入失败重试 |
| 房间 pipelined gate 三入口 | 3 行为失败 | 快照、单实体、批量均在建模水位前准入 transport |
| 直接 coordinator 三入口对照 | 3 通过 | 实际安装水位后先阻止、推进后投递一次 |
| `go test -race -count=1 -v .`（外部模块） | 合计 14 叶子，11 过、3 失败，无 race 报告 | 不是全仓 race；失败按产品行为保留 |
| Codegen `go test -count=1 ./internal/entity` | 通过 | 现有文本门未覆盖真实消费契约 |

早期夹具使用了错误返回类型 FrameDelta（实际为 DeltaFrame）和原始 42 作为完整实体 ID；修正为 BuildEntityID 后得到上述最终结果，这些夹具错误不计问题/场景。仅对新范围测试，未以包绿替代旧问题验收。

图谱：index_status 确认 D-whb_s、D:/whb_s、ready、26320 nodes/195520 edges。search_graph 对房间入口返回 5 项、房间 durable/watermark 名称返回 2 项（均为 JetStream 命名，非所查接线），相关分页均完整。RegisterSubject 双向 trace 在旧图谱没有生产调用方，不据此判定现在无使用方。NewRoomManager snippet 行尾被旧偏移截断，改读当前文件。

check_index_coverage generation `2026-09-08T14:39:26Z`，entity factory/base/subject_sync、entitysync subscription、room broadcaster/manager/transport/test、Codegen gen/parse/main 均 metadata_changed；room 有界 scope 无记录缺口也不保证完整。关键路径直接读取当前源码，room 两配置和安装点作有界全文检查；实际生成/消费补证。未重建索引、未启动第二个 MCP。

## Review 进度：审过什么，还差什么

累计 **49 份运行记录、29 篇实现机制文档、76 个不同 RR**。本轮扩充已有机制文档，没有为了计数新建重复主题。这些是文档/问题计数，**不是覆盖率，也不是当前未修复数**。本轮明确跳过旧验收，无法据此给出“当前所有未修复 bug 数”。

| 领域 | 已有 review 进度（包括历史证据） | 尚未完成的主要范围 |
| --- | --- | --- |
| Lockstep | sequencer/room/history/assembler/wire/desync 核心源码已读，多轮输入、追帧、消费者和回环场景；上一轮新增 demo 开局问题 | ReplayHorizon 外身份、真实业务模拟/快照恢复、生产网络拥塞、系统变异输入；不标全完成 |
| StateSync / EntitySync / Room | 投影/ACK/重组、订阅/水位、生命周期/准入多轮；本轮新增实际生成实体手动消费链及两装配缺口 | 自动生成路线、水位实际装配、真实客户端应用/WAL、卸载并发、跨节点恢复 |
| SyncStream / SyncBus / Journal | History、恢复、分片、故障语义及单节点真实 NATS 多轮有界检查 | 半包跨重启、业务 Apply 恢复、集群 failover、真实断电与多实例目录所有权 |
| Nest / Remote / Mirror / DataEngine | 事务/guard/finalizer/快照/生命周期已有多轮记录，Mirror 有方案与接入边界记录 | 真实分布式故障、完整 Mirror 业务消费、端到端持久恢复；历史证据不冒充当前重新验收 |
| Core/Kit 服务与职责 | session/mail/match、manager、平台重试、六 RPC 服务及 directory 分层已做有界审查/交接 | ARCH-05 实施、持久化后端/资产幂等和容量验证、其他服务未覆盖路径 |
| Codegen 与新 Feature | 实际 demo 构建、DAO/cfggen runtime、attribute/Saga/dungeon/battle 本轮前已查；本轮补 sync=true 消费编译 | GM 鉴权、聊天/榜单等完整行为专项、模板组合、发布版本矩阵与 Linux 部署 |

以上汇总来自 [PROGRESS](PROGRESS.md) 和 [Lockstep/Sync 覆盖表](LOCKSTEP-AND-SYNC-COVERAGE.md)，不是本轮重审全框架。各模块历史 SHA 与详细限制仍以对应运行记录为准。没有预先定义完整场景分母，因此不报任意百分比。

下一轮：若用户仍未修复，继续新内容——生成项目 GM 鉴权/参数边界、mail/gift 资产交接或真实 StateSync 消费与关闭并发；若明确已修复，再按用户范围独立验收。本轮只提交文档，旧 RR 状态保持，两个新 RR 交用户实施。
