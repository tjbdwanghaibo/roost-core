# 2026-09-10 第二轮：远程预分派与 Mail 去重保留

本轮新增 P2 RR-20260910-02：已领取邮件被容量淘汰后，重投可生成新发奖 token。[问题](../bug/REVIEW-2026-09-10-02.md)与[测试附录](../bug/REPRO-2026-09-10-02.md)。

## 基线与旧记录

三仓开始干净，main 的 pull --ff-only 均无新增；更新前后：

| 仓库 | SHA |
| --- | --- |
| Core | 76664a51be77bdeb79a62cf344d5cee0ecc15daa |
| Kit | c4e7cef1029fb6326fa88b84c622f059bc62c0c4 |
| Codegen | aa072edb35a0b97d234e0155142e36295c5f21d1 |

读取 bug/bugfix 索引与历史账本，RR-20260909-05、06 及 RR-20260910-01 无新修复，保留状态；未重复执行这三项旧复现。Codegen 本轮仅核对版本及状态。

## 图谱与源码补证

codebase-memory Verify：D-whb_s ready，26320 节点、195520 边，generation 2026-09-08T14:39:26Z。Nest remote 搜索 63 项，60+3 翻页完成；RemoteEntity Prepare/Finalize/Abort 搜索 15 项；Mail Claim 搜索 26 项，Deliver/evict 相关搜索 36 项。使用 evict snippet、Deliver 双向 trace；同名低置信度边不作为实际调用结论。

coverage 统一检查 11 个路径：nest remote_access/msg/nest_dispatch/remote_transaction_test，remoteentity batch/batch_test，mail service/mailbox/mail/mail_test，历史账本。源码均 metadata_changed，已读取本轮关键实现与测试补证；账本 partial 311 行直接补读。没有重建索引，没有全包无缺陷或穷尽调用关系结论。

## 新增范围和实际验证

远程侧读准备、部分失败清理、租约/所有权校验、Finalize、Commit、Abort、Indeterminate、Close 及快照预加载。独立测试验证第二个实体缺失时，批次失败后第一个实体可以立即再准备。相关既有异步 gate 和 Nest WAL 不确定结果测试也随包执行。

Mail 侧把 Reserve/Commit/Cancel 与 Deliver/evict 串联，核对 token 生命周期。正常存活 Entry 的稳定 token 不能覆盖 Entry 已被删除重建的情况；现有重投测试和终态淘汰测试分别通过，不等于二者组合正确。本轮未搭建资产服务，新增证据限定为同一附件获得不同 token。

Windows / Go 1.27.0 / CGO_ENABLED=1；临时 go.work 连接三仓原生 Windows 路径，不改 go.mod。实际运行：

- Core：`go test -race -overlay <remote-overlay.json> ./remoteentity ./nest -count=1 -timeout=180s`：两包通过，包含新增部分准备失败回收测试。
- Kit：`go test -race ./service/mail -count=1 -timeout=120s`：通过。
- Kit：`go test -race -overlay <overlay.json> ./service/mail -run '^TestReviewEvictionPreservesClaimIdentity$' -count=1 -timeout=120s`：行为断言失败，token-1 → token-2；不是 race 报警或环境失败。

三个相关包通过不等于整个分布式链路验收。临时测试使用 MemoryStore、fake envelope/remote backend/locks；真实 Redis/Mongo、网络分区、进程恢复和正式 tag 消费者未执行。

## 文档与续跑

新增[远程预分派实现学习](IMPLEMENTATION-REMOTE-PREPARE-AND-FINALIZE.md)，给[Mail/重放学习](IMPLEMENTATION-SERVICE-REPLAY-AND-CODEGEN.md)补充淘汰边界，更新[进度表](PROGRESS.md)。本轮只提交文档，不改源码或个人 skill（现有流程已覆盖）。

下轮先查修复记录，优先验收 Mail 去重保留修复，再继续 finalizer 停止/资源交接、Mail 部分 fanout 与发送账本失败恢复；Skill 取消失败及 checkpoint 仍为未完成后续范围。
