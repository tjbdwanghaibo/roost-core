# 2026-09-12 第二轮：RemotePolicy Mirror 专项

## 基线与范围

按用户指定范围核对 Mirror，优先已有框架工具并给实现方案，只改文档。三仓 main 工作树起始干净，`git pull --ff-only` 均 Already up to date，更新前后 SHA 相同：

| 仓库 / origin | SHA |
| --- | --- |
| core / tjbdwanghaibo/roost-core | ef44d770fc231896187b6e6b104d10ffa965bb01 |
| kit / tjbdwanghaibo/roost-kit | 3855c71f5aaaf5ca4b7091ae17bf8cb0e6943b79 |
| codegen / tjbdwanghaibo/roost-codegen | cacd627b70991c5d0e38545866610db78e695b53 |

已读 bug/bugfix 索引、最新进度、M-04、相关历史账本与上一轮记录。无新修复；四项旧未关闭问题维持原状，本轮不重复无关复现。历史 U-0009/U-0045 对应通用 mirror 消息身份/校验，不等于 RemotePolicy Mirror 已实现。

## 结果

Mirror 仍只实现声明；复用基础已存在。建议提取只读快照客户端，复用 `RemoteSnapshotCache`、`mirror.Replicator` 与 Remote 快照/兴趣链，补只读入口、启动水位、墓碑、断线恢复及轻量装配。详见[现有实现及分阶段方案](IMPLEMENTATION-REMOTE-POLICY-MIRROR.md)。不新增重复 RR，不把既知未实现功能伪装成新回归；[观察记录](../bug/REVIEW-2026-09-12-02.md)记录集成风险。

## 图谱证据与限制

使用 codebase-memory Verify，`list_projects/index_status` 确认 `D-whb_s` 根目录 D:/whb_s、ready、26320 nodes/195520 edges。coverage generation 为 `2026-09-08T14:39:26Z`，所有关键证据文件为 metadata_changed，不能作为最新源码证明。

`search_graph` Mirror/RemotePolicy 71 条、mirror Replicator 8 条、entitysync coordinator 12 条、cache ReadThrough 8 条、remoteentity Snapshot/BindSync 30 条均无下一页。一次错误的正则式 file_pattern 返回零，已换正确 glob 重查，没有用于不存在结论。读取 BindSync snippet 并双向 trace（3 callees、1 caller，无分页）；旧图仍指向 kit bindSyncer，已用当前 core assembly/assemble 源码纠正迁移后的调用关系。接口和回调不能仅靠 heuristic trace 证明。

对 policy、factory、remote_snapshot、mirror/envelope、cache/mirror/store、entitysync/subscription、remoteentity/syncer/assembly/transaction_manager、Nest 与 codegen gen/parse 检查 coverage；对 entity/mirror/remoteentity/codegen internal/entity 加 scope，均无记录缺口、无分页，但 freshness 陈旧。对当前三仓 Go/模板中的 Mirror/policy 引用作源码补证，并直接阅读关键函数。补查发现 assemble.go 和 M-04 在旧图中 not_tracked，已读当前文件；ledger 第 311 行 partial，已读原文；kit scope 仅报告 .git/.idea 排除目录，不作为业务源码证据。不存在结论限定本轮三仓基线与这些接入路径，不宣称全仓功能穷尽。

## 实际验证

```text
core: go test -race -count=1 ./mirror ./cache ./entity ./entitysync ./remoteentity
PASS: 1.526s / 1.383s / 1.973s / 1.506s / 2.676s
codegen: go test -count=1 ./internal/entity
PASS: 0.863s
core: go test -race -count=1 -overlay <temp>/overlay.json ./entity -run '^TestReviewMirror' -v
PASS: 1.546s
```

临时目录 `C:/Users/tjbdw/AppData/Local/Temp/roost-review-mirror-71eb63cf851d4b31a2d0c52f0886b7f4`，overlay 将 `mirror_test.go` 映射到 core/entity/review_mirror_external_test.go。未修改生产源码/测试/依赖，未使用临时 go.work。下面保留核心复现逻辑供目录清理后重建；包为 entity，导入 context/testing/time，初始化无外部服务：

```go
const kind EntityKind = 229
MustRegisterEntityKindDefs(EntityKindDef{Kind: kind, Category: EntityCategoryWorld, RemotePolicy: RemotePolicyMirror})
id, err := BuildEntityID(923001, kind) // 测试中检查 err
_ = err
c := NewRemoteSnapshotCache(RemoteSnapshotCacheConfig{
    Shards: 1, MaxEntries: 8, MaxBytes: 4096, TTL: time.Minute,
}, nil, nil)
key := RemoteSnapshotKey{EntityID: id, Kind: kind, Scope: 1}
update := RemoteSnapshotRecord{Key: key, StateVersion: 2, MarkerEpoch: 1,
    RouteEpoch: 1, Schema: 1, Codec: 1, Full: true, Data: []byte("new")}
ctx := context.Background()
// 每步检查 ApplyUpdate/Delete/Get 错误；实际 overlay 全部检查。
c.ApplyUpdate(ctx, update)
update.StateVersion, update.Data = 1, []byte("old")
c.ApplyUpdate(ctx, update) // Get 仍为 v2
c.Delete(ctx, key)
c.ApplyUpdate(ctx, update) // Get 现在为 v1：Delete 清除水位
```

实际 overlay 还断言 remote-capable/non-managed 分类、旧版本保护及 BytesCopy 修改隔离。它是设计前的原语行为探针，不是未实现 Mirror 的端到端验收。没有真实 broker、owner RPC、Linux 强杀/故障、订阅恢复、生成 Mirror 消费者编译或业务吞吐测试。kit 本轮仅同步和源码引用筛查，不新增行为覆盖。

## 续跑

[进度表](PROGRESS.md)已记录新范围。下一步若继续 review，优先实测 SnapshotReplicaStore 的旧 delete/重投与 L2 交错、bootstrap 水位，以及 Mirror 生成消费者的写能力暴露。若实施方案，先做 P0/P1 的单种快照纵向样例；本轮方案不代表已授权自动改代码。
