# 2026-09-13 第七轮：新增修复与 L2 删除边界

使用 roost-review/codebase-memory，文档审查，不修改生产代码。

## 同步与证据

三仓起始干净、main，pull --ff-only 完成。Core `655fe821e142ddc430d6be858a0006f9e364746f` → `76c6bac305c9c0408aa2966ad0ae05779dd50047`；Kit `3855c71f5aaaf5ca4b7091ae17bf8cb0e6943b79` → `ac1a8801604bde4b7ec5ed60104502c542602ed0`；Codegen `cacd627b70991c5d0e38545866610db78e695b53` 无变化。核对 bug/bugfix 索引、RR-01/02/08/09 修复说明及 ledger U-0180–0188 相关条目。

Verify 图谱项目 D-whb_s，generation 2026-09-08T14:39:26Z。search_graph 定位 entity Publish/Delete/LoadAuthoritative（4 条）与 interest renew/release（3 条），无剩余分页。Publish 双向 trace 两页已取完；低置信度同名边不作为证明。get_code_snippet 的旧行号实际截到了 Get，明确废弃该片段的符号定位结论。

coverage 检查 remote_snapshot.go、interest.go、transaction_manager.go、read_through.go、atomic_local.go 及两个新增 promises 测试：旧文件 metadata_changed，新测试 not_tracked。相关实际源码直接阅读补证；entity scope 无记录缺口不代表完整。本轮不声称整个依赖图或全仓完成审查。

## 实际测试

| 命令/范围 | 结果 |
| --- | --- |
| entity 历史 expiry.json，TestReview* | PASS 1.636s，Monotonic/Linearizable 过期与同版本新有效期三场景 |
| remoteentity 历史 overlay.json，TestReview* | PASS 1.666s，原两个删除、旧兴趣释放、身份拒绝及两个正向场景 |
| entity 历史 L2 entity.json | 测试替身重复 close panic；新增预检查使 Get 多调用，非产品缺陷证据 |
| 新 overlay，三个删除/L2 竞争场景 | FAIL 0.608s，三个明确行为断言失败，见问题文档 |
| 新 overlay，TestReviewRefillConflictAdapted | PASS 1.633s，保持同版本 B 不被在途 A 回填覆盖的断言 |
| go test -race -count=1 ./entity ./cache ./remoteentity ./nest ./nestwal | entity 2.270s、cache 1.452s、nest 2.819s、nestwal 4.672s PASS；remoteentity FAIL，重启代际断言 |
| remoteentity 单独重跑重启代际测试 | FAIL 0.659s，与包运行同类结果 |
| Kit go test -race -count=1 ./service/mail | PASS 2.523s，按 Kit go.mod 的 core v1.15.2 依赖运行，非本地 core 联调 |

所有 Go 命令使用 -race -count=1。临时 overlay 未修改源码或依赖。新测试保存为[可复跑源码](../bug/REPRO-2026-09-13-07.md)。本轮无真实 Redis、NATS、Linux、子进程崩溃重演；原 Transfer/WAL/完成链不能只凭现有包测试判定独立验收通过。

## 结论与进度

RR-08 三条历史残余通过，RR-06 适配复现通过；RR-01/05 部分修复，仍有三条确定性 L2 失败路径。RR-02 原乱序通过，但新跨重启代际承诺实际测试失败，保留平台边界。详细[问题与观察](../bug/REVIEW-2026-09-13-07.md)。

机制学习：墓碑只在 Publish 判定仍会漏掉 ReadThrough 回填；L2.Get 预检查与 L2.Set 不是同一原子边界。修复应落在实际写入准入与错误分类处，而非不断追加外层查询。性能上预检查为冷发布新增一次 L2 RTT，却不能取代实际 CAS 错误处理；未进行吞吐测量。

下轮优先独立验收 Transfer 真实 Redis 回复丢失、WAL 原屏障/截止场景、完成回调子进程和 Mail 原拒绝原子性；同时复核本轮残余修复。新增 Mirror 实施仍待用户推进。跨轮[进度](PROGRESS.md)与[Mirror 方案](PLAN-REMOTE-POLICY-MIRROR.md)同步更新。
