# 2026-09-13 第九轮：共享模式失败恢复

三仓 main、起始干净，pull --ff-only 均无变化。更新前后 Core `99be1ebc6191cf3e64acb22e39a8c439d7f3eea6`，Kit `ac1a8801604bde4b7ec5ed60104502c542602ed0`，Codegen `cacd627b70991c5d0e38545866610db78e695b53`。核对 bug 索引、RR-09 bugfix 的明确未做范围及上轮验收；没有新增修复，不重复运行全部已验收故障。

## 本轮新增证据

沿 EnterRemoteSharedMode/LeaveRemoteSharedMode 检查执行后错误恢复。两个方法与 Transfer 不同：仍对任何存储错误恢复旧状态。临时 overlay 四场景中 Enter 执行后丢回复失败：权威已共享，本地独占，写准入成功；Enter 发送前失败、Leave 执行后丢回复观察、Leave 发送前失败三项通过。命令 `go test -race -count=1 -overlay <remote.json> ./remoteentity -run '^TestReviewModeReplyLoss$' -v`，包 FAIL 0.607s，新增 P2 [RR-20260913-12](../bug/REVIEW-2026-09-13-09.md)。

使用可控 marker store，复用仓库 indeterminateTransferSetup。保持真实 Manager、版本切换和 PrepareRemoteWriteBatch；未连接真实 Redis，未执行资产写入或双节点竞争。Leave 分支通过只表示观测到拒绝准入，不代表整个修复契约成立。测试源码见[附录](../bug/REPRO-2026-09-13-09.md)。

`go test -race -count=1 ./remoteentity ./nestwal` 为相关包基线回归，两包均通过：remoteentity 2.693s，nestwal 4.155s；包通过不能否定独立复现失败。

## 图谱与源码补证

roost-review/codebase-memory Verify。list_projects 确认 D-whb_s；coverage generation 为 2026-09-08T14:39:26Z，ownership.go 和 wal.go 均 metadata_changed，直接阅读实际源码。首次 EnterShared/LeaveShared 简写搜索零结果，不作为缺失结论；准确 RemoteSharedMode 搜索找到两个方法，无剩余分页。EnterRemoteSharedMode 双向 trace 一页完成，接口边为启发式，零调用者不作为无使用证明。

WAL 继续读取 Close、writerLoop、awaitWriteBarrier：closed 在 Close 发起时置位，实际 drain 完成由 doneCh 表示；Sync 快速分支与并发 Close 仍需确定性调度验证。本轮未完成该交错复现，不登记功能 bug，也不将它标为已验证。新增机制学习见[未知结果恢复](IMPLEMENTATION-REMOTE-REDIS-UNCERTAIN-OUTCOMES.md)。

## 下轮

优先 RR-12 修复验收；继续 LeaveShared 重查/重试能否恢复以及真实 Redis EnterShared 丢回复；为 WAL Sync/Close 设置受控 writer 屏障，区分开始关闭与完成排空。Kit/Codegen 本轮只同步，没有新增代码覆盖。跨轮进度与 Mirror 依赖状态一并维护，无生产代码修改。
