# 2026-09-18 第二轮：八项修复验收与 K1 通知/事务边界

本轮使用 roost-review + codebase-memory，用户明确已修改一批 bug，因此恢复修复验收。只写文档，不修改三仓实现、依赖或正式测试。新增 [RR-20260918-03/04](../bug/REVIEW-2026-09-18-02.md)，附[独立复现代码](../bug/REPRO-2026-09-18-02.md)。

## 基线与同步

09-18 三仓均 main、工作树干净，fetch 后 ff-only 同步。Core 这批修复已在前一任务更新到本机；本轮不能因它没有新增提交而跳过用户要求的验收。

| 仓 | 更新前 | 本轮实际审查 HEAD | 结果 |
| --- | --- | --- | --- |
| Core | abb7b8066c649b5be4ff3e67e64143c84bbec439 | 同左 | 无新增；含此前 U-0226～U-0235 文档/修复链 |
| Kit | 55f3a35b32ea97ccac8459fd1efe2515049f91e3 | 399f175f78ec44868f06351367f7a45e602223d4 | 快进，8 文件 |
| Codegen | 2e09c169a354b61edc357cb78665c5d18a1dc199 | ffff2a11011d2657569a0f658d166b4ba07f48ee | 快进，60 文件 |

版本说明记 Core v1.15.7 / Kit v1.14.8 / Codegen v1.15.10。行为结论针对上列 source HEAD。新建 demo 的 project new 成功解析发布依赖；后续测试用外部 go.work 指向三仓源码，不混称发布 tag 全链路验收。

## 修复验收

读取 bug/bugfix 索引、8 个 RR 修复记录及 ledger U-0226～U-0235。WANTED 仅有已有分流记录，本轮无新未分流候选；旧候选没有作为新 bug 重报。未重做没有新修复声明的全量历史 RR，匹配历史键迁移等旧限制不因此关闭。

| 原 RR / 修复 | 本轮证据 | 结论与边界 |
| --- | --- | --- |
| RR-20260917-04 / U-0234 platform | 复用独立三场景；向新增 PendingOrders 注入候选，真实 owner 30 秒 tick 恢复订单；手动/供应商重试对照通过 | 原触发已验证；部署仍需提供候选索引，未配置不会凭空持久发现订单 |
| RR-20260917-05 / U-0232 nested | 原 raw-map/setter/BSON/parent 四场景均通过 | 原触发已验证；新替换/回滚归属问题另列 RR-0918-03 |
| RR-20260917-06 / U-0230 attribute | 新生成 demo 全包编译及实际属性公式、dirty、快照复制测试通过，Core attribute race 通过 | 消费运行时契约可用；未验证所有 profile/并发自定义访问 |
| RR-20260917-07 / U-0231 Saga | 原 Assembly 三订阅场景全过，Core saga race 通过 | 原生完成订阅原触发已验证；fake JetStream，无真实 broker/outbox crash E2E |
| RR-20260917-08 / U-0226 dungeon | 当前 Controller+真实 Nest+生成 Player/World，单次/即时重放/failed 后 success/expired retry/wrong owner 五场景全过 | 原触发已验证；新账本清理后重放 RR-0918-04，不能称奖励幂等整体收敛 |
| RR-20260917-09 / U-0228 battle | 原 no_input/input_control 两场景复用，2s grace 后切帧；新 demo 自带测试通过 | 原触发已验证；原 fixture 无 TCP 发送端，tick 发送告警不计开帧失败，无生产传输验收 |
| RR-20260918-01 / U-0229 entity sync | 三份原 fixture 重新生成：sync=false、sync=true、sync=true+packer 均编译通过 | 原 API 漂移已验证修复；不以编译等同自动同步全部行为 |
| RR-20260918-02 / U-0233 room gate | RoomManager 注入 watermark；snapshot/delta/batch 在 9 阻挡 LSN10，升到10各发送一次；原八组合场景+三个 coordinator 对照均过 | 14 叶子全过，race 无报告；无真实 WAL fsync、客户端或重启 |
| U-0227 补修 | 当前 game-demo 包含 dungeon.ClaimResult 返回类型，完整 generate + go test ./... 通过 | handler 不再因仅返回值导入而编译失败 |
| U-0235 补修 | roost 两项生成 header/template ownership 测试通过；生成 runtime 有生成标记 | 对应补修通过，不外推全部 project upgrade/doctor |

“原触发已验证”保留有界含义，和作者的发布说明分开。8 项修复的原问题有效，并不意味着其所在模块无其他问题。

## 实际执行与结果

临时目录 `D:/whb_s/review-temp-0918b` 位于三仓外；Go 1.27，外部 go.work 仅 use 三仓与临时消费工程，未 go work sync。原生 Go Windows 路径；无正式源码/测试残留。

| 独立场景组 | 叶子数 | 通过 / 行为失败 |
| --- | --- | --- |
| platform 原恢复三路径 | 3 | 3 / 0 |
| nested 原通知 | 4 | 4 / 0 |
| Saga 原订阅 | 3 | 3 / 0 |
| Room 消费、屏障与 coordinator 对照 | 14 | 14 / 0 |
| dungeon 真实 Nest 原五路径 + 清理后重放 | 6 | 5 / 1 |
| battle 原 grace 两路径 | 2 | 2 / 0 |
| K1 map/slice/pointer 归属 | 12 | 6 / 6 |
| K1 DAO 恢复后 commit 对照 | 2 | 1 / 1 |
| **合计（去重，不含重跑）** | **46** | **38 / 8，归为两个根因** |

另外：`go test -race ./saga ./room ./attribute`（Core）、`go test ./service/platform/...`（Kit）、`go test ./internal/dao ./internal/nest ./internal/entity ./internal/attribute`（Codegen）通过。Codegen `go test ./internal/roost -run 'TestAttributeRuntimeFileIsMarkedGenerated|TestGeneratedAttributeProjectPassesTheTemplateCheck' -count=1` 通过。新生成 game-demo 在加入独立红测前 `go test ./...` 通过。DAO 当前 golden+runtime fixture 测试通过。三种 sync 消费编译单列，不计行为叶子。

新红测为期望正确语义的断言，失败不是基础设施失败。测试搭建期间曾有 import 别名、marker 空行以及 DAO 初始装载方式错误，修正 fixture 后才计最终结果；Room 原 cleanup 在 gate 尚未推进时 Close 返回 deferred，现先推进水位，并增加发送恢复断言后14场景通过，不报产品 bug。

常用复跑命令：

```powershell
# 在临时消费模块
$env:GOWORK='D:\whb_s\review-temp-0918b\go.work'
go test ./dao ./nested -run TestReview -v -count=1
go test -race ./composition -v -count=1
# 在临时 demo
go test ./game/controllers/player -run TestReview -v -count=1
go test ./internal/service/game -run TestReviewBattleStartGrace -v -count=1
# 在 Kit/Core 分别使用临时 platform-overlay.json / saga-overlay.json
go test -overlay D:\whb_s\review-temp-0918b\platform-overlay.json ./service/platform -run TestReviewPlatformRecovery -v -count=1
go test -overlay D:\whb_s\review-temp-0918b\saga-overlay.json ./saga -run TestReviewAssemblyEffectSubscriptions -v -count=1
```

## 图谱证据与源码降级

Verify 层，D-whb_s 根 D:/whb_s，index_status ready，但 generation 仍 `2026-09-08T14:39:26Z`。不能把 ready 当最新索引。先 search_graph 定位 Rollback/RecordUndo/RunIsolatedTransaction；最初71结果的宽查询仅发现线索，随后限定 nest/rollback.go 精确查询5结果、无更多页。RunIsolatedTransaction 双向1层 trace 返回4 callees/1 caller、无分页；其中名称启发式边不作调用完整性证据。读取 RecordUndo snippet 后，以当前 rollback.go 实际源码核对。

对修复涉及的16个候选路径及本轮新增 transaction.go、template_nested.go、fixture definitions、hook.go、Session service/redis_store、Kit session_mod、demo claim/dungeon/profile 等批量 check_index_coverage。结果为 metadata_changed 或 not_tracked；相应范围均用当前源码/生成物补证。session 范围图谱无匹配且 scopes 无记录，实际目录有源码，因此没有据图谱零结果声称功能不存在。没有启动或终止共享索引，没有 Auditor/全图完备性结论。

## K1/K2/K3 进度及下一入口

历史计数更新为 **50 份运行记录 / 29 篇机制文档 / 78 个不同 RR**；是累计文档与问题编号数，不是未修复数或覆盖率。09-18 早先131/699=18.74%是历史路径引用基线，代码已更新，未据此宣称当前全仓覆盖增长。

| 域 | 本轮新增读证 | 已执行 / 明确计划范围 | 停点与未完成 |
| --- | --- | --- | --- |
| K1 | nestedTemplate 的 wire/绑定/三类 setter；Nest undo 去重、逆序撤销、RollbackUndo capture tracker；DirtyHook；真实生成 DAO → commit | 通知归属12 + 提交2 =14场景，7过7失败；本有界场景14/14已执行，绝非整域完成 | 下一入口：顶层 DAO Set/Del/Init 与 nested 引用的交接；多次写入、panic/拒绝提交/关闭结算；entity 锁/串行/加载释放历史证据仍待逐项回填 |
| K2 | dirty 丢失导致后续 commit 为空；Room 外发水位修复作为共享边界 | commit 两场景与 K1共用不重复计；gate三场景共享既有修复证据 | 未新增真实 WAL/Mongo、断电、恢复/schema 场景；不能称 K2完成 |
| K3 | 本轮无新增审查；保留历史 owner/mirror 记录 | 新场景0 | K1/K2边界完成后继续 owner/fence/epoch、迁移/镜像/防复活；不重新宣称 mirror 已落地 |

本轮首先响应修复验收，K4/K6 接触仅限对应修复与其资产边界；没有转去新的 K7 专项。两项问题的修复由用户实施，下轮若声明未修复，跳过本轮旧复现，直接从 K1 下一入口继续。机制解释更新于[跨层契约文档](IMPLEMENTATION-GENERATED-FEATURE-CONTRACTS.md)。
