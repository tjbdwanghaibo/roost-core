# 2026-09-14 第二轮：WAL 修复验收与 Activity 生命周期

本轮使用 roost-review/codebase-memory，默认 Verify。只更新文档，不修改生产代码、测试、依赖或生成模板。

## 同步与基线

2026-09-14（Asia/Shanghai，09:25 核对记录），三个 main 工作树开始均干净，pull --ff-only：

| 仓库 | 更新前 | 更新后 |
| --- | --- | --- |
| Core | bbd2d8bc1e497438f7c180c833d665948a32f11d | 84b2a4aa2e6354d726d2c268516df9a3e8141218 |
| Kit | ac1a8801604bde4b7ec5ed60104502c542602ed0 | 不变 |
| Codegen | cacd627b70991c5d0e38545866610db78e695b53 | 不变 |

Core 增量为 U-0190：WAL 关闭中 Sync 屏障修复、测试及文档。已读 bug 索引、bugfix/RR-20260914-01.md、history/ledger.md 的 U-0190 与上轮记录。Kit 无新提交仍轮转 Activity；Codegen 本轮仅同步，不计新增源码覆盖。

## 结果

- RR-20260914-01 原交错复跑通过；关闭取消重试与最终同步失败补证通过。
- 新 P2 RR-20260914-02：OpenActivity 与 sweep 交错使成功创建的活动失去窗口索引。
- 新 P2 RR-20260914-03：内置派发循环仅处理本次新完成活动，跨轮重试、notify 完成与 heal 都有扫描遗漏。
- 正常开活动/到期、显式派发重试/ACK 对照通过。详细触发与建议在[问题文档](../bug/REVIEW-2026-09-14-02.md)，新增[Activity 机制学习](IMPLEMENTATION-ACTIVITY-WINDOW-AND-DISPATCH.md)。

## 实际测试

| 命令/场景 | 结果 |
| --- | --- |
| Core `go test -race -count=1 ./nestwal` | PASS 4.451s |
| WAL overlay，`-run 'TestReview\|TestWALSyncPromise' -v` | PASS 1.848s；七个叶子场景（原有屏障/关闭五个、新增两个） |
| Activity overlay，`-run TestReview -v`（加入对照之前） | FAIL 0.795s；开活动交错 + grace/notify/heal 共四个失败场景 |
| Activity overlay，`-run TestReviewActivityControls -v` | PASS 1.785s；正常到期、显式两次尝试、ACK 排除 |
| Kit `go test -race -count=1 ./service/global/activity` | PASS 1.905s |

overlay 运行使用临时 go.work 引用当前 Core/Kit/Codegen，未修改三个 go.mod。临时 go.work 初次用正斜线路径时 Go 报模块未列入 workspace；改成带引号的 Windows 原生绝对路径后成功，初次 setup 失败不计业务失败。附录保存完整临时测试与复跑方式。

WAL 最终同步失败通过在完成异步 Append 后关闭真实文件句柄注入，随后 Close 报错、Sync 返回 ErrCommitIndeterminate。它验证失败传播，不模拟物理掉电。最初用 Enqueue 的测试误要求已成功的 ticket 变失败；源码显示 Enqueue 强制 requireSync=true，ticket 本来已经持久化，已纠正测试前提，未把这个错误断言列为产品 bug。正确测试改用非 strict Append，保留真实待同步数据。

关闭取消测试在现有 beforeProcessBatch 缝暂停 writer，Close 短超时后恢复 writer，再次 Close、Sync、ticket 全部成功。原问题使用本轮上游新增的原交错承诺测试复跑并阅读修复逻辑；新增边界由独立临时测试验证。未声称本轮执行了上一轮原始 overlay 文件。

## 图谱与限制

list_projects/index_status 确认 D-whb_s，generation 仍为 2026-09-08T14:39:26Z。WAL 搜索七条、Activity 派发搜索 49 条、窗口搜索两条，均无剩余分页；syncAndCloseActive、AdvanceExpired、admitToWindow 双向 depth=1 trace 未截断。最初错误的 Activity 目录搜索无结果后，coverage 与文件列表确认实际位于 service/global/activity，未据此推断模块不存在。

coverage 所有业务证据路径为 metadata_changed，新增 WAL 测试 not_tracked；未记录解析缺口不表示最新完整。get_code_snippet(syncAndCloseActive) 按旧行号取到 openActive 片段，已舍弃，改读当前文件。部分 trace 包含同名 Close/Errorf 错连，不作为跨模块影响结论。当前源码与实际测试是本轮主要证据，未重建用户索引。

WAL 使用真实本地文件；Activity 使用 MemoryStore、包装器和注入时钟，未连接 Redis/消息总线。复现得到四个行为失败，无 race 报告；包 race 通过不能替代故障交错测试。

## 进度与下轮入口

累计 26 份运行记录、17 篇机制文档、39 RR（37 标修复、2 未修复）；这是文档/问题数量，不是全仓覆盖率。OQ-05 原错误及两项边界已有新证据，物理故障/阻塞 I/O 尚未验证；其他 OQ 保留。更新见[进度](PROGRESS.md)与[未收敛工作](OPEN-QUESTIONS.md)。

下一轮先验收 Activity 两项修复，继续派发索引与发送交接、ApplyProgress 去重在局部失败后的恢复，以及 Codegen 消费者轮转。Mirror 与跨节点水位方案仍待实施，不因本轮 WAL 验收而关闭。
