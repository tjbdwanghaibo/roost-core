# 09-17 第二轮：Wanted、五项修复与恢复入口

用户要求继续 review 并注意 wanted。本轮已完成代码/测试，首次写文档时工具自动审批以额度耗尽拒绝；用户要求恢复后补齐文档。原临时测试与 new-results.txt 均仍存在，不重复运行已完成矩阵。后续 feature 审查另记第三轮，避免混淆基线。

## 基线

三仓起始工作树干净、main。Core 从 ab0133967a8fb0d130958b429ae61c606384f61a fetch/pull --ff-only 至 `6fb36e79da85d2cf8b3df6d681916478f1e695f3`；Kit `48301507d06343f0de788de38f8a670f45adcf49`、Codegen `242b4384d4d96ad616b4197396c55972835cd741` 同步前后不变。均为 tjbdwanghaibo/roost-{core,kit,codegen} origin/main。无额外 AGENTS.md。

已读五份新 bugfix、ledger U-0219～U-0223 与代码差异；用户此轮没有要求跳过验收，且确有新修复，恢复定向复核。此前其他旧 RR 不重新穷尽验收。

## 结果

[问题与验收](../bug/REVIEW-2026-09-17-02.md)：manager 两项、match 三项原触发通过；match 特殊字符历史键升级仍有限制。新增 P2 RR-20260917-04：platform 后台重试候选固定为空，部署没有注入入口。

[ARCH-05](IMPLEMENTATION-SERVICE-PLACEMENT-AND-RECOVERY.md)：Wanted 六个 RPC 服务都有领域实现，directory 先迁；列出 Kit 保留文件、RegistryBound、chat/activity 私有访问处理与生成/版本验收要求。不把架构职责单独计功能 RR，不进行代码迁移。

## 实际测试

Windows、Go 1.27.0，本地三仓 go.work 在仓库外 review-temp-0917b；overlay 不修改模块/源码。

| 组 | 叶子/结果 |
| --- | --- |
| 原 manager overlay | 3 通过：last、has_next_control、Register |
| 原 match 最小 overlay | 3 通过：QueueCollision、ScoreOverflow、MemoryResultOwnership |
| 新 EscapedKeys | 1 通过，内部检查 72 组合，不当作 72 测试叶子 |
| 新 ScoreRegression | 6 通过，含四原溢出边界与两个对照 |
| 新 AdditionalOwnership | 8 通过 |
| 新 LegacyKeyUpgrade | 5：1 对照通过、4 失败 |
| 新 PlatformRecovery | 3：手动/渠道重试通过，真实 owner timer 32 秒失败 |
| 新矩阵合计 | 23：18 通过、5 行为失败；与原 6 项复核分别计数 |

Core 原 manager/match 两包、Kit manager/match/account/chat/global/global/activity/platform/rank/directory 九包 `go test -race -count=1` 均通过。首次旧复现命令误在工作区根使用 ./manager 路径，报目录不存在；修正 cwd 后通过，准备错误不算产品 bug。未运行真实 Redis、渠道或 bus 端到端，未验证 Linux/性能/发布 tag。

新矩阵命令：`go test -race -count=1 -overlay <overlay.json> ./cube-core/service/match ./cube-kit/service/platform -run 'TestReviewEscaped|TestReviewScoreRegression|TestReviewAdditional|TestReviewLegacy|TestReviewPlatform' -v`（cwd 工作区）。完整代码见 [REPRO](../bug/REPRO-2026-09-17-02.md)。

## 图谱

Verify，list_projects/index_status 确认 D-whb_s、ready、26320 nodes/195520 edges，generation 2026-09-08T14:39:26Z。六接口查询 10 行、构造/入口 37 行、platform 6 行均无更多页；platform retryOrders 双向 trace 的 caller 为 run，snippet 确认固定 nil。

Core 迁入路径及 assembly 文件 not_tracked，Kit 相关路径 metadata_changed；覆盖检查包含 manager/match、七包领域入口及维护文件，scopes 无更多记录不代表完整。实际源码/import 清单补证，排除历史仓库。没有重启索引。图谱陈旧不用于作“没有调用/实现”的否定证明。

## 续审入口

先处理待实施 ARCH-05、RR-04 与匹配升级计划。下一轮 feature 增量单独审查、另计基线；本轮未将七包或整个 Kit 标记完成。[进度](PROGRESS.md)。
