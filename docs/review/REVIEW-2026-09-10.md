# 2026-09-10：Match 清理、Room 与 Skill 轮转

新增一个低频 P3：[RR-20260910-01](../bug/REVIEW-2026-09-10.md)。三个相关包 race 通过，两个新增行为验收通过。未修改源码。

## 同步与旧问题

三仓工作树开始干净，main 跟踪 origin/main；pull --ff-only 均返回 Already up to date。更新前后 SHA 相同：

| 仓库 | SHA |
| --- | --- |
| Core | 8ea815930b4b32bf59cb4a894e50782687046e0d |
| Kit | c4e7cef1029fb6326fa88b84c622f059bc62c0c4 |
| Codegen | aa072edb35a0b97d234e0155142e36295c5f21d1 |

Core 与上轮源码基线 1f7bb54 的差异只有已提交的审查文档。读取 bug/bugfix 索引、PROGRESS、历史账本：上轮 RR-20260909-05、06 无新修复，保留状态，不重复复现相同源码；本轮 Codegen 只核对同步及既有问题，未新增生成器审查。

## 图谱证据与范围

采用 codebase-memory Verify。项目 D-whb_s ready，26320 节点/195520 边，coverage generation 2026-09-08T14:39:26Z，早于当前基线。先搜索 Match/Room/Skill；room 初始旧路径无结果，依据历史迁移记录转到 core/room，不以空图谱结果推断代码不存在。

Room Broadcaster 搜索 62 项，60+2 翻页完成；Scheduler 有界文件搜索 44 项。Skill 广泛搜索 50/206 后缩小到 scheduler.go，不作全 Skill 符号或覆盖穷尽结论。Create 的 snippet 和双向 trace 提示 GetOrCreate → Create；其他低置信度同名 heuristic 边用源码甄别，不作为真实调用关系。

统一 coverage 检查 11 条路径：Room manager/其测试/broadcaster，Skill scheduler/其测试/cast_window/cast_policy 测试/regression_v141 测试，Match queue_store/其测试，历史账本。源码文件 metadata_changed，关键函数直接读当前源码；账本 partial 311 行补读。没有重建用户索引，也没有把 no_recorded_issue 当完整覆盖保证。

## 阅读和验证

- Match：Sweep/expireLockedLimit、QueueLength，过期票据从 Waiting 和 SubjectTickets 移出，保留 Tickets/Requests 以支持历史重放。独立测试验证 Sweep=1、旧 request 返回原 expired ticket、新 request 可重新入队且 Candidates 只含新票。历史保存的容量/清理策略尚未验证，不将其直接列为确认 bug。
- Room：Create/Get/GetOrCreate、Remove/Close、空闲清扫、Broadcaster Close/Stop、共享预算。现有测试覆盖超时重试与预算回收；发现默认周期推导边界 P3。
- Skill：调度排序、局部帧、阶段取消、旧任务丢弃、策略槽归属释放。独立取消 wait 测试验证原定 damage 不会在取消后触发。

实际命令均使用 Go 1.27.0、CGO_ENABLED=1、临时 go.work 指向最新三仓：

1. Core：`go test -race ./room ./skill -count=1 -timeout=180s`：两个包通过。
2. Kit：`go test -race -overlay <overlay.json> ./service/match -count=1 -timeout=120s`：通过，包含新增 Sweep 测试。
3. Core：`go test -race -overlay <overlay.json> ./room ./skill -run '^TestReview' -count=1 -timeout=120s`：Skill 通过；Room 的正 TTL 默认周期断言失败，整体退出 1。此失败是问题证据，不是 race 报警。

新测试全文见[复现及验收附录](../bug/REPRO-2026-09-10.md)。旧临时 go.work 已被清理，首次测试无法启动；重建时正斜杠路径未被本机 Go 正确识别，改为 Windows 原生路径后测试执行。两次环境失败不计产品失败；没有更改模块依赖或源码。

## 学习与下轮

新增[Room/Skill 实现学习](IMPLEMENTATION-ROOM-AND-SKILL-SCHEDULING.md)，维护[进度表](PROGRESS.md)。本轮无真实 Redis、跨服链路、Linux 集成或正式 tag 消费者验收，也不表示 Room/Skill 全包审查完成。

下轮先核对三项未关闭问题的 bugfix，再审 Remote Entity 预分派、Mail 发奖回执边界；Skill 继续取消失败与 checkpoint/恢复，Room 继续下游失败与退休重试。个人 skill 已包含本轮所需进度/文档/推送流程，无新规则需要修改。
