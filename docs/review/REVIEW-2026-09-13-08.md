# 2026-09-13 第八轮：残余修复与原故障验收

本轮使用 roost-review/codebase-memory，只写文档。三仓起始干净、main；pull --ff-only 后 Core 从 `d8e3cc82c070908e5632b1d9edd6e150b1de0e5f` 到 `66f65e258bc5655d929e54300dc9a18377ff7e3b`，Kit `ac1a8801604bde4b7ec5ed60104502c542602ed0`、Codegen `cacd627b70991c5d0e38545866610db78e695b53` 不变。核对 bug/bugfix 索引、RR-01/02/05 补修、WAL 与完成回调说明和 ledger U-0185–0188 条目。

## 实际测试与结论

所有 Go 命令均使用 `-race -count=1`。没有把包测试替代原复现。

| 范围 / RR | 实际验证 | 结果 |
| --- | --- | --- |
| RR-20260913-01/05/06 | 第七轮 entity overlay 四场景：墓碑与在途回填、冷 L1 删除新 L2、预检查后冲突、同版本回填 | 四项 PASS，1.960s；L2 使用当前仓库 l2Fake，包含新增版本化删除能力，未覆盖不实现该可选接口的第三方 Store |
| RR-20260913-02 | 原 TestManagerStampsInterestMessagesWithAdvancingGenerations，以及冻结时钟播种测试 | PASS；同一命令也匹配一个旧锁代际对照，合计 2.302s；不证明跨进程回拨安全 |
| RR-20260912-01/02 | 09-12 原 WAL overlay：held 阻塞后释放、Shutdown 截止、Sync 已准入 ticket、Close 排空、重开恢复 | 五项 PASS，2.802s，使用临时真实文件 |
| RR-20260911-05 | 原 TestReviewClaimHistoryRefusalIsAtomic | PASS，3.415s；Kit 按 go.mod 的 core v1.15.2 运行，不是本地 core 联调 |
| RR-20260911-06 | 原 TestReviewAfterCommitPanicPreservesReplyAndRelease | PASS，2.296s；回复到达且释放通知恰好一次 |
| RR-20260911-06 饱和路径 | 子进程 normal / panic；按新错误契约适配断言 | 两项 PASS，4.452s；panic 返回 ErrAfterCommitFailed，子进程正常结束 |
| RR-20260913-09 | 真实 Redis CAS 后注入回复丢失；发送前失败对照 | 两项 PASS，3.356s；旧 owner 不再被准入，发送前失败仍能恢复 |
| Core entity/cache/remoteentity/nest/nestwal | 五包完整 race 回归 | 全通过，分别 2.960s / 1.741s / 2.973s / 3.350s / 6.894s |

饱和子进程原测试先失败：返回已提交但回调失败的 error，旧断言却要求业务值 17，子进程退出 1；与历史未捕获 panic 的退出 2 不同。仅将 panic 分支改为 errors.Is(ErrAfterCommitFailed)，normal 仍要求 17，保留回复超时和 pump.stop。不能把原失败说成产品仍崩溃。

Transfer 原测试要求注入 I/O 错误一定向上传播；现在权威确认已转移会按成功返回。适配仅取消该分支必须报错的断言，发送前失败仍必须报错，最终 Redis owner 和旧 owner 准入断言保留。真实后端为独立 Redis 15998，bind 127.0.0.1、禁用持久化、独立临时目录；完成后 DBSIZE=0，SHUTDOWN NOSAVE，日志确认退出。

## 图谱与源码

Verify，list_projects 确认 D-whb_s；coverage generation 2026-09-08T14:39:26Z。nestwal 搜索返回 14 项，无剩余分页；Shutdown 双向 trace 指向 Flush/Close，低置信度跨仓调用不作完整影响证明。coverage 检查 wal/committer、remote_snapshot、read_through、transaction_manager、snapshot_l2、ownership、pipelined_completion、rollback、mail/service；均 metadata_changed，实际源码和执行路径补证。未宣称图谱最新或全仓穷尽。

新增阅读：WAL Sync 的 FIFO barrier 与 fsync 分工、Shutdown 的可取消 semaphore 等待；Transfer 独立 context 重查与 Recovering/Fenced 分支；cache 的 Superseded/FatalRemoteError 准入。相关机制更新见[验收学习](IMPLEMENTATION-REPAIR-ACCEPTANCE.md)。

## 边界与续跑

无新增确认 bug。以上是所列原触发和交错验收通过，不是整个协议已完成：删除无跨节点 L2 墓碑，TombstoneTTL 未与真实重放窗口建立证明；第三方 L2 未实现版本化删除会回退；兴趣跨进程时钟回拨仍待设计；EnterShared/LeaveShared 未知结果恢复尚未验收。Linux 强杀、Redis failover、磁盘 fsync 故障和性能压测未做。

下轮先查新增修复，再轮转 EnterShared/LeaveShared 的执行后回复丢失、共享 L2 跨节点墓碑和 WAL Sync/Close 交错。Mirror 方案仍待实施；本轮不修改其运行时代码。复跑材料见[验收附录](../bug/REVIEW-2026-09-13-08.md)，覆盖见[进度](PROGRESS.md)。
