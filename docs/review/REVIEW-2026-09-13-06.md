# 2026-09-13 第六轮：Mirror 生命周期验证

三仓 main 干净，pull --ff-only 均 Already up to date。更新前后 Core `d1b14b99a9fcf9ed4029966ac55372407b5a52a4`，Kit `3855c71f5aaaf5ca4b7091ae17bf8cb0e6943b79`，Codegen `cacd627b70991c5d0e38545866610db78e695b53`。remote 与 go.mod 身份一致，无新增源码或修复提交。

核对 bug/README、bugfix/README、history/ledger 相关 U-0178/0179 条目和第五轮记录。旧问题维持原状态，RR-20260913-08 仍部分修复；本轮没有重复运行无增量的旧复现。

## 证据与验证

采用 roost-review/codebase-memory Verify。list_projects 确认 D-whb_s 根 D:/whb_s。coverage generation 为 2026-09-08T14:39:26Z，envelope.go 和 sync.go 均 metadata_changed，已读取实际源码补证。mirror scope 无记录缺口不等于完整覆盖。search_graph 在 mirror 返回 5 项、syncbus 返回 10 项，均无剩余分页。get_code_snippet 读取 Start；Stop 双向 trace 零关系不作为无调用者证明。

临时 overlay 的三个确定性场景通过：`go test -race -count=1 -overlay <overlay.json> ./mirror -run '^TestReview' -v`，1.480s。

| 测试 | 已验证行为 |
| --- | --- |
| TestReviewStopDoesNotDrainApply | 阻塞 Apply 后 Stop 返回，接收 context 未取消；解除屏障后 Apply 在 Stop 后完成 |
| TestReviewOldCallbackSurvivesRestart | 保存旧 handler，Stop/Start 后调用仍进入 Store；模拟已被传输层取出的回调 |
| TestReviewStartFailureRetryAndIdempotence | Subscribe 失败后重试成功；重复 Start 不重复订阅；重复 Stop 只解绑一次 |

`go test -race -count=1 ./mirror ./syncbus` 两包通过，分别 1.563s / 1.564s。测试源码见[观察记录](../bug/REVIEW-2026-09-13-06.md)。PASS 刻画当前行为，不表示新 Mirror 的强关闭契约已经实现。

## 结论与续跑

无新增确认 RR。ISubscriber 未承诺 unsubscribe 等待回调完成，故以上列为能力边界，不报成接口违约。新客户端必须自己实现准入关闭、回调 generation、在途计数及有界等待，见[机制文档](IMPLEMENTATION-MIRROR-LIFECYCLE.md)。不能在 Stop 后立即释放仍被使用的 Store 依赖。

Kit/Codegen 本轮仅同步，无新增代码覆盖。没有真实 broker 调度、网络重连、Linux 部署或性能压测证据。下一轮先核验新增实施/修复；无增量时检查实际 bus 适配器的解绑与回调完成语义。进度见 [PROGRESS](PROGRESS.md)。
