# 2026-09-13 第二轮：L2 冲突与回填交错

三仓 main 起始/结束同步检查无无关改动，pull --ff-only 均 Already up to date。更新前后基线相同：

| 仓库 / origin | SHA |
| --- | --- |
| core / tjbdwanghaibo/roost-core | 2724442d886d9bb3ee5617d7ded814ce0f5267cc |
| kit / tjbdwanghaibo/roost-kit | 3855c71f5aaaf5ca4b7091ae17bf8cb0e6943b79 |
| codegen / tjbdwanghaibo/roost-codegen | cacd627b70991c5d0e38545866610db78e695b53 |

已核对 bug/bugfix 索引、上一轮运行记录、进度与相关源码；无新增修复。按用户要求继续后续范围，不重复未修复 RR 与未实施 Mirror 的原测试。本轮不改源码/依赖，仅审查文档。

## 图谱与补证

codebase-memory Verify：list_projects/index_status 确认 D-whb_s、D:/whb_s、ready、26320 nodes/195520 edges。generation `2026-09-08T14:39:26Z`，关键源码均 metadata_changed。cache Get/Set/Delete 搜索 33 条、remote L2 搜索 10 条，无分页；setLocal snippet 行号陈旧，返回了 Stats 邻接内容，已读取整个当前 read_through 相关范围纠正。双向 trace 各两条，无分页，低置信调用边仅供定位。

coverage 包含 snapshot_l2.go、snapshot_l2_test.go、entity/remote_snapshot.go、cache/read_through.go、cache/atomic_local.go，并加 remoteentity/entity/cache scope，均无记录缺口和下一页；这不证明图谱已更新。Expiry 字段引用为 literal 源码检索，核查目标两个文件未调用 Expired，结论限定实际读取链。ledger 已知 partial 第 311 行读原文。未进行其他三仓功能的穷尽检查。

## 八个新增场景

临时目录 `C:/Users/tjbdw/AppData/Local/Temp/roost-review-0913b-8c4eb37986c84622a384bba4706a7648`；remote_test.go/entity_test.go 通过各自 JSON overlay 注入真实包。完整代码见[复现文档](../bug/REPRO-2026-09-13-02.md)。

| 场景 | 结果 |
| --- | --- |
| L2 拒绝 payload 冲突，上层 Publish | FAIL，nil，L1=B/L2=A；RR-05 |
| L2 同版本修改 schema | FAIL，nil，schema=2；RR-07 |
| L2 重复与旧版本 Set | PASS，保留新版本 |
| 已在途 L2 响应与同版本 Publish 交错 | FAIL，A 覆盖 B；RR-06 |
| ExpiresAt 已过期且容器 TTL 未过 | FAIL，仍返回命中；RR-08 |
| 网络错误降级至 L1 | PASS |
| 本地同版本 schema 冲突 | PASS，拒绝冲突 |
| 权威 loader 首次失败、下次读重试 | PASS，两次调用后成功 |

```text
go test -race -count=1 -overlay <temp>/remote.json ./remoteentity -run '^TestReview' -v
FAIL，2 失败/1 通过，0.654s
go test -race -count=1 -overlay <temp>/entity.json ./entity -run '^TestReview' -v
FAIL，2 失败/3 通过，0.568s
go test -race -count=1 ./entity ./cache ./remoteentity
PASS，1.953s / 1.341s / 2.700s
```

失败均是行为断言，没有编译失败或 race detector 报告。Redis 使用仓库已有 snapshotRedisFake 并核对 Lua 比较字段；它不执行 Lua，也不能证明大整数、真实 Redis TTL/原子性。回填交错使用 channel 屏障，不依赖概率 sleep。未新增 kit/codegen 测试，无临时 go.work/依赖变化。

## 结果与下一入口

[四项新 P2](../bug/REVIEW-2026-09-13-02.md)、[实现学习](IMPLEMENTATION-REMOTE-L1-L2-CONSISTENCY.md)、[跨轮进度](PROGRESS.md)。本轮集中完成缓存一致性切片，开头计划的完整订阅恢复尚未扩展实测；没有将它标为完成。

下一轮优先核验修复；无修复则继续真实 Redis 脚本/数值域、L2 删除交错、owner 换代与回填失败后的订阅再同步。八项历史未关闭 RR 保持原状态，本轮新增四项；Mirror 方案仍只是方案。
