# 2026-09-19 独立复现记录

复现先基于 Codegen `992cc23`，随后在 `e12a4d3` 上从头生成并复跑；batch15 到达后又在最终 `7297f92` 生成 `review-temp-0919c`。临时模块均未提交到三仓。

## K1 DAO 所有权矩阵

当前 golden 生成到临时 `dao` 包；另用以下最小定义生成 `pointerdao`：

```go
//roost:dao coll=holders db=game dbscope=sid
type HolderDao struct { Child *Child }
type Child struct { Value int32 }
```

执行：

```text
go test -race ./dao ./pointerdao -count=1
```

共 11 个行为叶子，8 过、3 失败：

| 组 | 叶子 | 结果 |
| --- | ---: | --- |
| 同 key `Set→Set` / `Set→Del` / `Del→Set` 回滚 | 3 | 全过；old 恢复且弃用值解绑 |
| commit 拒绝 / transaction panic | 2 | 全过；old 恢复且 new 解绑 |
| 替换为 nil | 1 | 通过；old 解绑 |
| 同一 nested 指针占两个 map key | 1 | **失败；只有 `equips.2` 被标脏** |
| 顶层 pointer 提交/回滚 × old/new | 4 | 2 过 2 失败 |

关键失败：

```text
TestReviewAliasedMapValueMarksEveryOwnedKey:
  aliased value writes map[equips.2:true], want both equips.1 and equips.2

TestReviewTopLevelPointerOwnership/committed_old_is_detached:
  commit records = 1, want 0
TestReviewTopLevelPointerOwnership/rolled_back_new_is_detached:
  commit records = 1, want 0
```

测试不是只看 callback 次数：map alias 用 BSON 解码真实 `CommitRecord.Mutations[].Patch.SetBSON`，确认最终路径缺少 `equips.1`。

## 生命周期 callback panic

在新生成 demo 的 `internal/access/player/tcp` 同包增加一个订阅者，该订阅者 panic，再直接执行生成物 `deliverClosed`：

```go
runtime := &Runtime{subscribers: map[uint64]func(SessionClosed){
    1: func(SessionClosed) { panic("review subscriber panic") },
}}
runtime.deliverClosed(SessionClosed{})
```

执行：

```text
go test ./internal/access/player/tcp \
  -run TestReviewSessionCloseSubscriberPanicIsContained -count=1
```

稳定失败，panic 穿出派发边界。为了确认失败来自新增断言而非生成工程基线，跳过该独立红测后执行 `go test ./... -count=1`，
新生成 demo 全包通过，其中 `internal/service/game` 的 scene 关闭清理测试通过。

## 修复验收与环境限制

- Core：`go test -race ./service/mail ./spatial ./dataengine ./fctx -count=1` 通过；`configdata` 使用工作区 TMP/GOCACHE 后单独 race 通过。
- Codegen：`go test ./internal/entity ./internal/dao -count=1` 通过；8 个本轮相关 `internal/roost` 测试通过。
- `scripts/dao-golden-runtime.sh` 在 Git Bash 配置 Go 1.27.1 与工作区 GOCACHE 后通过，证明 RR-20260918-10 官方 map/slice 八叶子通过。
- 当前 codegen 重新创建的 demo 在 source `go.work` 下全包通过；没有真实 Mongo、Redis、NATS、TCP 客户端或多进程部署。
- `project new` 已完成渲染，随后依赖解析受网络访问限制，改用本地 source workfile 完成编译与行为验证；网络失败不计产品缺陷。

## 收尾期间新增的 U-0245 / game-demo 第十四批

Codegen `e12a4d3` 到达后，重新生成当前 golden 与完整 game-demo：

- 独立新增 `TestReviewFreshDaoWiresNestedValueWithoutManualInit`：只调用 `NewHeroDao()`，不手调 `Init()`，直接执行
  `hero.GetPos().SetX(7)`；真实 Nest transaction 产生 1 条 CommitRecord，U-0245 原触发通过。
- `scripts/dao-golden-runtime.sh` 通过，包含作者的 fresh DAO 回归和既有所有权门。
- 新 game-demo `go test ./... -count=1` 全包通过；`db/migrations`、`game/entities/player`、`game/handler`、
  `internal/service/game` 均实际执行，覆盖装备组件、回滚和 v1→v2 restore 接线。
- 随后把三项独立红测重新施加到 `e12a4d3`：map alias 仍只写 `equips.2`，顶层 pointer 仍 2/4 失败，
  session-close callback panic 仍逃逸。因此 RR-20260919-01/02/03 不是旧生成物误报。

`go test ./internal/roost -count=1` 的全包门仅因宿主 `%PATH%` 没有 `sh`、且未安装 shellcheck 而失败；限定 `^TestDemo`
的五项模板测试通过。该环境失败与项目源码无关，不计问题。

## batch15：platform 待办索引与付费 grant

最终同步 Kit `5116f2a`、Codegen `7297f92` 后，重新执行：

```text
go test -race ./service/platform
go test ./internal/roost -run '^Test(Demo|HostedService|PlatformConfig|DefaultCollaborators)'
go generate ./...
go test ./...
```

Kit platform race、Codegen 定向门和新生成 game-demo 全包均通过。`project new` 已完成 379 个文件的渲染，依赖解析因沙箱网络被拒，
随后用 Core/Kit/Codegen source workfile 完成生成和全包运行；这仍不是 Redis 多进程实跑。

在新生成工程的 `internal/service/platform` 同包增加两条只读审查用例：

1. 使用真实 `platform.Service`、Memory OrderStore 和生成的 `grantDeliverer`，令 pending ZADD 失败。`HandleCallback` 返回失败后，
   `Order("order-gap")` 仍能读到非终态持久订单，grant 写入数为 0；恢复 ZSet 后 `PendingOrders(128)` 仍返回 0 个 id。
2. ZSet 中 `broken` score=1、`healthy` score=2，仅让 `Order("broken")` 返回解码错误。`PendingOrders(128)` 返回错误和空结果，
   healthy 仍在集合中但未被交给后台循环。

执行输出：

```text
=== RUN   TestReviewUndeliveredOrderCanMissTheOnlyRetryIndex
--- PASS: TestReviewUndeliveredOrderCanMissTheOnlyRetryIndex
=== RUN   TestReviewOneUnreadableOrderBlocksHealthyOrdersBehindIt
--- PASS: TestReviewOneUnreadableOrderBlocksHealthyOrdersBehindIt
PASS
```

这里 PASS 表示复现断言准确捕获当前行为，不表示产品行为正确。第一条确认 RR-20260919-04 的不可枚举窗口，第二条确认 RR-20260919-05 的页首阻塞。

RR-20260919-06 由现成真实 Nest 测试和源码组合确认：`TestAGrantPastItsWindowIsRefusedRatherThanRepeated` 证明 30 天后的 grant 必然返回
`ErrPurchaseExpired`，而 `PurchaseDrain.DrainPlayer` 的该分支不 `continue`，随后无条件 `HDel`；平台 deliverer 在写 grant 成功后已经把订单推进为
`DeliveryDelivered`。无需等待 30 天，也不依赖时钟竞态。
