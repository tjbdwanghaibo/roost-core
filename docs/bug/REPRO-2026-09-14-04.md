# 第四轮 lockstep 复现

Core 30a6b5bbdc9dc3df93ad16a1249d37e1b7c51995，Go 1.27.0。以下为实际运行代码，仅通过临时 overlay 映射到 lockstep/review_extended_test.go。生产源码未改。结果见 [运行](../review/REVIEW-2026-09-14-04.md)。

Overlay JSON 的 Replace：D:/whb_s/cube-core/lockstep/review_extended_test.go → 保存本代码的临时文件绝对路径。设置临时 GOWORK 引用本轮三仓，Core 目录执行：

```powershell
go test -race -count=1 -overlay <overlay.json> ./lockstep -run 'TestReviewAccepted|TestReviewOld|TestReviewUDP' -v
```

结果：身份表边界失败，超期限重放边界与本机 UDP 对照通过。UDP 丢包是在读取成功后主动丢弃，不是内核故障注入。

```go
package lockstep
import("context";"net";"testing";"time";corestate "github.com/tjbdwanghaibo/roost-core/statesync")
func TestReviewAcceptedIdentityBound(t *testing.T){
 s,err:=NewSequencer(SequencerConfig{Players:[]PlayerID{1}});if err!=nil{t.Fatal(err)}
 for i:=0;i<4096;i++{s.Advance()}
 for frame:=FrameID(1);frame<=4096;frame++{if _,err=s.SubmitInput(1,frame,[]byte{1});err!=nil{t.Fatal(err)}}
 bound:=int(ReplayHorizon+MaxSubmitWindow+1)
 if len(s.accepted[1])>bound{t.Fatalf("one seat before next tick: accepted=%d bounded_window=%d pending_frames=%d",len(s.accepted[1]),bound,len(s.pending))}
}
func TestReviewOldReplayBoundary(t *testing.T){
 s,_:=NewSequencer(SequencerConfig{Players:[]PlayerID{1}});s.SubmitInput(1,1,[]byte{1});s.Advance()
 for s.NextFrame()<=ReplayHorizon+1{s.Advance()}
 s.SubmitInput(1,1,[]byte{1});if frame:=s.Advance();len(frame.Inputs)!=1{t.Fatal("documented out-of-horizon behavior changed")}
}
type reviewUDPSender struct{conn *net.UDPConn;peer *net.UDPAddr}
func(s reviewUDPSender)SendDatagram(_ context.Context,_ corestate.SessionID,p []byte)error{_,err:=s.conn.WriteToUDP(p,s.peer);return err}
func TestReviewUDPLoopbackRedundancy(t *testing.T){
 recv,err:=net.ListenUDP("udp4",&net.UDPAddr{IP:net.IPv4(127,0,0,1)});if err!=nil{t.Fatal(err)};defer recv.Close()
 send,err:=net.ListenUDP("udp4",&net.UDPAddr{IP:net.IPv4(127,0,0,1)});if err!=nil{t.Fatal(err)};defer send.Close()
 r,err:=NewRoom(RoomConfig{Sequencer:SequencerConfig{Players:[]PlayerID{1},MaxInputBytes:4},RedundancyDepth:3,Datagrams:reviewUDPSender{send,recv.LocalAddr().(*net.UDPAddr)}});if err!=nil{t.Fatal(err)};defer r.Close()
 if err=r.Attach(1,101);err!=nil{t.Fatal(err)};a:=NewFrameAssembler(8);applied:=0
 buf:=make([]byte,4096)
 for id:=FrameID(1);id<=24;id++{
 if _,err=r.SubmitInput(1,id,[]byte{byte(id)});err!=nil{t.Fatal(err)}
 if _,err=r.Tick(context.Background());err!=nil{t.Fatal(err)}
 if err=recv.SetReadDeadline(time.Now().Add(time.Second));err!=nil{t.Fatal(err)}
 n,_,err:=recv.ReadFromUDP(buf);if err!=nil{t.Fatal(err)}
 if id==5||id==6{continue} // received then intentionally discarded, not kernel packet loss
 frames,err:=a.Ingest(buf[:n]);if err!=nil{t.Fatal(err)}
 for _,f:=range frames{applied++;if f.ID!=FrameID(applied)||len(f.Inputs)!=1||f.Inputs[0].Payload[0]!=byte(f.ID){t.Fatalf("bad frame %+v",f)}}
 }
 if applied!=24||a.Gap(){t.Fatalf("applied=%d gap=%v",applied,a.Gap())}
}

```
