# RR-20260925-11：通用故障矩阵遗漏 Remote 集成套件

日期：2026-09-25；基线：feature/arch-10-sync-policy 的累计优化工作树，提交前全模块回归发现。等级 P2；状态：已修复，验证见[修复记录](../bugfix/RR-20260925-11.md)。

新增 Remote 实际 Redis/Mongo/进程故障测试后，独立 `scripts/test-remote-matrix.sh` 已运行它们，但 nightly 使用的 `kit/scripts/integration/dataengine-env.sh test` 包列表未加入 `./remoteentity`。

预期：通用完整环境入口覆盖仓库所有对应集成包。实际：`TestFaultMatrixScriptNamesEveryFullEnvironmentSuite` 失败，明确报告 `remoteentity has full-environment integration tests the fault matrix never runs`；nightly 入口本身遗漏此包。

复现：`GOWORK=off go test . -run '^TestFaultMatrixScriptNamesEveryFullEnvironmentSuite$' -count=1`。本地 `go test ./...` 还扫描到忽略目录中的历史 .go 快照，这是本地产物隔离问题，和本缺陷分开；提交内容通过不含这些产物的干净源码副本验收。
