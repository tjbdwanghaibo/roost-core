# RR-20260926-31：demo 闲置交还先驱逐并释放租约，未等本实体投影完成，其他进程接手时从 Mongo 读到旧版本

日期：2026-09-26。基线：main `985d5ba`（v1.16 起已有）。等级 P2（多进程部署；后果为两边投影版本冲突 fatal）。RR-10 修复只覆盖进程内重载。
状态：已确认（源码判定），未修复。

## 触发条件

多进程部署；`handBackIdle` 先 `dropResident`（`Destroy(..., false)`，`demo/internal/service/game/playerowner.go.tmpl:506`）再 `Release` Redis 租约（`:511`）；
此时本进程 WAL 中该玩家投影未完成（投影延迟超过约 20 秒 HandBackIdle）；玩家随后在另一进程登录。

## 预期 vs 实际

预期：交出所有权前等待本实体已写 WAL 的事务投影完成。
实际：`WaitEntityProjection` 只跟踪本进程在途记录（`dataengine/engine/entity_projection.go:10-11,66-90`）；B 进程 `Claim` + `GetOrCreate` 直接读 Mongo 旧版本。

## 实施方向与验收

- 驱逐后、`Release` 前（慢路径）调用 `Runtime.WaitEntityProjection`（`dataengine/engine/runtime.go:202-207`）；等待失败则不释放租约并重试。
- 回归（模板测试）：投影暂停时闲置交还不释放租约；投影完成后释放。
