# 2026-09-10 Review 问题

本轮只审查并写文档。上轮 RR-20260909-05、06 没有新增修复记录，Kit/Codegen SHA 未变化，保留未修复状态；本轮不重复开单或声称重新执行了原复现。

## RR-20260910-01（P3）：极短 IdleTTL 推导出零扫描周期

状态：未修复。Core 基线 `8ea815930b4b32bf59cb4a894e50782687046e0d`。

位置：`room/room_manager.go:106–107`（默认值推导），`room/room_manager.go:359–362`（run 创建 ticker）。

触发条件：NewRoomManager 的 Downstream 有效，IdleTTL 为 time.Nanosecond，SweepInterval 留零。IdleTTL 大于零，构造器接受该值；整数除法 IdleTTL/2 得零，min 后仍为零。Start 启动的 run 随后直接调用 time.NewTicker(0)。

期望：构造阶段拒绝不支持的 TTL，或默认扫描周期保证严格为正。实际：构造成功，但保存 SweepInterval=0。独立 overlay 测试在启动前断言失败，输出 `accepted positive IdleTTL but derived SweepInterval=0s`；零周期会导致 ticker panic 是沿当前 run 源码得到的结论，本轮没有主动让后台 goroutine panic 杀死测试进程。

影响仅限极端有效正值且使用默认 SweepInterval；常见毫秒/秒级配置未见此问题，因此定为 P3。建议同时验证派生默认值的约束，测试最小正 TTL、普通 TTL、显式正扫描周期与非法输入。

[复现代码及运行方式](REPRO-2026-09-10.md) · [完整运行记录](../review/REVIEW-2026-09-10.md)。
