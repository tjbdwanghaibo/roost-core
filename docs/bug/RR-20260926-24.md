# RR-20260926-24：合仓迁移表缺少 `room` / `kit/room` → `sync/syncbus/driver` / `kit/syncbus` 映射，`roost project upgrade --consolidate` 后旧工程残留失效 import

日期：2026-09-26。基线：main `aaada47`。等级 P2（升级路径编译失败，需要用户手工修改；新建工程不受影响）。
状态：已复现（v1.16.1 生成器建工程 + HEAD 升级），未修复。来源：上线前发布门禁审查。

## 触发条件

用 v1.16.1 生成器建 `game-demo`（或业务代码手写 import `github.com/tjbdwanghaibo/roost-core/room` / `.../kit/room`），再用 HEAD 执行 `roost project upgrade --consolidate`。

## 预期 vs 实际

预期：迁移表把 ARCH-10 / M-17 删除或改名的包全部改写（`room` → `sync/syncbus/driver`，`kit/room` → `kit/syncbus`），升级后可编译。

实际：`codegen/internal/roost/migration/consolidation_imports.yaml` 第一阶段只有旧仓路径 `roost-kit/room` → `roost-core/syncbus/driver`（`:111-113`），v1.17.0 边界的 `layout` 阶段（`:181-199`）只搬 entitysync / statesync / nettransport / lockstep / syncbus / mirror，没有 v1.16.x 工程实际使用的 `roost-core/room` 与 `roost-core/kit/room`。实跑：`scene.go`、`scene_test.go`、`cmd/loadtest` 残留 `roost-core/room`；
`kit/room` 也没有映射（generated.go 靠重新生成恢复）；业务代码手写的 `roost-core/room` import 直接编译报错。demo 那几个文件 M-13 已要求重新生成，这里只是 import 表不完整。
另：遇到旧的 `syncTopic` 标记时 upgrade 报“已改名”并 exit 1，需用户手改后重跑——属设计行为，但 CHANGELOG 未写明。

## 实施方向（约束）

- 迁移表补齐 `room`、`kit/room` 及其符号改名（`RoomMod` → `SyncBusMod` 等）的映射，或在无法自动映射时给出明确的升级报错与指引。
- CHANGELOG 迁移说明补充 `syncTopic` 需手工改名。

## 验收覆盖点

- v1.16.1 生成的 game-demo 经 `upgrade --consolidate` 后 `go build ./... && go vet ./...` 通过（无 replace 的已发布组合与 source-head 两种口径）。
