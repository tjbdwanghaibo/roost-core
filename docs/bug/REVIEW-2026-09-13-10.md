# 第十轮：LeaveShared 失败后的恢复

## P2 RR-20260913-13：权威退出共享后，普通写重试卡在非法状态迁移

Core `7be357c1cae69b18bca2c6a87ef918f130c60121`，未修复。位置：[ownership.go](../../remoteentity/ownership.go):99，失败后恢复旧 Shared 状态；[batch.go](../../remoteentity/batch.go):268，后续直接 TransitionRemoteOwnership(LocalOwned)。关联 RR-12，但一个是 Enter 错误放行，一个是 Leave 恢复后持续拒绝，分别记录。

触发：LeaveSharedExpected 已在权威成功、Shared=false，随后返回回复丢失错误；live 被恢复成旧 Shared。第一次 PrepareRemoteWriteBatch 依据热 shared marker 取锁，刷新后发现已非 shared，返回 owner transition in progress。第二次已知道权威 local_owned，却从 live Shared 直接迁移到 LocalOwned，被状态机拒绝；第三次普通重试仍相同失败。

实际输出：第一轮 `authority Shared:false / live shared / owner transition in progress`；第二、三轮 `invalid ownership transition: shared -> local_owned`。期望在确认权威后沿合法恢复路径同步 live，或明确保持 Recovering 并提供可收敛的重查；普通重试不应重复卡在非法边上。

证据为可控 marker store + 真实 Manager/状态机，复用第九轮成功后丢回复注入，未执行真实 Redis/跨进程网络。它证明三次普通准入不恢复，不声称所有管理干预、重载或重启都无法恢复；本轮未证明错误写入数据库。

建议 Leave 复用“失效 marker、独立有界确认、合法状态恢复”的协调流程。不能为了消除错误任意开放 Shared→LocalOwned 直达边，绕过原有 draining/fencing 意义。补验收成功后丢回复、权威查询失败、恢复后连续写、发送前失败对照。

第九轮只观察了第一次安全拒绝，本轮推进到重试才确认可用性问题；保留历史观察，不把第九轮 PASS 改写成完整恢复验收。测试见[附录](REPRO-2026-09-13-10.md)。
