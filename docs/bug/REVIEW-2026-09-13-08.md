# 第八轮修复验收附录（无新增确认 RR）

完整基线、结果与限制见[运行记录](../review/REVIEW-2026-09-13-08.md)。下列原复现已运行，不改生产代码。源材料保留历史基线，本轮运行基线为 Core 66f65e2 / Kit ac1a880。

- L2：复用[第七轮源码](REPRO-2026-09-13-07.md)，当前仓库 l2Fake 新增了版本化删除接口，四项通过并不覆盖缺少该能力的第三方 Store。
- WAL：复用[09-12 附录](REPRO-2026-09-12.md)的 wal_test.go，运行全部 TestReview，共五项通过。
- Mail：复用[09-11 第三轮](REPRO-2026-09-11-03.md)的 mail_test.go，运行 TestReviewClaimHistoryRefusalIsAtomic，通过。
- Nest 回复释放：复用[09-11 第四轮](REPRO-2026-09-11-04.md)的 nest_test.go，运行 TestReviewAfterCommitPanicPreservesReplyAndRelease，通过。
- 饱和子进程：复用[09-12](REPRO-2026-09-12.md)的 nest_test.go，按下方新契约适配。
- 真实 Redis：复用[09-13 第三轮](REPRO-2026-09-13-03.md)的 real_test.go，仅运行 TestReviewRealTransfer 前缀两项，按下方适配。

## 饱和子进程的唯一契约调整

新增 errors import，将回复处理中的固定 `ret != 17` 判断替换为：

```go
if mode == "panic" {
    e, ok := ret.(error)
    if !ok || !errors.Is(e, ErrAfterCommitFailed) {
        t.Fatalf("want after commit failure, got %v", ret)
    }
} else if ret != 17 {
    t.Fatal(ret)
}
```

保留队列饱和控制、独立子进程、回复超时、pump.stop 和 normal 场景。原断言运行失败退出 1，日志为已捕获错误；适配后两个子进程均正常退出。

## Transfer 的唯一契约调整

将原 `TransferRemoteOwnership(...); e == nil` 时报 injected failure missing 的条件改为 `before && e == nil`。权威确认转移成功时允许返回 nil，发送前失败仍必须报错。Redis 实际 owner 与旧 owner PrepareRemoteWriteBatch 准入断言保持不变，未删掉核心 fencing 验收。

Redis 使用独立临时配置：bind 127.0.0.1，port 15998，save 空字符串，appendonly no，dir 当前临时目录。ROOST_REVIEW_REDIS=127.0.0.1:15998；测试后 DBSIZE=0，SHUTDOWN NOSAVE，日志确认退出。不要连接用户业务 Redis 或清理其数据。

所有复现采用 Go overlay：Replace 将相应包 review_external_test.go（L2 为 review_boundary_test.go）映射到临时源文件。执行形式为 `go test -race -count=1 -overlay <对应文件.json> ./包 -run '<上述测试名或前缀>' -v -timeout=90s`。本轮实际临时目录为 roost-review-0913h-2f495b5e67634cb991ce6b587a81fa72，但复跑不应依赖该目录仍存在。

## 状态更新

RR-20260913-01/05 的第七轮残余通过；RR-02 同进程播种通过；RR-09 真实 Redis 回复丢失通过；RR-20260912-01/02 真实 WAL 原触发通过；RR-20260911-05 原拒绝原子性通过；RR-20260911-06 回复/释放和饱和子进程通过。保留各 bugfix 原文与协议边界，不将这次验收泛化为所有分布式场景无问题。
