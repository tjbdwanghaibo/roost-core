# 复现附录：2026-09-22（真实环境实施 CARRYOVER B 类）

基线 Core `00277bd`。所有输出为原始日志摘录，机器人与服务端时间同机。

## 0. 环境

```bash
export PATH=/opt/homebrew/bin:$PATH
kit/scripts/integration/dataengine-env.sh up      # Mongo rs roost-it 27117-27119, NATS 14222-14224, Redis 16379, toxiproxy 18474
GOWORK=off go run ./codegen/cmd/roost project new rv21 -module example.com/rv21 -out <scratch>/rv21 \
  -mods configdata,mongo,nats,dataengine,nest,saga,redis,etcd -template game-demo
```

生成工程的 `configs/service/config.*.yaml` 改成：Mongo `mongodb://127.0.0.1:27117,27118,27119/?replicaSet=roost-it`、
NATS `nats://127.0.0.1:14222,14223,14224`、Redis `127.0.0.1:16379`、`nats.prefix: rv22`、
`activity.game_sids: [1000, 1001]`，并把 `dataengine.effects.max_bytes` 与 `saga.stream_max_bytes` 从 8 GiB 降到 512 MiB
（itest 节点 `max_file_store` 是 4 GiB，否则 `ensure effect stream: … no suitable peers for placement, insufficient storage`）。

```bash
SID=1000 sh deploy/dev/run.sh start                # 九个服务 + game 1000，并 upsert-server 1000
SECOND_SID=1001 sh deploy/dev/second-game.sh start # game 1001（ops 9110，客户端 7001）
go run ./cmd/accountctl -redis 127.0.0.1:16379 -prefix roost:rv21:account upsert-server -sid 1001
```

两个 game 进程启动后 `.dev/game.log` / `.dev/game2.log` 各 0 条 ERROR。

## 1. B1：故障矩阵（`dataengine-env.sh test`）

```text
[roost-it] toxiproxy ready: http://127.0.0.1:18474, nats proxies 127.0.0.1:24222-24224
ok   github.com/tjbdwanghaibo/roost-core/kit/dataengine  61.583s
?    github.com/tjbdwanghaibo/roost-core/kit/saga         [no test files]
?    github.com/tjbdwanghaibo/roost-core/kit/remoteentity [no test files]
ok   github.com/tjbdwanghaibo/roost-core/kit/nats         1.808s
[roost-it] roost-core checkout not found at /Users/whb/roost/roost-core/kit/../roost-core; core redis fault suites NOT run
kit-exit=0
```

core 侧手动补跑（脚本本该做的）：

```text
ok   github.com/tjbdwanghaibo/roost-core/redis          0.487s
ok   github.com/tjbdwanghaibo/roost-core/redis/driver   3.206s
ok   github.com/tjbdwanghaibo/roost-core/etcd/driver    3.176s
ok   github.com/tjbdwanghaibo/roost-core/mongo/driver   2.594s
```

`kit/dataengine` + `kit/nats` 的格子（`grep '^func Test' $(grep -rl 'go:build integration' kit/dataengine kit/nats)`）：

```text
TestRealIntegrationDeadlineIsBounded                    TestRealMultiDocumentReceiptAndOutboxAreAtomic
TestRealJetStreamLeaderFailoverPreservesDedupAndOrder   TestRealNATSOutageDoesNotBlockProjectionAndRecoversOutbox
TestRealLoadAndMigrationRestoresTrackerVersion          TestRealNatsModProvideRefusesBareRegistriesAndReliableWithoutRedis
TestRealMixedProjectionSegmentsPreserveOrder            TestRealPatchConflictFencesWithoutFullFallback
TestRealMongoMixedRatioWALReplayAckThroughput           TestRealProjectionOnlyMongoAckFailureRestartPreservesSameEntityOrder
TestRealMongoPrimaryFailoverContinuesProjection         TestRealSagaReceiptTransactionThroughput
TestRealMultiDocumentFailureRollsBackEarlierMutation    TestToxicJetStreamRPCCallHonoursItsDeadlineWhileHalfOpen
TestToxicNATSConnectionResetDeliversTheEffectExactlyOnce TestToxicNATSHalfOpenAckLossIsBoundedAndDeliversExactlyOnce
TestToxicNATSLatencyKeepsTheCommitOnTheDurablePath
```

全仓带 integration tag 的测试文件按包计数（RR-20260922-02 的"应该跑什么"）：

```text
4 kit/dataengine   2 kit/service/integration   2 kit/nats   1 service/mail
1 redis/driver     1 mongo/driver              1 kit/service/rank   1 etcd/driver
```

## 2. RR-20260922-03：`service/mail` 的 Redis 用例

```bash
REDIS_ADDR=127.0.0.1:16379 go test -tags integration ./service/mail/ -run Integration -count=1 -v
```

```text
--- PASS: TestIntegrationBatchReadReturnsOneValuePerKey (0.00s)
--- PASS: TestIntegrationAnEmptyBatchIsNotSentToRedis (0.00s)
--- PASS: TestIntegrationConcurrentCreatesOfOneIDProduceOneEnvelope (0.00s)
--- PASS: TestIntegrationAnEnvelopeExpiresOnItsOwnDeadline (0.40s)
--- PASS: TestIntegrationTheServiceRunsEndToEndOnRedis (0.00s)
ok   github.com/tjbdwanghaibo/roost-core/service/mail  1.427s
```

`grep -n "tags integration" .github/workflows/*.yml` 只有 `ci.yml:36`（vet）、`:106`（`./kit/service/...`）、`:120`（race 三个 kit 包）。

## 3. B3 / B5：两进程 + 机器人

### wave 1（两个进程刚起；16 → 7000，8 → 7001，同时）

```text
# 7000
scenario failed robot=1  player=900001 err="robot action scene_expect: scene_expect: after 10s the client knows 16 subjects (want 2) and 56 frames; \"pos_x\" never arrived"
scenario failed robot=9  player=900009 ... 16 subjects ... 63 frames; \"pos_x\" never arrived"
scenario failed robot=4  player=900004 ... 16 subjects ... 59 frames; \"pos_x\" never arrived"
scenario failed robot=15 player=900015 err="robot action poll_match: ticket still waiting"   # 3 人掉队后人数变奇数，配不上对
run done state=failed stop_reason=threshold started=16 success=12 failure=4 elapsed=26.9s   quantiles_ms.p95=15701
# 7001
run done state=failed stop_reason=threshold started=8 success=8 failure=0 elapsed=9.4s      quantiles_ms.p95=16384（只超 p95 阈）
```

服务端两进程 WARN/ERROR 汇总：只有 `slow dispatch`（`handlerFoundGuild` 0.8–1.4s）。**没有 `scene: subscribe`。**

### probe A（同一 1000 进程已 warm，16 人单独）

```text
run done state=finished stop_reason=completed started=16 success=16 failure=0 elapsed=10.9s   p95=15872
```

### probe B（warm 1000 ×16 + 1001 ×8 同时）

```text
7000: started=16 success=16 failure=0 elapsed=13.1s
7001: started=8  success=8  failure=0 elapsed=9.0s（p95 阈）
```

### probe C（`run.sh stop game; run.sh start game` 冷起 1000 ×16；25 秒后 1001 ×8）

```text
# 7000（冷进程）
7 × err="robot action found_guild: response code 100020: guild is busy, try again"
9 × err="robot action gift_status: gift saga still waiting (step 0)"
run done state=failed started=16 success=0 failure=16 elapsed=14.1s
# 7001（warm，但 1000 正在承压）
robot=1 ... knows 8 subjects (want 2) and 23 frames; \"equipment\" never arrived
robot=2 ... knows 8 subjects (want 2) and 24 frames; \"pos_x\" never arrived
robot=6 ... knows 8 subjects (want 2) and 25 frames; \"equipment\" never arrived
```

重启后 1000 前 6 秒的日志（节选）：

```text
10:44:42 INFO activity: lease acquired
10:44:44 INFO gift saga: ran a step handed over by another process player_id=100066
10:44:44 INFO gift saga: ran a step handed over by another process player_id=100074
…（共 20+ 条，玩家 100066–100079 都是 1001 在 probe B 里的机器人）
10:44:48 WARN slow dispatch trace
```

零 ERROR。

### 所有权日志（两进程合并，去重计数）

```text
10:35:06–:09  gift saga: step left to the player's owner  player_id=100001..100008 owner_sid=1001   （1000 让位，20 条）
10:35:39  idle lease handed back ×8   (100001–100008，1001 侧)
10:35:46  idle lease handed back ×8   (100009,11,13,14,15,16,22,23)
10:35:56  idle lease handed back ×7
10:36:46  idle lease handed back ×1   (100012)
10:43:06  ×4   10:43:16 ×8   10:43:26 ×4      （probe B 之后）
```

`fenced` / `lease lost` / `renewal failed` / `version conflict` / `owned by another process`：**0**。

### 机器人侧排除项（RR-20260922-01 §"为什么不是 RR-06"）

- `cmd/loadtest/main.go` consume：`known[key] = value` 逐字段合并。
- `robot/session/session.go:285`：`if byMsg := s.handlers[packet.MsgID]; len(byMsg) > 0 { … }`——没有 handler 就没有投递，无缓冲。
- `demo.yaml.tmpl`：`enter_game`（:18）在 `scene_watch`（:24）之前。
- `map_component.go.tmpl:105` `if from == to { return nil }`；出生点 (500,500)，`move` 到 (501,500)；`:117-119` 写 `pos_x/pos_y` 并 `PublishSyncDirty`。

**本轮没做的**：没有把某一个缺字段的客户端与服务端某一帧对上（需要 pair 级 / 帧级日志，服务端目前不打）；
没有做"`scene_watch` 前置"与"`scene_expect` 前再 move 一次"两个判别实验（留给修的那一半，见 REVIEW）。
B2（性能对比）、B4（活动跨 300 秒边界）未做。

探针二进制（只改了失败信息、加了 own_updates 计数）只存在于 scratch 工程，仓库未改。


## 4. RR-20260922-01 追加：wire 级探针（2026-09-22 第二次会话）

探针改动（临时，仓库未改）：`sceneWatcher` 记住 `ownPlayer`，`consume` 里对每一条**自己 subject** 的
`SubjectSyncUpdate` 打一行 `WIRE player=<id> full=.. ver=.. base=.. mask=0x.. reason=.. datagram=.. fields=[..]`。

干净世界（`dataengine-env.sh reset && up`，删 `data/wal`、`.dev`），16 机器人 → 7000。原始摘录：

```text
# 4 个失败客户端 == 4 个从未收到 pos_x 的客户端（按真实 id）
player=100015 own_updates=7 last: ver=8 base=7 mask=0x712  fields=[_id attr_base attr_final equipment items level]
player=100017 own_updates=7 last: ver=8 base=7 mask=0x4712 fields=[_id attr_base attr_final equipment guild_id items level]
player=100021 own_updates=8 last: ver=9 base=8 mask=0x4000 fields=[_id guild_id]
player=100022 own_updates=7 last: ver=8 base=7 mask=0x4712 fields=[_id attr_base attr_final equipment guild_id items level]
# 邻居（收到了 pos_x）
player=100016 ... ver=9 base=8 mask=0x30 fields=[_id items pos_x]
player=100010 ... ver=9 base=8 mask=0x4030 fields=[_id guild_id items pos_x]

# 汇总：恰好 4 个真实 player 从未出现过 pos_x，与 4 个 scene_expect 失败一一对应
total_without_pos_x=4  →  100015 100017 100021 100022
```

对齐说明：`scenario failed … player=9000xx` 里的 `player=` 是机器人**临时** id；`WIRE` 行用的是账号服务分配的
**真实** id（`1000xx`）。两者一一对应（16 vs 16），失败集合 == 无-pos_x 集合。

判别：
- **不是慢**：`pos_x` 位在整轮里对这 4 个客户端从未出现；100021 甚至收到了后一个版本（ver 9 = guild_id），
  说明它的自身流没有停，只是那一次 move 的位没进任何一版。
- **通道活**：这些客户端总帧数 56–61（别的 subject 一直在到）。
- **零** `scene: subscribe` WARN（服务端两进程日志）。

**本轮没做的**：core 层的红（并发 `MarkDirty` vs 在飞 `Prepare`/`Commit` 造"版本进、位丢"）、定到行、修。
探针二进制 `bin/loadtest-wire` 只在 scratch 工程，仓库未改。

## 5. 顺带撞出的两条（已登记 WANTED，本轮未修）

- **W-2026-09-22-01**：`go test -tags=integration ./redis/driver -run TestToxicRedisDroppedAcquireReplyIsReconciledNotRetried -count=3`
  → 1 绿 2 红（`:151` `Acquire … ok=false err=<nil>` 与 `:161` `key still held after reconciliation: exists=1`）。
  U-0276 把故障矩阵修好、toxiproxy 真的在位之后第一次跑到它。
- **W-2026-09-22-02**：v1.16.1 的 game 进程 `run.sh stop` 正常停止后重启，`mod dataengine start` 报
  `startup projection recovery: … remote projection: mongo: duplicate key`，每次重启都一样。WAL 目录已存证
  （`<scratch>/evidence/wal-dataengine-1000/`：段最后写 10:50，ack-*.chk 停在 10:44）；Mongo 里该事务
  `_dataengine_transactions` / `_receipts` 都查不到。把它移开后进程正常启动。
