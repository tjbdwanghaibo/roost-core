# 第六轮独立复现

将下方代码存入临时文件，用 Go overlay Replace 映射到当前 Core statesync/review_baseline_test.go（键值均用本机绝对路径）。同包复用已有 mustSnapshot。临时 go.work 指向三仓，不修改生产依赖。

执行 `go test -race -count=1 -overlay <json> ./statesync -run TestReviewSameTickProjection -v`。2c89f3d 与 2c5469e 均为 next_tick_control 通过、same_tick_lost_revision 失败：client objects=1 want=2, delta changes=0 base=1。

```go
package statesync
import "testing"
func TestReviewSameTickProjection(t *testing.T) {
 for _, resend := range []bool{false,true} { name := "next_tick_control"; if resend {name="same_tick_lost_revision"}; t.Run(name,func(t *testing.T){
 show:=false
 r:=NewReplicator(ReplicatorConfig{Projector:ProjectorFunc(func(_ SessionInfo,s Snapshot)(Snapshot,error){if !show {s.Objects=s.Objects[:1]};return s,nil})}); defer r.Close()
 check:=func(err error){t.Helper();if err!=nil {t.Fatal(err)}}
 check(r.RegisterSession(SessionInfo{ID:10}))
 s:=mustSnapshot(t,1,[]ObjectState{{Ref:ObjectRef{ID:1,Generation:1},Archetype:1},{Ref:ObjectRef{ID:2,Generation:1},Archetype:99}})
 check(r.Publish(s)); p,err:=r.PrepareLatest(10);check(err);check(p.Commit())
 client,err:=ApplyDelta(nil,p.Frame,DefaultLimits());check(err)
 show=true
 if resend {p2,e:=r.PrepareLatest(10);check(e);check(p2.Commit()) /* accepted for delivery, then lost */}
 check(r.Acknowledge(10,1));check(r.Publish(mustSnapshot(t,2,s.Objects)))
 frame,_,err:=r.BuildLatest(10);check(err)
 client,err=ApplyDelta(&client,frame,DefaultLimits());check(err)
 if len(client.Objects)!=2 {t.Fatalf("client objects=%d want=2, delta changes=%d base=%d",len(client.Objects),len(frame.Objects),frame.BaseTick)}
 }) }
}


```

U-0199 独立复核：分别新建单玩家 Sequencer，提交 2^31、4e9、MaxUint32，断言 ErrFrameTooEarly 且 accepted[1] 无分配。当前实现通过。用 overlay 替换 sequencer.go 为 git show 2c89f3d:lockstep/sequencer.go，并把新增 submit_validation_promises_test.go 映射到只有 package lockstep 的文件（避免旧实现缺 helper），相同独立测试三次报 rejected but allocated 129 slots。仅测试拒绝分配副作用，没有运行 32 位 panic。
