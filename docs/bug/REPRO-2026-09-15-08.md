# 09-15 第八轮：29 个新场景复现

Core 6cef240。最终去重 29 叶子，23 通过、6 失败确认 RR-20260915-08/09。用户声明没有修复，代码没有包含旧 RR 的专项复现。将以下代码保存到临时目录，并用 overlay Replace 将 `D:\\whb_s\\cube-core\\syncstream\\review_handover_test.go` 映射到保存路径。

设置外部 go.work use 当前 core、kit、codegen 和 GOWORK，不修改仓库 go.mod。

```powershell
go test -race -count=1 -overlay C:/your-temp/overlay.json ./syncstream -run TestReview -v
```

真实 journal 文件均在 t.TempDir 中；Recover 用 channel 固定捕获与追加顺序；checkpoint 对照为两 writer 和一 checkpointer，同一 History 对象。恢复快照比较排除 LastActivityUnixNano，因为现有 WAL 不记录该字段。重复流用 MaxStreams=2 避免容量守卫遮蔽重复校验。

```go
package syncstream
import("errors";"reflect";"sync";"testing")
type reviewStore struct{s HistorySnapshot;err error}
func(s reviewStore)Load()(HistorySnapshot,error){return s.s,s.err}
func(s reviewStore)Save(HistorySnapshot)error{return s.err}
func TestReviewImportDurability(t *testing.T){for _,mode:=range []string{"import_same_epoch","restore_same_epoch","import_new_epoch","restore_new_epoch","checkpoint_same_epoch","checkpoint_new_epoch"}{t.Run(mode,func(t *testing.T){dir:=t.TempDir();j,e:=NewFileHistoryJournal(dir,7);if e!=nil{t.Fatal(e)};defer j.Close();h,e:=NewHistoryWithJournal(HistoryOptions{},j);if e!=nil{t.Fatal(e)};s:=Stream{Topic:"state"};if _,e=h.Append(Packet{Stream:s,Full:true,Payload:[]byte("old")});e!=nil{t.Fatal(e)};epoch:=uint64(7);if mode=="import_new_epoch"||mode=="restore_new_epoch"||mode=="checkpoint_new_epoch"{epoch=8};replacement:=NewHistory(HistoryOptions{Epoch:epoch});replacement.Append(Packet{Stream:s,Full:true,Payload:[]byte("replacement")});snapshot:=replacement.Export();if mode=="restore_same_epoch"||mode=="restore_new_epoch"{e=h.Restore(reviewStore{s:snapshot})}else{e=h.Import(snapshot)};if e!=nil{t.Fatal(e)};if mode=="checkpoint_same_epoch"||mode=="checkpoint_new_epoch"{if e=h.Checkpoint();e!=nil{t.Fatal(e)}};if _,e=h.Append(Packet{Stream:s,Payload:[]byte("next")});e!=nil{t.Fatal(e)};j.Close();j2,e:=NewFileHistoryJournal(dir,99);if e!=nil{t.Fatal(e)};defer j2.Close();restored,e:=NewHistoryWithJournal(HistoryOptions{},j2);if e!=nil{t.Fatalf("successful replacement and append cannot restart: %v",e)};snap:=restored.Export();if string(snap.Streams[0].Packets[0].Payload)!="replacement"{t.Fatalf("replacement reverted on restart: %q",snap.Streams[0].Packets[0].Payload)}})}}
type reviewProvider func(ResyncRequest)(Packet,error)
func(f reviewProvider)Snapshot(r ResyncRequest)(Packet,error){return f(r)}
func TestReviewRecoverInterleaving(t *testing.T){for _,mode:=range []string{"no_change","same_stream_append","epoch_rotation","provider_error","replay_skips_provider"}{t.Run(mode,func(t *testing.T){h:=NewHistory(HistoryOptions{Epoch:7});s:=Stream{Topic:"state"};if mode=="replay_skips_provider"{h.Append(Packet{Stream:s,Full:true,Payload:[]byte("current")});r,e:=h.Recover(ResyncRequest{Stream:s,Epoch:7},reviewProvider(func(ResyncRequest)(Packet,error){t.Fatal("provider called for replay");return Packet{},nil}));if e!=nil||len(r.Packets)!=1{t.Fatal(e)};return};entered,release:=make(chan struct{}),make(chan struct{});type outcome struct{r ResyncResult;e error};done:=make(chan outcome,1);go func(){r,e:=h.Recover(ResyncRequest{Stream:s,Epoch:7},reviewProvider(func(ResyncRequest)(Packet,error){captured:=Packet{Payload:[]byte("old")};close(entered);<-release;if mode=="provider_error"{return Packet{},errors.New("capture failed")};return captured,nil}));done<-outcome{r,e}}();<-entered;if mode=="same_stream_append"{h.Append(Packet{Stream:s,Full:true,Payload:[]byte("new")})};if mode=="epoch_rotation"{h.RotateEpoch(8);h.Append(Packet{Stream:s,Full:true,Payload:[]byte("new")})};close(release);got:=<-done;if mode=="provider_error"{if got.e==nil||h.Metrics().Streams!=0{t.Fatal("provider failure mutated history")};return};if got.e!=nil{if mode=="no_change"{t.Fatal(got.e)};return};p:=got.r.Packets[0];if mode!="no_change"&&string(p.Payload)=="old"{t.Fatalf("stale capture appended after newer state: epoch=%d seq=%d payload=%q",p.Epoch,p.Sequence,p.Payload)}})}}
func TestReviewImportValidationMatrix(t *testing.T){for _,mode:=range []string{"duplicate_stream","wrong_packet_epoch","wrong_packet_observer","zero_sequence","full_with_base","broken_chain","delta_schema_change","latest_mismatch","oversized_payload","max_streams"}{t.Run(mode,func(t *testing.T){h:=NewHistory(HistoryOptions{Epoch:7,MaxPayloadBytes:8,MaxStreams:1});s:=Stream{Topic:"state"};h.Append(Packet{Stream:s,Full:true,Payload:[]byte("x")});h.Append(Packet{Stream:s,Payload:[]byte("y")});if mode=="duplicate_stream"{h.options.MaxStreams=2};before:=h.Export();bad:=h.Export();p:=&bad.Streams[0].Packets[0];switch mode{case "duplicate_stream":bad.Streams=append(bad.Streams,bad.Streams[0]);case "wrong_packet_epoch":p.Epoch=8;case "wrong_packet_observer":p.Observer.ID=3;case "zero_sequence":p.Sequence=0;case "full_with_base":p.BaseSequence=4;case "broken_chain":bad.Streams[0].Packets[1].BaseSequence=9;case "delta_schema_change":bad.Streams[0].Packets[1].SchemaVersion=2;case "latest_mismatch":bad.Streams[0].Latest=3;case "oversized_payload":p.Payload=make([]byte,9);case "max_streams":extra:=bad.Streams[0];extra.Stream.Topic="other";bad.Streams=append(bad.Streams,extra)};if e:=h.Import(bad);e==nil||!reflect.DeepEqual(before,h.Export()){t.Fatalf("invalid replacement modified history: %v",e)}})}}
func TestReviewCheckpointConcurrency(t *testing.T){for _,mode:=range []string{"same_stream","different_streams","checkpoint_each"}{t.Run(mode,func(t *testing.T){dir:=t.TempDir();j,e:=NewFileHistoryJournal(dir,7);if e!=nil{t.Fatal(e)};defer j.Close();h,e:=NewHistoryWithJournal(HistoryOptions{MaxPacketsPerStream:128},j);if e!=nil{t.Fatal(e)};errs:=make(chan error,40);var wg sync.WaitGroup;for worker:=0;worker<2;worker++{wg.Add(1);go func(id int){defer wg.Done();for i:=0;i<8;i++{s:=Stream{Topic:"state"};if mode=="different_streams"{s.Key=int64(id)};_,e:=h.Append(Packet{Stream:s,Full:true});if e!=nil{errs<-e};if mode=="checkpoint_each"{if e=h.Checkpoint();e!=nil{errs<-e}}}}(worker)};wg.Add(1);go func(){defer wg.Done();for i:=0;i<4;i++{if e:=h.Checkpoint();e!=nil{errs<-e}}}();wg.Wait();close(errs);for e:=range errs{t.Fatal(e)};expected:=h.Export();j.Close();j2,e:=NewFileHistoryJournal(dir,99);if e!=nil{t.Fatal(e)};defer j2.Close();r,e:=NewHistoryWithJournal(HistoryOptions{MaxPacketsPerStream:128},j2);if e!=nil{t.Fatal(e)};actual:=r.Export();for i:=range expected.Streams{expected.Streams[i].LastActivityUnixNano=0};for i:=range actual.Streams{actual.Streams[i].LastActivityUnixNano=0};if !reflect.DeepEqual(expected,actual){t.Fatalf("durable state mismatch expected=%+v got=%+v",expected,actual)}})}}
func TestReviewRestoreStoreFailure(t *testing.T){h:=NewHistory(HistoryOptions{Epoch:7});h.Append(Packet{Stream:Stream{Topic:"x"},Full:true});before:=h.Export();if e:=h.Restore(reviewStore{err:errors.New("load failed")});e==nil||!reflect.DeepEqual(before,h.Export()){t.Fatal("failed restore mutated",e)}}

func TestReviewValidReplacementBoundaries(t *testing.T){for _,mode:=range []string{"retention_trim","pruned_stream","payload_detached","restore_success"}{t.Run(mode,func(t *testing.T){source:=NewHistory(HistoryOptions{Epoch:8,PruneAcknowledged:true});s:=Stream{Topic:"state"};for i:=0;i<3;i++{source.Append(Packet{Stream:s,Full:true,Payload:[]byte("x")})};if mode=="pruned_stream"{source.AcknowledgeEpoch(Observer{},s,8,3)};snapshot:=source.Export();target:=NewHistory(HistoryOptions{Epoch:7,MaxPacketsPerStream:2});var e error;if mode=="restore_success"{e=target.Restore(reviewStore{s:snapshot})}else{e=target.Import(snapshot)};if e!=nil{t.Fatal(e)};status:=target.Status(Observer{},s);if status.LatestSequence!=3||target.Epoch()!=8{t.Fatal(status)};switch mode{case "retention_trim":if status.Retained!=2||status.Dropped!=1{t.Fatal(status)};case "pruned_stream":if status.Retained!=0||status.AckedSequence!=3{t.Fatal(status)};case "payload_detached":snapshot.Streams[0].Packets[2].Payload[0]='z';if string(target.Export().Streams[0].Packets[1].Payload)!="x"{t.Fatal("alias")}}})}}
```
