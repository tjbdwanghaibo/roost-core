# 2026-09-17 第三轮：独立复现源码

基线与证据限制见 [运行记录](../review/REVIEW-2026-09-17-03.md)，问题见 [交接](REVIEW-2026-09-17-03.md)。以下代码直接归档本轮实际运行的临时文件，未修改仓库源码。预期当前基线得到行为失败，不要把这些失败误当环境故障。

## 准备外部模块与生成物

使用 Go 1.27.0，三仓检出交接记录的 SHA；下面目录是本轮实际路径，重跑可换成新的空临时目录并同步修改路径。不要覆盖已有用户工程。

1. 在 `D:/whb_s/review-temp-0917c` 建 `go.mod`（见下），先建只含三仓和此外部模块的 go.work；demo 创建后才把 demo 路径加入 use。
2. `dao` 子目录复制 Codegen `internal/dao/testdata/golden/*.go`，不要复制 `*_redis_dao.go`（其定义夹具不在生成物中）。复制 `internal/dao/testdata/runtime/roundtrip_test.go`，仅移除 `//go:build daoruntime` 行。将下面 DAO 测试保存为 `dao/review_dirty_test.go`。
3. 在 Codegen 仓运行 `go run ./cmd/roost project new reviewdemo -module example.com/reviewdemo -out D:/whb_s/review-temp-0917c/demo -template game-demo`。本轮文件创建成功，末尾依赖下载遇 goproxy.cn INTERNAL_ERROR；未视为产品问题。确认创建完成后将 demo 加入 go.work，用本地三仓源码继续。设置 GOWORK 后运行 `go run ./cmd/roost generate --root D:/whb_s/review-temp-0917c/demo`。将下面两份 demo 测试保存到对应生成包，不能放回模板仓库。
4. attribute 子目录保存下方 profile，Codegen 中运行 `go run ./cmd/attribute -dir D:/whb_s/review-temp-0917c/attribute`。
5. Saga 文件保存在外部模块根，使用下方 overlay 映射，不直接写 Core/saga。

最终 go.work 的 use 使用本机 Windows 路径。本轮采用源码覆盖，而非发布依赖验收；不运行 go work sync，不改任何三仓 go.mod。

## go.mod

```go
module example.com/reviewfeature

go 1.27.0

require github.com/tjbdwanghaibo/roost-core v1.15.6


```

## go.work（demo 创建后的版本）

```go
go 1.27.0
use (
 D:\whb_s\cube-core
 D:\whb_s\cube-kit
 D:\whb_s\cube-codegen
 D:\whb_s\review-temp-0917c
 D:\whb_s\review-temp-0917c\demo
)


```

## DAO：dao/review_dirty_test.go

```go
package testdata

import (
 "testing"
 "go.mongodb.org/mongo-driver/v2/bson"
)

func TestReviewNestedNotify(t *testing.T) {
 for _,mode:=range []string{"raw_restore","setter","bson_restore","parent_control"} {
  t.Run(mode,func(t *testing.T){
   equip:=&EquipInfo{}
   gem:=&GemInfo{level:1}
   switch mode {
   case "setter": equip.SetGems(map[int32]*GemInfo{2:gem})
   case "bson_restore":
    src:=&EquipInfo{};src.setGemsRawMap(map[int32]*GemInfo{2:gem})
    raw,err:=bson.Marshal(src);if err!=nil{t.Fatal(err)}
    if err:=bson.Unmarshal(raw,equip);err!=nil{t.Fatal(err)}
   default:equip.setGemsRawMap(map[int32]*GemInfo{2:gem})
   }
   calls:=0;equip.SetNotify(func(){calls++})
   if mode=="parent_control" {equip.SetLevel(2)} else {
    nested,ok:=equip.GetGems(2);if !ok{t.Fatal("missing")};nested.SetLevel(3)
   }
   if calls!=1 {t.Fatalf("nested change did not reach parent: notifications=%d",calls)}
  })
 }
}

```

## attribute：attribute/profile.go

```go
package attribute
//roost:attribute index=1 max=4
type PlayerProfile struct { HP int64; dirtyMask uint64 }


```

## Saga：saga_review_test.go

```go
package saga

import (
 "context"
 "strings"
 "sync"
 "testing"
 "time"
 "github.com/tjbdwanghaibo/roost-core/mongo/mongotest"
 fnats "github.com/tjbdwanghaibo/roost-core/nats"
)
type reviewSub struct {once sync.Once; closed chan struct{}}
func(s *reviewSub) Stop(){s.once.Do(func(){close(s.closed)})}
func(s *reviewSub) Drain(){s.Stop()}
func(s *reviewSub) Closed()<-chan struct{}{return s.closed}
type reviewJS struct{configs []fnats.JetStreamConsumerConfig}
func(*reviewJS) EnsureStream(context.Context,fnats.JetStreamConfig)error{return nil}
func(*reviewJS) Publish(context.Context,string,[]byte,fnats.JetStreamPublishOptions)(fnats.JetStreamPublishAck,error){return fnats.JetStreamPublishAck{},nil}
func(j *reviewJS) Subscribe(_ context.Context,c fnats.JetStreamConsumerConfig,_ fnats.JetStreamHandler)(fnats.IJetStreamSubscription,error){j.configs=append(j.configs,c);return &reviewSub{closed:make(chan struct{})},nil}
func TestReviewAssemblyEffectSubscriptions(t *testing.T){
 j:=&reviewJS{}
 cfg:=AssemblyConfig{Store:MongoStoreOptions{Database:"review"},Engine:DefaultOptions(),Prefix:"roost.saga",Stream:fnats.JetStreamConfig{Name:"ROOST_SAGA",Subjects:[]string{"roost.saga.>"}},Completions:CompletionConsumerConfig{Stream:"ROOST_SAGA",Durable:"review-results",SubjectPrefix:"roost.saga"},Starts:NestStartConsumerConfig{Stream:"ROOST_EFFECTS",Durable:"review-starts",EffectPrefix:"roost.effect"}}
 cfg.Engine.Owner="review"
 a,err:=Assemble(mongotest.NewClient(),j,cfg);if err!=nil{t.Fatal(err)}
 if err:=a.Start(context.Background());err!=nil{t.Fatal(err)}
 defer func(){ctx,cancel:=context.WithTimeout(context.Background(),time.Second);defer cancel();if err:=a.Stop(ctx);err!=nil{t.Error(err)}}()
 effect,err:=NewCompletionEffect(Completion{SagaID:"s1",CommandID:"cmd1",IdempotencyKey:"key1",Success:true});if err!=nil{t.Fatal(err)}
 for _,tc:=range []struct{name,stream,subject string}{
  {"direct_control","ROOST_SAGA","roost.saga.result.s1"},
  {"start_control","ROOST_EFFECTS","roost.effect.saga.start"},
  {"native_completion","ROOST_EFFECTS","roost.effect."+effect.Topic},
 } {t.Run(tc.name,func(t *testing.T){
  for _,c:=range j.configs{if c.Stream==tc.stream&&(c.FilterSubject==tc.subject||(strings.HasSuffix(c.FilterSubject,">" )&&strings.HasPrefix(tc.subject,strings.TrimSuffix(c.FilterSubject,">")))){return}}
  t.Fatalf("no consumer covers %s/%s; subscriptions=%+v",tc.stream,tc.subject,j.configs)
 })}
}

```

## Saga：saga-overlay.json

```json
{
  "Replace": {
    "D:/whb_s/cube-core/saga/review_effect_test.go": "D:/whb_s/review-temp-0917c/saga_review_test.go"
  }
}

```

## 副本：demo/game/controllers/player/review_finish_test.go

```go
package player

import (
 "context"
 "testing"
 "time"
 agent "example.com/reviewdemo/game/player_agent"
 playerentity "example.com/reviewdemo/game/entities/player"
 "example.com/reviewdemo/protocol/pb"
 "github.com/tjbdwanghaibo/roost-core/nest"
 session "github.com/tjbdwanghaibo/roost-core/service/session"
 "github.com/tjbdwanghaibo/roost-core/versionstore"
)
type reviewRewardClient struct {nest.Client; grants int}
func(n *reviewRewardClient) RequestMulti(context.Context,nest.HandlerName,[]int64,nest.Params,...nest.SendOpt)(any,error){n.grants++;return int32(1),nil}
func TestReviewFinishDungeonRewards(t *testing.T){
 playerentity.RegisterEntity()
 for _,mode:=range []string{"single_control","replay","failed_then_success","expired_retry","wrong_owner_control"}{t.Run(mode,func(t *testing.T){
  now:=time.Unix(1000,0)
  s,err:=session.New(session.Config{Runs:versionstore.NewMemoryStore[string,session.Run](),Claims:versionstore.NewMemoryStore[int64,session.Claim](),Requests:versionstore.NewMemoryStore[string,session.LedgerEntry](),Release:session.ReleaserFunc(func(context.Context,session.Run,session.Resource)error{return nil}),Now:func()time.Time{return now},TTL:time.Minute});if err!=nil{t.Fatal(err)}
  run,err:=s.Enter(context.Background(),42,session.EnterRequest{Kind:"dungeon",RequestID:"review"});if err!=nil{t.Fatal(err)}
  n:=&reviewRewardClient{};c:=&Controller{sessions:s,nestClient:n,worldID:1}
  ctx:=&agent.Context{PlayerID:42}
  call:=func(success bool){t.Helper();r,e:=c.HandleFinishDungeon(ctx,&pb.FinishDungeonRequest{RunID:run.ID,Success:success});if e!=nil{t.Fatal(e)};t.Logf("response=%+v",r)}
  want:=1
  switch mode {
  case "replay":call(true);call(true)
  case "failed_then_success":call(false);call(true);want=0
  case "expired_retry":now=now.Add(2*time.Minute);call(true);call(true);want=0
  case "wrong_owner_control":ctx.PlayerID=43;call(true);want=0
  default:call(true)
  }
  if n.grants!=want{t.Fatalf("reward commands=%d want=%d",n.grants,want)}
 })}
}

```

## Battle：demo/internal/service/game/review_battle_test.go

```go
package Game

import (
 "testing"
 "time"
 "example.com/reviewdemo/game/battle"
 "github.com/tjbdwanghaibo/roost-core/lockstep"
)
func TestReviewBattleStartGrace(t *testing.T){
 for _,input:=range []bool{false,true}{name:="no_input";if input{name="input_control"};t.Run(name,func(t *testing.T){
  room,err:=lockstep.NewRoom(lockstep.RoomConfig{Sequencer:lockstep.SequencerConfig{Players:battle.Seats(2),SubmitWindow:2,MaxInputBytes:8},Datagrams:battleSender{}});if err!=nil{t.Fatal(err)}
  if err:=room.Attach(1,1);err!=nil{t.Fatal(err)}
  managed:=&battleRoom{id:"review",commands:make(chan battleCommand,2),stop:make(chan struct{}),done:make(chan struct{})}
  manager:=&battleManager{rooms:map[string]*battleRoom{"review":managed}}
  manager.wg.Add(1);go manager.run(managed,room)
  if input{reply:=make(chan error,1);managed.commands<-battleCommand{seat:1,frame:1,payload:battle.Encode(battle.Input{DX:1}),reply:reply};if err:=<-reply;err!=nil{t.Fatal(err)}}
  time.Sleep(battleStartGrace+200*time.Millisecond)
  close(managed.stop);<-managed.done;manager.wg.Wait()
  if room.NextFrame()<=1{t.Fatalf("grace elapsed but no frames cut: next=%d",room.NextFrame())}
 })}
}

```

## 运行命令（PowerShell）

```powershell
$env:GOWORK='D:/whb_s/review-temp-0917c/go.work'
Set-Location D:/whb_s/review-temp-0917c
go test -count=1 -v ./dao
go test -count=1 ./attribute
Set-Location D:/whb_s/cube-core
go test -count=1 -v -overlay D:/whb_s/review-temp-0917c/saga-overlay.json ./saga -run '^TestReviewAssemblyEffectSubscriptions$'
Set-Location D:/whb_s/review-temp-0917c/demo
go test -count=1 -v ./game/controllers/player ./internal/service/game -run '^TestReview'
go build ./...
go test ./... -run '^$'
```

DAO 新四叶子 1 通过/3 失败，原三项 BSON runtime 通过；attribute 编译 undefined 类型；Saga 三叶子 2 通过/1 失败；副本五叶子 2 通过/3 失败；battle 两叶子 1 通过/1 失败。构建与仅编译测试通过。副本替身只统计 Nest 奖励命令，未运行真实奖励存储；Saga 只检查实际注册的过滤器，未投递真实 NATS；battle 对照会记录未接 TCP runtime 的发送错误，只核验服务端推进。

## cfggen 原运行时门的复核

Codegen 当前 CLI：

```powershell
Set-Location D:/whb_s/cube-codegen
go run ./cmd/cfggen -meta internal/cfggen/testdata/runtime/cfg.yaml -out D:/whb_s/review-temp-0917c/cfg -pkg cfg
```

复制 `internal/cfggen/testdata/runtime/data/*.json` 到外部模块 `cfg/data/`；复制该目录的 `roundtrip_test.go` 到外部模块 `cfg/`，仅移除 `//go:build cfgruntime`。在外部模块执行 `go test -count=1 -v ./cfg`，真实配置读取、悬空引用保留旧快照、必填项校验和重载四项通过。原测试从已固定 SHA 获取，未重新发明等价测试；本轮没有运行发布 pin 的 shell 脚本，不能将源码模式结果写成发布验收。