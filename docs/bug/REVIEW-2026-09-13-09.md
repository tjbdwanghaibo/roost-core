# 第九轮：共享模式切换未知结果

## P2 RR-20260913-12：EnterShared 回复丢失后恢复独占写准入

Core `99be1ebc6191cf3e64acb22e39a8c439d7f3eea6`，未修复。[ownership.go](../../remoteentity/ownership.go) 的 EnterRemoteSharedMode（59–82 行），尤其 73–76 行错误恢复分支。关联 RR-20260913-09 的未知结果机制，但该项是另一入口，不重开已经验收通过的 Transfer 修复。

触发：owner 原为 local_owned；EnterSharedExpected 实际成功，将权威 Shared 改为 true、推进 lease；客户端未收到回复而得到 I/O 错误。本地直接 restoreOwnershipAfterTransitionFailure(expected)，回到旧 local_owned，未清热 marker 或重查权威。随后 PrepareRemoteWriteBatch 返回成功。

本轮临时 overlay 使用真实 Manager/状态机与可控 marker store，在实际执行 mock CAS 后注入回复丢失。结果 `authority_shared=true state=local_owned admission=<nil>`；期望重新确认权威进入 shared 模式，或者查询不明时冻结准入。实际独占快捷准入与权威共享模式不一致，可能跳过共享写所需的协调。没有执行真实双写或数据库提交，不能据此声称数据库已经损坏。

MarkerCacheTTL 在测试中设为一分钟以稳定放大窗口；这不意味着默认配置没有该窗口。发送前失败对照正常恢复 local_owned 并允许写入，说明不能简单把所有错误都永久 fence。

建议复用 Transfer 已有的“失效 marker → 独立有界查询 → 按权威结果恢复/冻结”骨架，按共享模式目标处理状态，不直接照抄 Transfer 的 owner 切换判定。权威确认 shared 后同步 live 状态和版本；查询失败时禁止独占准入。验收覆盖成功后丢回复、查询不可用、发送前失败、重试和后续共享锁路径。

## LeaveShared 观察，不作为同一确认 bug

执行后丢回复时测得 `authority_shared=false state=shared admission=remote entity: owner transition in progress`，本次写准入被拒。状态确实未同步，但未证明绕过协调或永久不可恢复；后续刷新/重试仍需继续审查。发送前失败对照 `authority_shared=true state=shared admission=<nil>`。

四个场景中 EnterShared 的安全断言失败，其余三项通过；LeaveShared 测试仅观测当前拒绝行为，不声称其未知结果协议完整通过。源码与命令见[复现](REPRO-2026-09-13-09.md)。
