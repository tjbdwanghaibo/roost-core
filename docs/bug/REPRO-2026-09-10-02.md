# 2026-09-10 第二轮独立测试

基线及实际结果见 [运行记录](../review/REVIEW-2026-09-10-02.md)。代码保存在 TEMP，通过 Go overlay 加入同包，复用已有测试助手；不修改仓库源码。

临时 go.work use 三仓 Windows 绝对路径；GOTOOLCHAIN=go1.27.0、CGO_ENABLED=1、GOWORK 指向该文件。Replace JSON 将下方测试映射到对应仓库包内不存在的 review_external_test.go。

## Mail 淘汰后重投：失败

```go
package mail
import("context";"fmt";"testing")
func TestReviewEvictionPreservesClaimIdentity(t *testing.T){
 h:=newHarness(t);ctx:=context.Background()
 envelope:=mustSend(t,h,withAttachment(directTo(1),"reward"))
 first,err:=h.service.ReserveClaim(ctx,1,envelope.ID,"");if err!=nil{t.Fatal(err)}
 if _,err=h.service.CommitClaim(ctx,1,envelope.ID,first.Token);err!=nil{t.Fatal(err)}
 for i:=0;i<MaxMailboxEntries;i++{if err=h.service.Deliver(ctx,1,fmt.Sprintf("filler-%d",i),0);err!=nil{t.Fatal(err)}}
 stored,found,err:=h.mailboxes.Get(ctx,1);if err!=nil||!found{t.Fatal("mailbox missing",err)}
 if _,exists:=stored.Value.Entries[envelope.ID];exists{t.Fatal("control: claimed entry was not evicted")}
 if _,err=h.service.Delete(ctx,1,"filler-0");err!=nil{t.Fatal(err)}
 if err=h.service.Deliver(ctx,1,envelope.ID,0);err!=nil{t.Fatal(err)}
 again,err:=h.service.ReserveClaim(ctx,1,envelope.ID,"")
 if err==nil&&again.Token!=first.Token{t.Fatalf("claimed mail resurrected with new delivery key: first=%q replay=%q",first.Token,again.Token)}
}
```

## Remote 部分准备失败后的再准入：通过

```go
package remoteentity
import("context";"testing";"time";"github.com/tjbdwanghaibo/roost-core/entity")
func TestReviewPartialPrepareReleasesFirstGate(t *testing.T){
 const kind entity.EntityKind=121
 entity.MustRegisterEntityKindDefs(entity.EntityKindDef{Kind:kind,Category:1,RemotePolicy:entity.RemotePolicyManaged})
 mgr:=NewManager(newMockVersionedLockFactory(),DefaultConfig(),1000)
 t.Cleanup(func(){ctx,cancel:=context.WithTimeout(context.Background(),time.Second);defer cancel();if err:=mgr.StopFinalizer(ctx);err!=nil{t.Error(err)}})
 loader:=newRemoteTestLoader();mgr.SetBackend(loader);mgr.SetOwnershipStore(newMockMarkerStore())
 first:=newTestRemoteEntity(1401,1,kind);missing:=newTestRemoteEntity(1402,1,kind);loader.add(first)
 ctx,cancel:=context.WithTimeout(context.Background(),time.Second);defer cancel()
 failed,err:=mgr.PrepareRemoteWriteBatch(ctx,[]int64{first.GUId(),missing.GUId()})
 if err==nil{if failed!=nil{_ = failed.Close(ctx)};t.Fatal("control: missing second entity should fail")}
 again,err:=mgr.PrepareRemoteWriteBatch(ctx,[]int64{first.GUId()});if err!=nil{t.Fatalf("partial prepare leaked first gate: %v",err)}
 if err:=again.Abort(ctx,nil);err!=nil{t.Fatal(err)}
 if err:=again.Close(ctx);err!=nil{t.Fatal(err)}
}
```
Mail 映射 cube-kit/service/mail/review_external_test.go；Kit 执行 go test -race -overlay <overlay.json> ./service/mail -run '^TestReviewEvictionPreservesClaimIdentity$' -count=1 -timeout=120s，失败输出：claimed mail resurrected with new delivery key: first="token-1" replay="token-2"。

Remote 映射 cube-core/remoteentity/review_external_test.go；Core 执行 go test -race -overlay <remote-overlay.json> ./remoteentity ./nest -count=1 -timeout=180s，通过。测试使用既有锁/所有权/加载器替身，第二个实体缺失；不是真实分布式后端验收。

Mail 用公开 Service 操作建立、领取、填充、删除及重投；填充用 Deliver 插入占位邮件引用，与现有容量测试方式相同。被重新领取的目标邮件使用真实有效信封。没有修改内部 Entries 来制造淘汰，也没有连接真实资产服务。
