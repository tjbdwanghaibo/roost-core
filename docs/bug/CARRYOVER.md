# 遗留清单：修完之后剩下的东西

> 2026-09-21 整理。`WANTED.md` 里待分流是 0、`README.md` 里未修复的 RR 是 0——但那不等于"没有遗留"：
> 每份 bugfix 记录末尾的"未做 / 边界"加起来有 44 条，散在十几个文件里。本文把其中**真正的缺口**挑出来，
> 按"要拍板的"和"没跑过的"分开，并标出**已经过时、不要再追**的那几条。
>
> 这不是 RR 表。这里的每一条都还没有被判定过——要登记成 RR、判为非问题、还是继续观察，是 review 的事。
> 判定之后请把它从本文移走（或标注去向），本文只保留"还没有结论"的。

## A. 要拍板的（有取舍，不是干不了）

| # | 条目 | 出处 | 一句话 |
| --- | --- | --- | --- |
| A1 | 已存在的毒丸 WAL 记录没有隔离流程 | [RR-20260920-01](../bugfix/RR-20260920-01.md) | 新记录已不可能成为毒丸；但一台**已经**因为旧记录起不来的进程没有出路 |
| A2 | 丢失检测 / 缺口补发没有做 | [RR-20260920-02](../bugfix/RR-20260920-02.md) | `SessionSequence` 的本意就是让客户端发现缺口并要全量，目前只有"不丢"这一半 |
| A3 | 毒药订单没有运维面；推后是固定 15 分钟 | [RR-20260920-05](../bugfix/RR-20260920-05.md) | 索引条目上没地方记尝试次数，所以做不成指数退避；被推后的坏订单也没有人会去看 |
| A4 | 空闲场景不重试被拒绝的 subscribe | [RR-20260920-06](../bugfix/RR-20260920-06.md) | demo 没有场景级定时 tick，重试搭的是下一次场景事件的便车 |
| A5 | 租约没有世代号 | [RR-20260920-09](../bugfix/RR-20260920-09.md) | 现在用"有没有间断"近似"中间有没有人写过"，代价是偶尔多扔一份其实没人碰过的副本 |
| A6 | 没有跨进程移交协议 | [RR-20260920-10](../bugfix/RR-20260920-10.md) | 玩家换进程登录要等 `HandBackIdle`（20 秒）才进得去，期间被按名拒绝 |
| A7 | 在途工作没有计数 | [RR-20260920-11](../bugfix/RR-20260920-11.md) | `Admit` 没有配对的 `Done`，归还只靠"多久没被使用"近似；严格保证要改契约 |
| A8 | `EntityManager.Destroy` 不可取消 | [RR-20260920-12](../bugfix/RR-20260920-12.md) | demo 侧把**等待**变有界了，撤离本身仍会等到事务结束；超时后晚到的撤离会卸载一份仍然合法拥有的实体（安全，代价是一次冷加载） |
| A9 | 活动窗口未开时贡献被静默丢弃 | [RR-20260921-01](../bugfix/RR-20260921-01.md) | 端点只记一条日志（有意：一个窗口少一点不是失败的副本），但没有指标，没人知道丢了多少 |
| A10 | demo-publish 的发布内容变了 | [RR-20260921-02](../bugfix/RR-20260921-02.md) | 为了能推上去，`.github/workflows/` 改名成 `generated-github/workflows/`；要保留原路径得配带 `workflow` scope 的 PAT |
| A11 | 测试侧的重复要不要收敛 | [合仓交接 §5](../feature/SINGLE_MODULE_MIGRATION.md) | `fakeBus` 两份逐字相同（`service/mail` 与 `kit/service/mail`，`mirror` 还有第三份变体）、`assertWALReplay*` 两份、errcode 契约测试 8 份。**取舍**：共享替身会把"替身比实现更宽容"的风险集中到一处 |
| A12 | 本仓 CI 不校验自己的生成物 | [合仓交接 §5](../feature/SINGLE_MODULE_MIGRATION.md) | 只在生成工程那侧 `generate --check`。合仓以来生成物一直不是规范形态（import 顺序），是手动重生成才发现的 |
| A13 | 同层内的小工具重复 | [合仓交接 §5](../feature/SINGLE_MODULE_MIGRATION.md) | `snakeCase`×2、`isNilInterface`×2、codegen 的 `toSnake`×4 / `parseKV`×2、skill 的三个算术助手。均早于合仓 |
| A14 | ARCH-01 / 02 的 kit 侧收尾；ARCH-05 的"要不要打散" | [bugfix README 的 ARCH 表](../bugfix/README.md) | 合仓把 kit 物理上搬进了 core，"下沉"已完成；剩下的是"要不要把它打散进 core 的包树"——是另一条线 |

## B. 没跑过的（不是干不了，是还没有证据）

| # | 条目 | 为什么重要 |
| --- | --- | --- |
| B1 | kit 的 `scripts/integration` 故障矩阵合仓后没重跑 | 搬迁只移动代码不改行为，所以预期无差异——**但没有证据，不要说成验过** |
| B2 | 迁移前后没有同机性能对比 | 同上。`ci.yml` 里那条 remote-snapshot 基准每次都跑，可作长期观察点 |
| B3 | 所有权那三条（RR-09/10/11）没有两进程实测 | 都是确定性单测 + 变异验证。真实的 `fatal projection version conflict` 需要两进程 + 真 Redis + 数倍于 `Lease=30s` 的窗口 |
| B4 | 活动窗口的机器人分支逻辑没有真实跨边界跑过 | 要等 CI 恰好跨过整 300 秒；确定性覆盖在单测里 |
| B5 | RR-20260920-06 的"16 观察者同场景无滞留 pair"没有自动化用例 | 需要一次 16 机器人实跑 |

## C. 已经过时的"未做"条目——不要再追

| 出处 | 原话 | 现状 |
| --- | --- | --- |
| [RR-20260920-04](../bugfix/RR-20260920-04.md) | "实体仍然不会卸载，被 fence 的玩家的 Player 对象留在内存里直到进程结束" | **已解决**：U-0268 让 fence 同时扔副本，U-0269 让空闲租约归还前先卸载。原记录已加更正 |
| [U-0270](../bugfix/U-0270-framework-release-version-drift.md) | "只防住了 codegen 这一个字段，framework.core / framework.kit 的漂移仍无人守" | **已消解**：schema 3 的清单只剩一行 `release`，那两个字段不复存在。原记录已加更正 |
| [RR-20260920-02](../bugfix/RR-20260920-02.md) | "16 客户端场景仍有红，但换了形状（3/16 缺 pos_x）" | **已闭环**：转 W-2026-09-20-02 → RR-20260921 之前的 RR-20260920-06 → U-0267 修复 |

## 维护约定

- 一条被 review 判定之后，从本文移走并在 `docs/bug/README.md` 留痕；判为 RR 的按正常流程走。
- 新的"未做"仍然写在各自的 bugfix 记录里（那是它的上下文所在），但**属于 A 或 B 两类的**要同时在这里登一行，
  否则它只会躺在一个没人打开的文件里——这正是本文存在的原因。
