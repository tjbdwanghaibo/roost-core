# 第七轮独立复现

Core a123605；将以下代码存为临时文件，并用 overlay Replace 映射到 statesync/review_round7_test.go。键、值使用本机绝对路径；同包复用已有 mustSnapshot/testMeta；临时 go.work 指向当前三仓，不运行 go work sync。

运行 `go test -race -count=1 -overlay <json> ./statesync -run TestReview -v`。九叶子六通过三失败，详见[运行](../review/REVIEW-2026-09-14-07.md)。FrameLimit 子测试名采用 rune(size)，日志 Ĭ 对应 300（两片、通过）、Ұ 对应 1200（单片、失败）；数字参数和断言中均保留 datagram 大小。

```go
package statesync
import("bytes";"errors";"testing";"time")
func TestReviewRecoveryBarrier(t *testing.T){for _,mode:=range []string{"ack_before_force","raw_ack_after_force","wire_ack_after_force","old_wire_after_resync"}{t.Run(mode,func(t *testing.T){
 check:=func(e error){t.Helper();if e!=nil{t.Fatal(e)}}
 r:=NewReplicator(ReplicatorConfig{});defer r.Close();check(r.RegisterSession(SessionInfo{ID:10}));s:=mustSnapshot(t,1,[]ObjectState{{Ref:ObjectRef{ID:1,Generation:1},Archetype:1}});check(r.Publish(s));p,e:=r.PrepareLatest(10);check(e);check(p.Commit())
 ack,e:=EncodeControl(ControlMessage{Type:ControlAck,RoomID:s.RoomID,Epoch:s.Epoch,Tick:1,Sequence:1});check(e)
 if mode=="ack_before_force"{check(r.Acknowledge(10,1))};check(r.ForceFull(10))
 switch mode{case "raw_ack_after_force":check(r.Acknowledge(10,1));case "wire_ack_after_force":check(r.HandleControl(10,ack));case "old_wire_after_resync":resync,e:=EncodeControl(ControlMessage{Type:ControlResync,RoomID:s.RoomID,Epoch:s.Epoch,Sequence:2});check(e);check(r.HandleControl(10,resync));if e=r.HandleControl(10,ack);!errors.Is(e,ErrInvalidControl){t.Fatalf("old control accepted: %v",e)}}
 check(r.Publish(mustSnapshot(t,2,s.Objects)));f,_,e:=r.BuildLatest(10);check(e);if f.Kind!=FrameFull{t.Fatalf("ForceFull lost: kind=%d base=%d",f.Kind,f.BaseTick)}
})}}
func TestReviewReassemblyFrameLimit(t *testing.T){for _,size:=range []int{300,1200}{t.Run(string(rune(size)),func(t *testing.T){
 limits:=DefaultLimits();limits.MaxFrameBytes=256
 frame:=DeltaFrame{SnapshotMeta:testMeta(1),Kind:FrameFull};packets,e:=FragmentFrame(frame,1,bytes.Repeat([]byte{1},400),size,DefaultLimits());if e!=nil{t.Fatal(e)}
 r:=NewReassembler(limits,time.Second);var last error;var done bool;var out []byte
 for _,packet:=range packets{out,done,_,last=r.PushFor(10,packet,time.Now());if last!=nil{break}}
 if !errors.Is(last,ErrFrameTooLarge){t.Fatalf("datagram=%d chunks=%d accepted=%v bytes=%d err=%v",size,len(packets),done,len(out),last)}
})}}
func TestReviewReassemblyRecoveryControls(t *testing.T){
 now:=time.Now();frame:=DeltaFrame{SnapshotMeta:testMeta(1),Kind:FrameFull};data:=bytes.Repeat([]byte{7},600);packets,e:=FragmentFrame(frame,1,data,300,DefaultLimits());if e!=nil{t.Fatal(e)}
 t.Run("reverse_duplicate",func(t *testing.T){r:=NewReassembler(DefaultLimits(),time.Second);for _,i:=range []int{2,2,1,0}{out,done,_,e:=r.PushFor(10,packets[i],now);if e!=nil{t.Fatal(e)};if i==0{if !done||!bytes.Equal(out,data)||r.Len()!=0{t.Fatal("bad assembly")}}else if done{t.Fatal("premature completion")}}})
 t.Run("expiry_restart",func(t *testing.T){r:=NewReassembler(DefaultLimits(),time.Second);r.PushFor(10,packets[0],now);if r.Expire(now.Add(time.Second))!=1||r.Len()!=0{t.Fatal("expiry failed")};for i,p:=range packets{out,done,_,e:=r.PushFor(10,p,now.Add(time.Second));if e!=nil{t.Fatal(e)};if i==len(packets)-1&&(!done||!bytes.Equal(out,data)){t.Fatal("restart failed")}}})
 t.Run("conflict_clears",func(t *testing.T){r:=NewReassembler(DefaultLimits(),time.Second);r.PushFor(10,packets[0],now);h,p,e:=DecodeDatagram(packets[0],DefaultLimits());if e!=nil{t.Fatal(e)};p[0]++;bad,e:=encodeDatagram(h,p);if e!=nil{t.Fatal(e)};_,_,_,e=r.PushFor(10,bad,now);if !errors.Is(e,ErrInvalidDatagram)||r.Len()!=0{t.Fatalf("err=%v inflight=%d",e,r.Len())}})
}
```
