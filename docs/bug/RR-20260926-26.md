# RR-20260926-26：快阶段访问未加载目标（不阻塞的冷缺失）被 RR-06 改为 panic，业务不能再按 ErrColdLoadInLogic 降级

日期：2026-09-26。基线：main `985d5ba`。等级 P2（兼容性破坏：原可处理的错误变为整个 handler 失败并回滚）。引入：RR-20260926-06 修复。
状态：已复现（修前 PASS / 修后 FAIL），未修复。

## 触发条件

快阶段经 `msg.getter` 或 `CastOne/CastMulti`（`nest/cast.go:139`）访问未加载且配置了 loader 的实体。

## 预期 vs 实际

预期（RR-02 契约、`entity/entity.go:48-49` 注释）：返回可 `errors.Is` 的 `ErrColdLoadInLogic`，不做 I/O，业务可降级（例如“好友离线”）。
实际：`entity/manager_access.go:99-101`（Get）与 `:211-214`（GetMany）在原有 `LoadedEntitiesOnly` 错误分支（`:102-104`、`:215`）**之前**
检查 `fctx.BlockingError` 并 panic；这条路径本不阻塞，超出 RR-06“等待入口”范围。

## 复现

[REPRO-2026-09-26-03](REPRO-2026-09-26-03.md) §2：`GOWORK=off go test ./nest -run TestProbeCastColdTarget -v`

```text
985d5ba: err=entity: cold load forbidden in logic ...: fctx: blocking operation in fast worker: entity.ManagerAccess.GetMany (handler=optional_friend) fallback=false  FAIL
aaada47: ret=friend offline err=<nil> fallback=true  PASS
```

## 实施方向与验收

- 快阶段冷缺失返回 `ErrColdLoadInLogic`（不 panic、不 I/O、不等 flight）；只有真正会阻塞的入口（I/O、等待）才 fail-fast。
- 回归：快阶段 CastMulti 冷目标 → 业务拿到 `ErrColdLoadInLogic` 并降级；loader 0 次。
