# 2026-09-09：新增接入问题与旧问题复核

## RR-20260909-01：当前接入文档与三仓发布入口不一致

**P2，已确认，未修复；文档/接入缺陷，不是运行时数据故障。**

基线：Core `c355259c7dafd75961b6f24879a4c8fed10559ea`，Codegen `4c607254ace82b16866c8ce2c5de74c89a33a452`。Core 本地原目录保留旧 HEAD，本轮确认以下文档与最新审查提交相同。

**位置：** Core `docs/QUICKSTART.md:21–27` 固定 Codegen v1.7.0 并给出没有 `-module` 的生成命令；`docs/README.md` 的版本基线仍列五仓。Codegen `ci/framework-release.yaml` 当前记录 Core v1.15.0、Kit v1.14.0、Codegen v1.15.1；`internal/roost/cli.go:117–118` 强制检查 module。

**触发：** 使用当前 Codegen，按当前 Core Quickstart 复制 `project new` 示例。

**期望：** 当前指南指向三仓发布组合，命令提供当前 CLI 的必需参数，使开发者可以继续生成工程。

**实际：** 最新 CLI 在生成前返回 exit 1，`-module is required so generated imports do not use someone else's repository`。指南同时推荐过时固定工具版本，不能可靠说明如何接入当前三仓框架。

**复现：** 从上述 Codegen 提交构建 `./cmd/roost`，在临时目录执行：

```text
roost project new planet -services game,gate -mods configdata,etcd,redis,mongo,nats,sync,remote_entity,dataengine,nest
```

**证据边界：** 实测的是最新 CLI 执行指南参数，未安装或执行指南固定的 v1.7.0 二进制，因此不声称旧版自身必然报同样错误。补 module 后能生成工程；后续 Go proxy 500/下载超时属于环境失败，没有另报框架缺陷。

**建议：** 以当前 release manifest 统一指南版本，补目标 module 示例，更新三仓发布顺序，并将指南命令纳入干净消费者验收。保留旧版指南时应明确适用版本。本轮只记录问题，不直接改 Quickstart。

图谱 Verify 已检查相应路径覆盖；因 metadata_changed 读取实际源码/文档补证，并用 CLI 验证。

## 旧问题复核

以下保持原编号，均在最新三仓 SHA 重跑原外部 overlay 测试，仍未修复：

| 编号 | 当前位置 | 结果 |
| --- | --- | --- |
| RR-20260908-01 | Kit `service/session/service.go:311–321` | 两个 owner 同一 RequestID 都成功，输家重试失败 |
| RR-20260908-02 | Core `cache/read_through.go:91–105` | 取消等待后，新等待者仍被拒绝 |
| RR-20260908-03 | Codegen `internal/roost/consolidate.go:271–275` | 单行 import 分流后格式化失败 |

完整三仓 SHA、测试环境、结果和学习结论见[本轮评估](../review/REVIEW-2026-09-09.md)。原始问题与测试分别见[首轮报告](REVIEW-2026-09-08.md)、[复现附录](REPRO-2026-09-08.md)。
