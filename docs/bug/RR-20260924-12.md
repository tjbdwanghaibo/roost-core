# RR-20260924-12：Remote 永久版本冲突未触发 DataEngine fencing

2026-09-24；P1；基线 `967bc69` 加 W-02 发号修复；状态：已修复、未发布。[修复](../bugfix/RR-20260924-12.md)。

W-02 继续复核发现：Remote meta 已有 `_ver=1`，另一事务以 base=0 创建时返回 Mongo duplicate key；其他 Remote CAS 不匹配返回 Mongo version conflict。MongoStore 原样包装，Projector 的 fatal 分类却仅识别自身 ErrProjectionConflict/身份冲突，故将这类确定冲突当成瞬时错误不断重试，也不拒绝后续准入。

触发：已有实体的确定状态与 WAL 新事务冲突。预期：普通写入与 Remote 写入都应触发同一 fatal fencing，拒绝新事务，不能 ack/丢弃未投影记录。

`TestRemoteCreationConflictFencesProjectorAndKeepsWAL` 修前返回原始 duplicate key，没有 ErrProjectionConflict。测试还覆盖同事务普通 DAO 回滚、fatal 通知一次、拒绝后续 Commit 和 WAL 记录保留。[历史根因](../bugfix/W-2026-09-22-02.md)。
