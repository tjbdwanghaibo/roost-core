# RR-20260924-17：无效 Remote 提交消耗 pending 容量

日期：2026-09-24；基线 `967bc69` 加 DataEngine 优化工作树。等级 P2。状态：已复现、修复并验证，未发布；[修复记录](../bugfix/RR-20260924-17.md)。

ApplyRemoteCommits 在验证提交身份、字段与可编码性之前先创建 tracker。校验失败直接返回，没有完成这个 tracker；pending 又不允许淘汰。持续无效输入最终使合法事务被 ErrRemoteOverloaded 拒绝。

容量设为 1，提交仅含匹配 TransactionID、缺失合法 Entity 身份的 RemoteCommit，返回错误后预期 Transactions=0，旧实现为 Transactions=1 / ActiveTransactions=1。`TestInvalidRemoteApplyDoesNotConsumeTrackerCapacity` 修前失败，日志 `/tmp/roost-remote-red.log`。

```sh
GOCACHE=/tmp/roost-nest-go-cache GOWORK=off go test ./remoteentity \
  -run '^TestInvalidRemoteApplyDoesNotConsumeTrackerCapacity$' -count=1
```

根因位于 `remoteentity/transaction_manager.go: ApplyRemoteCommits` 的准入时序。空批次也不构成有效 Remote 工作，不应制造完成记录或访问 backend。
