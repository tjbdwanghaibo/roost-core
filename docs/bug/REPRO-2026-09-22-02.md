# 复现附录：2026-09-22 第二轮（全部源码判定）

基线 `5b95547`。本轮无实跑，以下是判定依据的原文与 grep 结果。

## W-2026-09-22-04

```text
$ grep -n "NewRoomEnvelopeSink(" room/*.go | grep -v _test
room/room_broadcast.go:125:func NewRoomEnvelopeSink(downstream ReliableRoomFrameSink) *RoomEnvelopeSink {
room/room_broadcast.go:449:	envelopeSink := NewRoomEnvelopeSink(downstream)          # NewRoomBroadcaster 内，每房一个
$ grep -n "NewRoomBroadcaster(" room/room_manager.go
room/room_manager.go:176:	room, err := NewRoomBroadcaster(roomID, m.config.Downstream, …)   # Downstream 共享
```

```go
// room/room_broadcast.go:98-106
type RoomEnvelopeSink struct {
	downstream       ReliableRoomFrameSink
	subjectRooms     map[int64]int64
	roomFrames       map[int64]uint64
	sessionSequences map[roomSubscriberKey]uint64
// room/room_transport_sink.go:137-140
type roomSessionKey struct {
	roomID  uint64
	session core.SessionID
}
```

## W-2026-09-22-05

```text
$ grep -rl 'roost-core/entitysync"' --include='*.go' . | grep -v _test | grep -v '^./entitysync/'
room/room_broadcast.go
room/room_transport_sink.go
$ grep -on 'r\.coordinator\.[A-Za-z]*' room/room_broadcast.go | wc -l
12
```

`room_broadcast.go:698-713`：`r.subscribers[subscriber]++` 与 `r.coordinator.Subscribe(...)` 并列；`:1133-1160` `retryRetirement` 以 `coordinator.Subscribers` 为准。

## W-2026-09-22-06

```text
$ grep -rln "SubjectSyncState" --include='*.go' . | grep -v "_test\|^./demo/\|^./codegen/"
room/room_broadcast.go  entity/subject_sync.go  entity/entity_base.go  entitysync/subscription.go
$ grep -rn "\.Topic\b" entity room syncbus mirror --include='*.go' | grep -v _test | grep -v syncbus | grep -v jetstream | grep -v nats_syncbus
entity/subject_sync.go:150:	ret := EntitySyncCreateParam{Enabled: p.Enabled, Topic: p.Topic}
entity/entity_base.go:160:		Enabled: param.Enabled, SubjectID: param.EntityID, Namespace: param.Topic,
$ grep -rn "\.Sync()" demo --include='*.tmpl' | grep -v _test
demo/internal/service/game/scene.go.tmpl:305:	… bridge.room.RegisterSubject(subject.Sync()) …
demo/game/... spawner.go.tmpl:119:		… s.bridge.ShowSubject(ctx, value.Sync(), …)
```

`syncbus/patch_syncer.go:1-3`："transient patch replication over ISyncBus … long-lived state belongs in cache replica or entity snapshot layers"——与 `SubjectSyncState` 无关。
