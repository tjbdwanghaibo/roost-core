# RR-20260926-45：Remote 托管实体允许使用 sid 作用域 DAO——提交按提交方 sid 选库、加载按本服 sid 选库，跨服写或所有权迁移后实体不可写

日期：2026-09-26。基线：v1.17.0（`8811c02`）。等级 P3（拒绝写入，不会静默写坏）。状态：源码判定，未修复。

提交：`remoteentity/mongo_payload.go:36` → `mongo_committer.go:366-372` `DatabaseForSid(name, s.serverID)`；权威 meta 在全局 controlDB；加载：`mongo_load.go:47-48` → `dataengine/engine/mongo_store.go:272-273` 按本服 ServerID。
codegen 允许 `remote=managed` + `dbscope=sid`（`codegen/internal/dao/gen.go:314-319`）。共享模式多进程写或所有权迁移后 payload 分散于 `db_<sid>`，另一进程加载得旧版本 / 缺失 → `ErrRemoteVersionConflict` / not found（`batch.go:268-273`）。

修复约束（维护者 2026-09-26 批准：禁止该组合）：codegen 生成时报错；Assemble / 运行时注册同样校验（防手写 DAO），错误信息给出替代（改 global）。
