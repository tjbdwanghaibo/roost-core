# 2026-09-18 第三轮：独立复现记录

对应[六项问题](REVIEW-2026-09-18-03.md)和[运行记录](../review/REVIEW-2026-09-18-03.md)。临时测试位于 `D:/whb_s/review-temp-0918b` 与 `review-temp-0918c`，均在三仓之外；断言表达期望语义，没有修改产品实现。

## 复现矩阵

| 根因 | 独立叶子 | 结果 |
| --- | --- | --- |
| RR-20260918-05 邮件保留 | 已领取记录过期、信封仍可领 | 失败：`Claimed=true`，BagCount=3 |
| RR-20260918-06 会话关闭 | 加入后活动会话归零并等待两个周期 | 失败：Members=1 |
| RR-20260918-07 topic 解析 | 裸同包常量表达式 | 失败：得到字符串字面量 |
| RR-20260918-08 AOI 预算 | 合法大半径/小 block 配置 | 失败：接受 40,401 blocks |
| RR-20260918-09 怪物 ID | 两个独立 spawner 首次发号 | 失败：均为 900001 |
| RR-20260918-10 顶层 map 所有权 | committed old/new、rollback old/new | 2 通过、2 失败 |

## 顶层 DAO 所有权测试

使用当前 Codegen 重新生成 `internal/dao/testdata` 的 Hero DAO。初始化 key=1 的 old，事务内 `SetEquips(1,new)`，选择提交或返回 sentinel 回滚；清空记录型 committer 后，在新事务中修改 old 或 new 的 `SetLevel`，统计真实 Nest 产生的 CommitRecord。

```go
tests := []struct {
    name string
    abort, mutateOld bool
    wantCommit int
}{
    {"committed_old_is_detached", false, true, 0},
    {"committed_new_is_owned", false, false, 1},
    {"rolled_back_old_is_restored", true, true, 1},
    {"rolled_back_new_is_detached", true, false, 0},
}
```

执行：

```powershell
$env:GOCACHE='D:\whb_s\.gocache'
go test . -run '^TestReviewTopLevelMapValueOwnership$' -count=1 -v
```

实际 `committed_old_is_detached` 和 `rolled_back_new_is_detached` 都得到 1 条 commit，另外两项通过。该测试只计通知是否越权；修复验收还需断言 mutation payload，防止 stale value 写回。

## Wanted 五项的最小触发

- 邮件：fresh demo 的真实 `handlerClaimMailReward`；先写入 `now-ClaimRetentionSeconds-1` 的同一 mail ID，再在邮件仍可 Reserve/Commit 的前提调用。期望拒绝，实际再次增加物品。
- 关闭：scene 加入玩家，Runtime stub 的 `ActiveSessions` 从 1 改 0，不触发 Join/Push，等待两个复制周期。期望成员清理，实际保留。
- topic：在 Codegen `internal/entity` 包内调用 `syncTopicExpr("SyncTopicPlayer")`。期望常量表达式或显式拒绝，实际返回带引号字符串。
- AOI：临时 overlay 在 `spatial` 包内构造 Bounds `10000×10000`、BlockSize 10、Enter 900、Leave 1000，然后 AddObserver 并读取内部 block 数。期望受配置预算拒绝，实际 40,401。
- ID：fresh demo 的 `game` 包内各建一个 spawner 并调用第一次 `mintID`。期望不同，实际均为 900001。

## 修复验收与正向基线

本轮先验收 U-0236/RR-03 与 U-0237/RR-04：RR-03 的 map/slice/pointer 替换与回滚、后续 commit 共 10 个独立叶子全部通过；RR-04 的时间闸、单次领取、清理后旧 Run 拒绝 3 个叶子全部通过。官方 DAO golden runtime 脚本通过。

fresh demo 在加入上述红测前完成 generate，并通过 `go test ./...`；`roost project doctor` 全部检查通过。Codegen game-demo/template 的 8 个定向测试通过；Core `go test -race ./spatial ./entitysync ./room`、Kit `go test ./service/mail/...` 通过。Codegen `internal/dao`、`internal/entity` 通过；`internal/roost` 全包曾因 Windows PATH 中没有 `sh/shellcheck` 失败，相关模板测试定向执行通过，该环境失败不计产品问题。

合并计数：修复验收 13 个叶子通过，Wanted 5 个叶子失败，K1 顶层 map 4 个叶子 2 过/2 失败，共 **22 个独立行为叶子：15 通过、7 失败，归为六个新根因**。全包/doctor/模板门禁不重复计入叶子。
