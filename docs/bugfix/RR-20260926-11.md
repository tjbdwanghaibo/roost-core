# RR-20260926-11：旧 Remote 回执重放不回退活实体

问题：[原始复审](../bug/RR-20260926-11.md)。日期 2026-09-26。
实施基线：main `381efc9` 加上一轮 RR-03～09 的未提交修复；原报告基线 `aaada47` 保留。
状态：代码已修复，适用本地验证通过，尚未提交/发布。完整证据和限制见[本轮汇总](../review/REVIEW-2026-09-26-release-fixes.md)。

## 修复与依据

验证持久回执后，若本地版本向量已全面不低于记录且至少一维更高，仅跳过本地版本设置/ack；仍重放快照发布与 invalidation。setter 的旧 fence 拒绝契约不变。

涉及：`remoteentity/transaction_manager.go`。

## 验证

TestCommittedReplayPreservesNewerLiveFenceAndDirtyState（修前失败、修后通过）。命令、负对照范围与环境见汇总；没有把全部测试绿色当成生产故障已穷尽。

## 兼容与边界

只作用于已验证持久回执的重放，不授权旧 writer。

## 复核后的补修（2026-09-26）

对应[复核残留](../bug/RR-20260926-11.md#复核残留2026-09-26基线-985d5ba未修复)与 [REPRO-2026-09-26-03](../bug/REPRO-2026-09-26-03.md) §9。
实施基线：main `87d9499` + RR-20260926-28 提交 `9a4ef81`（后者不触及 tracker）。状态：已修复，适用本地验证通过，已本地提交，未推送/发布。

### 根因（基线行号）

`remoteentity/transaction_tracking.go:103`，`completeRemoteTransaction` 无条件 `tracker.status = status.Clone()`。已 Committed 的事务被投影器
重放、重放的发布步骤（`ApplyRemoteCommits` 的 `MarkRemoteCommitPublished`，`transaction_manager.go:450-452`）失败时，把终态改写成 Indeterminate；
`done` 已关闭，后到的 `waitRemoteTransaction` / `FlushRemoteAll` / `RemoteCommitStatus` 读到的都是“结果未知”。

### 修复

- 新增 `remoteCommitFinal`（`transaction_tracking.go:134`），写明终态：**Committed**（已持久并发布）与 **Rejected**（持久拒绝或确定未提交）。
  Admitted、Applied、Indeterminate、Unknown 是过程态或未知结论，可以被后续持久结论覆盖；Indeterminate 仍会关闭 `done` 唤醒等待方（行为不变）。
- `completeRemoteTransaction`（`transaction_tracking.go:103`）遇到终态直接返回，不改写；与现有终态矛盾的更新只计数
  `remote_entity_transaction_final_overwrite_ignored_total`，保留先到的持久结论。
- 重放调用本身的返回值不变：发布失败仍返回可重试的 `ErrRemotePersistenceIndeterminate`，投影器照常退避重试并最终发布、ack；
  只是不再污染已经确定的 tracker。Published 状态当前没有写入 tracker 的路径，未纳入终态集合。

### 回归

- `TestCommittedTrackerSurvivesFailedReplayPublication`（`remoteentity/replay_outcome_test.go`）：单元级，Committed 后注入一次重放发布失败，
  等待方、`FlushRemoteAll`、`RemoteCommitStatus` 仍得到 Committed；并验证 Indeterminate → Committed 的升级仍然生效。
- `TestCommittedReplayAfterFinalizerAndNewerFenceAcksWAL`（`remoteentity/replay_publication_e2e_test.go`）：端到端，真实 nestwal WAL（V2）+
  `engine.Projector`/`engine.MongoStore` + mongotest 伪 Mongo，Remote 走正式 `Backend` + `MongoCommitter` 权威。脚本化存储按调用序号让竞态确定：
  tx1（async）首次发布标记失败 → finalizer 回源 Applied、抢先发布并释放 gate → 同实体下一写者 `GrantWrite` 拿到更大 fence → 投影器重放旧回执
  不被新 fence 拒绝、不回退活实体 fence（再注入一次发布失败，此时 tracker 必须仍是 Committed）→ 第三次投影成功 ack → 新写者 tx2 经 WAL 正常提交。
  断言 `WALUnacked=0`、`FatalProjectionConflicts=0`、`ProjectionFailures=2`（恰为两次注入，无无限重试）、权威 `_ver=2`、tx1/tx2 持久 Committed、写额度归零。

修前（基线 `87d9499` + `9a4ef81`，只加测试）：

```text
$ GOWORK=off go test -race -count=1 -v -run 'TestCommittedReplayAfterFinalizerAndNewerFenceAcksWAL|TestCommittedTrackerSurvivesFailedReplayPublication' ./remoteentity
=== RUN   TestCommittedTrackerSurvivesFailedReplayPublication
    replay_outcome_test.go:240: committed tracker overwritten by failed replay: state=6 err=remote entity persistence outcome is indeterminate: injected publish mark failure
--- FAIL: TestCommittedTrackerSurvivesFailedReplayPublication (0.00s)
=== RUN   TestCommittedReplayAfterFinalizerAndNewerFenceAcksWAL
    replay_publication_e2e_test.go:208: committed tx1 reported as unresolved after a failed replay: remote entity persistence outcome is indeterminate: injected publish mark failure
--- FAIL: TestCommittedReplayAfterFinalizerAndNewerFenceAcksWAL (0.03s)
FAIL
```

端到端用例在修前停在终态被覆盖这一步；此前的 finalizer 抢先、新写者拿到新 fence、重放不被 fence 拒绝都已通过（原 RR-11 修复生效）。

修后：

```text
$ GOWORK=off go test -race -count=20 -run 'TestCommittedTrackerSurvivesFailedReplayPublication|TestCommittedReplayAfterFinalizerAndNewerFenceAcksWAL|TestCommittedReplayPreservesNewerLiveFenceAndDirtyState|TestMemoryNeverCommittedTransientErrorGetsDurableRejection|TestMemoryLateCommitBeatsFinalizerRejection|TestWALDurabilityUnknownIsNotRejectedByFinalizer|TestMongoRejectUnresolvedDefersToExistingTransaction|TestMemoryLostCommitReplyKeepsFinalizerOwnership|TestDurableLeaseRejectionReleasesDeferredWriteGate' ./remoteentity
ok  	github.com/tjbdwanghaibo/roost-core/remoteentity	4.266s
$ GOWORK=off go test -race -count=1 ./remoteentity ./kit/remoteentity ./dataengine/... ./nest ./entity
ok  	github.com/tjbdwanghaibo/roost-core/remoteentity	2.918s
ok  	github.com/tjbdwanghaibo/roost-core/kit/remoteentity	1.608s
ok  	github.com/tjbdwanghaibo/roost-core/dataengine	2.058s
ok  	github.com/tjbdwanghaibo/roost-core/dataengine/engine	5.246s
ok  	github.com/tjbdwanghaibo/roost-core/nest	3.512s
ok  	github.com/tjbdwanghaibo/roost-core/entity	2.449s
$ GOWORK=off go vet ./...            # 无输出
$ GOWORK=off go run ./cmd/glsvet ./remoteentity/... ./dataengine/...   # exit 0
$ bash scripts/test-remote-generated.sh                                  # async/strict/pipelined PASS，ok remoteflow 4.298s
$ GOWORK=off go test -tags integration -count=1 -p 1 -skip 'Partition|ColdAdmissionDeadlineUnderLatency|RedisCluster' ./remoteentity/...
ok  	github.com/tjbdwanghaibo/roost-core/remoteentity	6.569s
```

集成用例的实际范围与隔离方式同 [RR-20260926-28 修复记录](RR-20260926-28.md#验证)；未运行 `scripts/test-remote-matrix.sh`。

### 兼容与未验证项

- 公开 API 与持久格式不变。可观察变化：已 Committed/Rejected 的事务不会再因为重放失败或迟到的矛盾通知变成其他状态。
- 首次调试端到端用例时，tx2 的投影确认与 finalizer 对账并发调用测试实体的 `AcknowledgeRemoteCommit`，race 检测报出测试夹具的非原子 `dirty`
  字段；生成实体走原子 `AdvanceVersion`，不受影响。用例改为投影 Flush 之后再 Close tx2，未修改夹具。
- D2 并行窗口的“成功后缀重放”没有专门的端到端用例：本用例每段只有一条记录。终态保护与 stale-fence 判断不依赖投影路径，但未单独验证。
