# RR-20260924-20 修复记录

问题见 [RR-20](../bug/RR-20260924-20.md)。已修复并验证，未发布。

成功与拒绝回复只在本地 token 仍等于请求 token 时更新 acquired/version。旧操作返回值仍按 Redis 结果处理，与 Touch/Refresh 的代际保护一致。

两种旧回复场景修前红、修后 race 通过，断言新代持锁、版本和 fence 保持，并能正常解锁。历史丢回复重试与旧 owner 隔离测试通过。日志 `/tmp/roost-remote-lifecycle-generation-green.log`，完整回归见 [第二轮实施](../feature/REFACTOR-2026-09-24-remote.md#第二轮启停与锁代际)。

Redis token、幂等回执和公开 API 不变。此次限定于迟到回复的本地更新，不宣称穷尽所有并发使用方式。
