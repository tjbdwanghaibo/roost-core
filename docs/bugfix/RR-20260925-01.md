# RR-20260925-01：测试流随夹具回收

对应 [问题](../bug/RR-20260925-01.md)。修复 `realFixture.close`：停止 DataEngine/outbox 后，删除本夹具独有的 JetStream 流，再关闭客户端；流不存在视为已清理，其他清理错误使测试失败。使用隔离集群直连，避免故障注入代理阻断清理。保留 closeOnce，重复关闭不会重复回收。

历史残留只清理固定隔离集群 `127.0.0.1:14222..14224` 中匹配 `^ROOST_IT_EFFECTS_[0-9]+_[0-9]+$` 的已结束测试流；清理时持有隔离验收锁，没有业务测试在运行。清单共 56 项，未处理其他流或生产数据。

真实网络故障三项 race 复跑全部通过；复跑后没有新增本次测试流。第一次清理确认删除 23 个，33 个旧流的删除请求回复停滞，停止了该清理子进程。随后使用每个请求最多 2 秒的幂等删除，再独立查询实际流列表，最终匹配的历史测试流为 **0**；以列表核对确认完成，而不是把超时当作删除失败或成功。未重置测试集群或强删存储目录。原始清单和补跑日志见 `artifacts/perf/remote/authority-verification-20260925/`。未修改生产 NATS/JetStream 能力接口或存储限额。
