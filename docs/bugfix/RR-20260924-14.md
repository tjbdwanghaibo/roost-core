# RR-20260924-14 修复记录

问题见 [RR-20260924-14](../bug/RR-20260924-14.md)。已修复并验证，未发布。

`mongo_projection.go` 明确告知 applyMutation 是否处于 Mongo transaction。多文档路径已在写入前核对持久业务 marker，applyPut 在事务内收到重复键时直接返回 ErrProjectionConflict，不再在已中止的 session 中继续查询。整个事务回滚，Projector 停在真实冲突处。单文档快路仍允许在普通 context 查询末次事务 ID，保持其丢 ack 幂等语义。

不改变 BSON/WAL 格式、业务 TransactionID、durability 或 Remote 协议，不跳过冲突记录。

修前真实测试耗尽 15 秒超时；修后相同测试 0.80s 通过，确认四份文档版本均为 2、只有前两笔 marker、WAL 保留两笔、fatal 冲突计数为 1。完整 `go test -race -tags=integration ./kit/dataengine -run '^TestReal' -count=1 -v -timeout=300s` 通过（50.533s，大积压用例须另开环境变量，本次明确跳过）；日志 `/tmp/dataengine-batch-real-green.log`。相关九包 race 通过，证据 `/tmp/dataengine-batch-race.log`。
