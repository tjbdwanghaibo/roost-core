# RR-20260925-13 修复：保留冷快照预算窗口的时间基准

[问题与复现](../bug/RR-20260925-13.md)。日期：2026-09-25。

首次检查初始化窗口；后续按已跨越的完整 Interval 数推进窗口起点，而非直接赋实际执行时间。
错过的窗口不积攒配额，每次仍只允许一份对象/字节/每会话额度。periodic 行为保持。
提取同包 `refreshSnapshotWindowAt`，让边界、晚醒和长时间空闲可用固定时间验证，无需 sleep。
未新增公开配置或包，也未修改 20Hz 兜底及 Guard 捕获契约。

修前定向回归失败；修后定向 race ×10、11 包 race/vet、glsvet 和正式双模式生成工程通过。
最终回归和正式负载结果见[本轮收尾记录](../feature/REFACTOR-2026-09-25-sync-snapshot-scheduling.md)。
窗口边界附近可能在很短时间内分别消耗两份额度，这是固定窗口软预算语义；可靠队列和
帧硬上限仍负责实际发送背压。不是滑动窗口限速器，更不代表严格 50ms 已验收通过。
