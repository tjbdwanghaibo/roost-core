# RR-20260926-05 修复：Sync 未尝试会话预算预扣

对应[问题](../bug/RR-20260926-05.md)。日期：2026-09-26；基线 aaada47 / a22c5a5。状态：已修复、已验证，未提交/部署。

scheduleSnapshots 只用局部预算规划并记录 charges；Flush 结束按原计划顺序结算实际尝试 Push 的会话，更新额度和全局游标。提前取消、编码失败、失效与未轮到的会话不扣费。

涉及文件：sync/entitysync/snapshot_budget.go、flush.go、workspace.go。维持原包结构，不改公开协议或 WAL 格式；行为与豁免详见[复审核实记录](../review/REVIEW-2026-09-26-followup.md#决策边界)。

回归：TestSnapshotRetryOnlyChargesAttemptedSessions、TestSnapshotBudgetDoesNotChargeUnattemptedSessions、TestSnapshotAttemptBudgetPreservesSuccessfulFramePrefix。

旧版首次重试后本窗零交付；修后另外两会话可继续。一会话一旦开始尝试就消费本批冷额度；成功帧前缀仍单独提交，不采用每帧退款。

本轮相关包 race、vet/glsvet 和正式生成 Sync/DataEngine/Remote 验收通过；准确命令、负对照、首次失败与未验证项统一见[验证记录](../review/REVIEW-2026-09-26-followup.md#验证与负对照)。不沿用历史性能数字作为本轮压测。


## 复核后的补修（2026-09-26）

对应[复核残留](../bug/RR-20260926-05.md#复核后的补修2026-09-26)。基线 main `87d9499` + RR-04 补修 `2b770f6`；状态：已修复、已验证，本地提交，未 push。

### 根因

Push 按会话 ID 顺序（`87d9499` `flush.go:258-265`），`commitSnapshotAttempts` 按计划顺序给每个已尝试会话推进游标（`snapshot_budget.go:338`）。游标轮转后两者不一致，RetryLater 中断时游标会停在“ID 更小但计划更靠后”的已尝试会话上，越过计划内未尝试的会话。

### 方案（两处同时改）

- **Push 顺序跟随计划**：`snapshotPlan.admissionOrder` 让本批有冷创建计费的会话按计划中首次出现的顺序先 Push，其余会话仍按 ID 顺序（`flush.go` 在 `scheduleSnapshots` 之后取用）。单类别时，被 RetryLater/取消打断后实际尝试的会话恰好是计划前缀，计费与游标一致。
- **游标只推进到按计划顺序的实际尝试前缀**：`commitSnapshotAttempts` 遇到仍然有效却未被尝试的会话就停止推进游标和下一类别，其后已尝试的会话照常计费（“一旦尝试即消费本批冷额度”的既有规则不变），但不越过前面的未尝试者。已失效的会话（编码失败被关闭、Close、Hold 替换）不再参与轮转，可以越过。这一条覆盖同一会话同时有新入场与恢复请求、Push 顺序无法与两个类别都一致的情况。

取舍：跨类别时排在未尝试者之后、已经尝试过的会话已被计费，但下一次计划仍可能先轮到它一次（重复一个轮次的机会），换取“不越过未尝试者”；不做逐帧退款。

涉及文件：`sync/entitysync/snapshot_budget.go`、`flush.go`；回归：`snapshot_attempt_test.go`。无公开 API、协议或持久格式变化；Push 在会话间的顺序由“全按 ID”变为“冷创建计费会话按计划顺序在前”，各会话内部帧序和交付语义不变。

### 回归

- `TestSnapshotRetryWithRotatedCursorDoesNotSkipUnattempted`：游标从中间开始、轮转越过末尾。on_change（MaxObjects=3，游标 2，计划 3→4→1，会话 3 RetryLater）与 periodic（MaxObjects=2，游标 3，计划 4→1，会话 1 RetryLater）；断言 on_change 下计费会话与实际尝试会话相同，且第二次 Flush 后会话 4 已持有对象。修后输出 `attempted=[3] windowSessions=map[3:1] cursor=[3 0]`、`second flush delivered=[1 4]`；periodic `attempted=[4 1]`、`delivered=[2 3 4]`。
- `TestSnapshotCursorStopsBeforeUnattemptedAcrossClasses`：恢复 s2 → 新入场 s1 → 新入场 s2，s2 RetryLater；断言 Push 顺序为计划顺序、新入场游标不越过 s1。修后 `attempted=[2] windowSessions=map[2:2] cursor=[0 2]`，同窗口第二次 Flush 交付 s1。
- 修前红、负对照见问题记录；既有 `TestSnapshotRetryOnlyChargesAttemptedSessions`、`TestSnapshotBudgetDoesNotChargeUnattemptedSessions`、`TestSnapshotAttemptBudgetPreservesSuccessfulFramePrefix`、`recovery_fairness_test.go`、`snapshot_requests_test.go` 保持通过。

### 验证命令

```text
GOWORK=off go test -race ./sync/entitysync -count=20 -run 'TestSnapshotCursorStopsBeforeUnattemptedAcrossClasses|TestSnapshotRetryWithRotatedCursorDoesNotSkipUnattempted|TestSnapshotRetryOnlyChargesAttemptedSessions|TestSnapshotBudgetDoesNotChargeUnattemptedSessions|TestSnapshotAttemptBudgetPreservesSuccessfulFramePrefix'   ok
GOWORK=off go test -race ./sync/entitysync -run 'TestSnapshot|Recovery|Fair' -count=20   ok
GOWORK=off go test -race -count=3 ./sync/... -skip 'TestConcurrentOpenResultMatchesSessionState|TestSessionInvisibleUntilTransportOpened'   8 个包 ok
GOWORK=off go vet ./...                              无输出
GOWORK=off go run ./cmd/glsvet ./sync/entitysync     exit 0
gofmt -l sync/                                       无输出
bash scripts/test-sync-modes-generated.sh            ok  syncmodes
```

`-skip` 的是同时在工作树中、尚未修复的 RR-15 复核红测试（下一笔提交修复），不属于本项。

### 性能

每次有冷创建计费的 Flush 多一个会话集合与一次 O(会话数) 重排。同机 Apple M5 / Go 1.27.0，修前（`2b770f6`）与修后测试二进制交替 6 轮：`ByteBudgetRecoveryDrain` 152.0ms → 159.1ms（p=0.699，无显著差异；本轮机器负载约 5，时间方差 ±21%～±161%，时间结论不可靠），分配 216.3k → 216.8k allocs/op（+0.25%，p=0.002）、83.80 → 84.01MiB/op；`ExhaustedSnapshotWindow`、`ManagerFlush` 5 档无显著差异。未运行 `scripts/perf/sync-recovery.sh` 端到端对照。

### 未验证项与边界

- 未做端到端恢复压测；持续 RetryLater（传输长时间不可用）时会话间公平性只按本条规则推理，未单独压测。
- 非冷创建会话（只有增量/删除）在 RetryLater 下仍按 ID 顺序，低 ID 先尝试；这与冷预算无关，本次未改。
