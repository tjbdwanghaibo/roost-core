# RR-20260924-18 修复记录

问题见 [RR-20260924-18](../bug/RR-20260924-18.md)。已修复并验证，未发布。

remoteentity/snapshot_l2.go 在 Redis 边界使用 strconv.FormatUint(uint64(value.Checksum), 10)，把校验和转换为驱动支持的精确十进制字符串。保持 Lua 的 checksum 比较、既有 Redis 文本格式、JSON 数值和 BSON 二进制格式，不把校验和转成有符号数或浮点数，不改变正常 Redis 故障时的缓存降级契约。

新增 kit/dataengine/remote_integration_test.go，实际执行 Mongo 原子提交、Remote Mongo outbox、Redis Lua/L2 和文件 WAL；发布传输错误与 WAL checkpoint 错误由测试注入。修前真实驱动报错、修后 `-race` 通过。断言四份 L2 快照以及另一个没有参与重放的 Manager 的冷读取，避免只查本机 L1。

同一个集成场景还覆盖两个 Remote Entity 各两个 DAO 加两个本地 DAO 的原子事务、发布失败后新 Manager 的 RecoverOutbox、重开 WAL 的确认丢失幂等重放，以及最后一个 Remote Entity 冲突时六个 DAO 全部保持原版本、无错误发布且 WAL 保留。

验证日志 `/tmp/roost-remote-real-final.log`；关联包 race 与 vet 见 [实施记录](../feature/REFACTOR-2026-09-24-remote.md)。该测试直接构造正式 CommitRecord，没有运行生成的 Remote Nest handler、真实跨进程 ownership 迁移或网络快照广播；不能据此声称 Remote 全业务链路已完成上线验收。
