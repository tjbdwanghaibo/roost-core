# RR-20260924-15 修复记录

问题见 [RR-20260924-15](../bug/RR-20260924-15.md)。已修复并验证，未发布。

`MongoStore.ProjectBatch` 在构建 digest map 时比较已经出现的 ID；同 ID 不同 digest 立即返回 ErrTransactionIdentity，在开启 session、查询 marker 或写入文档前拒绝。保持现有 record validation 优先顺序和单笔幂等语义。

`TestMongoBatchRejectsConflictingDuplicateIDBeforeMarkerLookup` 修前失败、修后 race 通过；相关九包 race 通过。修前日志 `/tmp/dataengine-batch-identity-red.log`，修后 `/tmp/dataengine-batch-race.log`、`/tmp/dataengine-batch-last-unit.log`。真实 Mongo 的已提交 marker 内容不符检查由 `TestRealLocalMultiBatchOrderIdentityAndLostCheckpoint` 覆盖，通过。没有自动删除或改写历史坏 WAL。
