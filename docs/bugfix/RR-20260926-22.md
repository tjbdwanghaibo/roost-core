# RR-20260926-22：公会 ID 首次并发分配

问题：[原始复审](../bug/RR-20260926-22.md)。日期 2026-09-26。
实施基线：main `381efc9` 加上一轮 RR-03～09 的未提交修复；原报告基线 `aaada47` 保留。
状态：代码已修复，适用本地验证通过，尚未提交/发布。完整证据和限制见[本轮汇总](../review/REVIEW-2026-09-26-release-fixes.md)。

## 修复与依据

upsert 只按唯一 _id 匹配，返回后检查动态 ID 上限；计数器库由 GuildDao.DbName 决定。补 128 并发真实 Mongo 首次分配；既有 WAL 重启 fixture 补正式 ClaimOwnership/GrantWrite，旧版本碰撞仍被拒绝。

涉及：`demo/game/controllers/player/guild_ids.go.tmpl`、`controller.go.tmpl`、`guild_ids_test.go.tmpl`。

## 验证

完整 game-demo build/vet；TestGuildIDs* -race：内存并发、真实 Mongo WAL 三次重启、128 并发首次分配均通过。命令、负对照范围与环境见汇总；没有把全部测试绿色当成生产故障已穷尽。

## 兼容与边界

创建失败仍允许空号，不能回收。未放宽持久权威校验；旧测试伪造 fence=0 的问题被修正。
