# RR-20260924-17 修复记录

问题见 [RR-20260924-17](../bug/RR-20260924-17.md)。已修复并验证，未发布。

ApplyRemoteCommits 先检查非空、事务 ID、每项 Validate 与可编码性，并冻结提交切片，全部成功后才申请 tracker。坏输入不会占用新的容量，也不会覆盖同 ID 已有结果。空批次明确返回 ErrRemoteRejected；有效请求的提交、快照发布和错误处理顺序保持。

`TestInvalidRemoteApplyDoesNotConsumeTrackerCapacity` 修前失败、修后通过；`TestInvalidRemoteApplyPreservesExistingCompletion` 覆盖空批次、无效提交和不匹配 ID 对已有完成状态的保护。Remote 及关联包 race 通过，日志 `/tmp/roost-remote-race-final.log`。有效请求 API、WAL 与 Mongo 格式不变，空批次从无工作提交收紧为明确拒绝。
