# RR-20260924-16 修复记录

问题见 [RR-20260924-16](../bug/RR-20260924-16.md)。已修复并验证，未发布。

FlushRemoteAll 在 txMu 下保存 pending tracker 指针，单笔和批量等待共用 waitTrackedRemoteTransaction。等待者持有对象直到返回；从历史 map 淘汰不会改变已接受等待的对象。调用后新准入的事务不纳入当前 Flush，保持原边界；状态仍在原锁下读取，避免完成更新竞争。

`TestFlushRemoteAllKeepsTrackersAcrossConcurrentEviction` 修前失败、修后通过；保留并执行历史单笔等待淘汰测试。Remote 及关联包 race 通过，日志 `/tmp/roost-remote-race-final.log`。公开 API、持久化和协议不变。此次不扩展为无限保留所有历史事务，也不改变针对一个已经被淘汰 ID 新发起等待的旧语义。

结构与微基准见 [Remote 实施记录](../feature/REFACTOR-2026-09-24-remote.md)。
