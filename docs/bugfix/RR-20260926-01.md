# RR-20260926-01 修复：统一 Flush 错误留存

[问题与修前复现](../bug/RR-20260926-01.md)。日期：2026-09-26。

在 Flush 的执行出口统一更新 FlushFailures 与 LastError，删除分支重复记账。保留
policy、prepare、admission、reserve、commit 阶段上下文，捕获/准入错误附带 subject/session。
使用 `%w` 与 errors.Join 保留 errors.Is；成功不清空历史错误。尚未取得 flushGate 的取消、
已关闭 Manager 等未开始执行的调用不新增失败计数，原契约保持。

改动集中在 `sync/entitysync/flush.go`，回归位于 `failure_test.go`。测试证明：

- policy 返回的 sentinel 可由 Flush 和 LastError 追溯，一次只计一次；恢复成功后保留历史。
- 两会话部分准入后取消仍保留错误原因；成功帧的前缀不能回滚，下一次 Flush 补齐全量后排空。

针对性 race 与 `go test -race ./sync/... ./entity ./nest ./scripts/perf/sync-aoi` 均通过；
同范围 go vet、glsvet 和 `scripts/test-sync-modes-generated.sh` 通过。
正式负载和公平调度结果见[验收记录](../feature/REFACTOR-2026-09-25-sync-recovery-fairness.md)。

取消测试证明一种合法失败路径可重试，不证明历史偶发错误就是取消；后续再出现时须按实际
阶段和原因调查。公开 API/线协议不变，错误文本增加上下文；尚未部署。
