# Code generated by roost-codegen. DO NOT EDIT.
services:
  account:
    image: ${ROOST_IMAGE:?ROOST_IMAGE must be an immutable digest}
    container_name: planet-account
    command: ["account", "--sid=1000", "--config=/etc/roost/config.yaml"]
    restart: unless-stopped
    read_only: true
    init: true
    user: "65532:65532"
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    tmpfs: [/tmp:rw,noexec,nosuid,size=64m]
    stop_grace_period: 45s
    healthcheck:
      test: [CMD, /app/healthprobe, http://127.0.0.1:9100/readyz]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 30s
    volumes:
      - type: bind
        source: ${ROOST_CONFIG_ROOT:?ROOST_CONFIG_ROOT is required}/config.account.yaml
        target: /etc/roost/config.yaml
        read_only: true
    deploy:
      resources:
        limits: {cpus: "2", memory: 2G}
        reservations: {cpus: "0.25", memory: 256M}
  activity:
    image: ${ROOST_IMAGE:?ROOST_IMAGE must be an immutable digest}
    container_name: planet-activity
    command: ["activity", "--sid=1001", "--config=/etc/roost/config.yaml"]
    restart: unless-stopped
    read_only: true
    init: true
    user: "65532:65532"
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    tmpfs: [/tmp:rw,noexec,nosuid,size=64m]
    stop_grace_period: 45s
    healthcheck:
      test: [CMD, /app/healthprobe, http://127.0.0.1:9100/readyz]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 30s
    volumes:
      - type: bind
        source: ${ROOST_CONFIG_ROOT:?ROOST_CONFIG_ROOT is required}/config.activity.yaml
        target: /etc/roost/config.yaml
        read_only: true
    deploy:
      resources:
        limits: {cpus: "2", memory: 2G}
        reservations: {cpus: "0.25", memory: 256M}
  chat:
    image: ${ROOST_IMAGE:?ROOST_IMAGE must be an immutable digest}
    container_name: planet-chat
    command: ["chat", "--sid=1002", "--config=/etc/roost/config.yaml"]
    restart: unless-stopped
    read_only: true
    init: true
    user: "65532:65532"
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    tmpfs: [/tmp:rw,noexec,nosuid,size=64m]
    stop_grace_period: 45s
    healthcheck:
      test: [CMD, /app/healthprobe, http://127.0.0.1:9100/readyz]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 30s
    volumes:
      - type: bind
        source: ${ROOST_CONFIG_ROOT:?ROOST_CONFIG_ROOT is required}/config.chat.yaml
        target: /etc/roost/config.yaml
        read_only: true
    deploy:
      resources:
        limits: {cpus: "2", memory: 2G}
        reservations: {cpus: "0.25", memory: 256M}
  game:
    image: ${ROOST_IMAGE:?ROOST_IMAGE must be an immutable digest}
    container_name: planet-game
    command: ["game", "--sid=1003", "--config=/etc/roost/config.yaml"]
    restart: unless-stopped
    read_only: true
    init: true
    user: "65532:65532"
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    tmpfs: [/tmp:rw,noexec,nosuid,size=64m]
    stop_grace_period: 45s
    healthcheck:
      test: [CMD, /app/healthprobe, http://127.0.0.1:9100/readyz]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 30s
    volumes:
      - type: bind
        source: ${ROOST_CONFIG_ROOT:?ROOST_CONFIG_ROOT is required}/config.game.yaml
        target: /etc/roost/config.yaml
        read_only: true
      - type: volume
        source: planet-game-wal
        target: /var/lib/roost/wal
    deploy:
      resources:
        limits: {cpus: "2", memory: 2G}
        reservations: {cpus: "0.25", memory: 256M}
  global:
    image: ${ROOST_IMAGE:?ROOST_IMAGE must be an immutable digest}
    container_name: planet-global
    command: ["global", "--sid=1004", "--config=/etc/roost/config.yaml"]
    restart: unless-stopped
    read_only: true
    init: true
    user: "65532:65532"
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    tmpfs: [/tmp:rw,noexec,nosuid,size=64m]
    stop_grace_period: 45s
    healthcheck:
      test: [CMD, /app/healthprobe, http://127.0.0.1:9100/readyz]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 30s
    volumes:
      - type: bind
        source: ${ROOST_CONFIG_ROOT:?ROOST_CONFIG_ROOT is required}/config.global.yaml
        target: /etc/roost/config.yaml
        read_only: true
    deploy:
      resources:
        limits: {cpus: "2", memory: 2G}
        reservations: {cpus: "0.25", memory: 256M}
  mail:
    image: ${ROOST_IMAGE:?ROOST_IMAGE must be an immutable digest}
    container_name: planet-mail
    command: ["mail", "--sid=1005", "--config=/etc/roost/config.yaml"]
    restart: unless-stopped
    read_only: true
    init: true
    user: "65532:65532"
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    tmpfs: [/tmp:rw,noexec,nosuid,size=64m]
    stop_grace_period: 45s
    healthcheck:
      test: [CMD, /app/healthprobe, http://127.0.0.1:9100/readyz]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 30s
    volumes:
      - type: bind
        source: ${ROOST_CONFIG_ROOT:?ROOST_CONFIG_ROOT is required}/config.mail.yaml
        target: /etc/roost/config.yaml
        read_only: true
    deploy:
      resources:
        limits: {cpus: "2", memory: 2G}
        reservations: {cpus: "0.25", memory: 256M}
  match:
    image: ${ROOST_IMAGE:?ROOST_IMAGE must be an immutable digest}
    container_name: planet-match
    command: ["match", "--sid=1006", "--config=/etc/roost/config.yaml"]
    restart: unless-stopped
    read_only: true
    init: true
    user: "65532:65532"
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    tmpfs: [/tmp:rw,noexec,nosuid,size=64m]
    stop_grace_period: 45s
    healthcheck:
      test: [CMD, /app/healthprobe, http://127.0.0.1:9100/readyz]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 30s
    volumes:
      - type: bind
        source: ${ROOST_CONFIG_ROOT:?ROOST_CONFIG_ROOT is required}/config.match.yaml
        target: /etc/roost/config.yaml
        read_only: true
    deploy:
      resources:
        limits: {cpus: "2", memory: 2G}
        reservations: {cpus: "0.25", memory: 256M}
  platform:
    image: ${ROOST_IMAGE:?ROOST_IMAGE must be an immutable digest}
    container_name: planet-platform
    command: ["platform", "--sid=1007", "--config=/etc/roost/config.yaml"]
    restart: unless-stopped
    read_only: true
    init: true
    user: "65532:65532"
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    tmpfs: [/tmp:rw,noexec,nosuid,size=64m]
    stop_grace_period: 45s
    healthcheck:
      test: [CMD, /app/healthprobe, http://127.0.0.1:9100/readyz]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 30s
    volumes:
      - type: bind
        source: ${ROOST_CONFIG_ROOT:?ROOST_CONFIG_ROOT is required}/config.platform.yaml
        target: /etc/roost/config.yaml
        read_only: true
    deploy:
      resources:
        limits: {cpus: "2", memory: 2G}
        reservations: {cpus: "0.25", memory: 256M}
  rank:
    image: ${ROOST_IMAGE:?ROOST_IMAGE must be an immutable digest}
    container_name: planet-rank
    command: ["rank", "--sid=1008", "--config=/etc/roost/config.yaml"]
    restart: unless-stopped
    read_only: true
    init: true
    user: "65532:65532"
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    tmpfs: [/tmp:rw,noexec,nosuid,size=64m]
    stop_grace_period: 45s
    healthcheck:
      test: [CMD, /app/healthprobe, http://127.0.0.1:9100/readyz]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 30s
    volumes:
      - type: bind
        source: ${ROOST_CONFIG_ROOT:?ROOST_CONFIG_ROOT is required}/config.rank.yaml
        target: /etc/roost/config.yaml
        read_only: true
    deploy:
      resources:
        limits: {cpus: "2", memory: 2G}
        reservations: {cpus: "0.25", memory: 256M}
  session:
    image: ${ROOST_IMAGE:?ROOST_IMAGE must be an immutable digest}
    container_name: planet-session
    command: ["session", "--sid=1009", "--config=/etc/roost/config.yaml"]
    restart: unless-stopped
    read_only: true
    init: true
    user: "65532:65532"
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    tmpfs: [/tmp:rw,noexec,nosuid,size=64m]
    stop_grace_period: 45s
    healthcheck:
      test: [CMD, /app/healthprobe, http://127.0.0.1:9100/readyz]
      interval: 10s
      timeout: 3s
      retries: 6
      start_period: 30s
    volumes:
      - type: bind
        source: ${ROOST_CONFIG_ROOT:?ROOST_CONFIG_ROOT is required}/config.session.yaml
        target: /etc/roost/config.yaml
        read_only: true
    deploy:
      resources:
        limits: {cpus: "2", memory: 2G}
        reservations: {cpus: "0.25", memory: 256M}
volumes:
  planet-game-wal: {}
