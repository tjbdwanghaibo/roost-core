# Code generated by roost-codegen. DO NOT EDIT.
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: planet-game
  namespace: roost
  labels:
    app.kubernetes.io/name: planet
    app.kubernetes.io/component: game
spec:
  serviceName: planet-game
  podManagementPolicy: OrderedReady
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: planet
      app.kubernetes.io/component: game
  updateStrategy:
    type: RollingUpdate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: planet
        app.kubernetes.io/component: game
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "9100"
        prometheus.io/path: /metrics
    spec:
      serviceAccountName: planet
      automountServiceAccountToken: false
      terminationGracePeriodSeconds: 45
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: server
          image: ghcr.io/CHANGE_ME/planet:v1.0.0
          imagePullPolicy: IfNotPresent
          args: ["game", "--sid=$(ROOST_SID)", "--config=/etc/roost/config.yaml"]
          env:
            - name: ROOST_SID
              value: "1000"
          ports:
            - name: ops
              containerPort: 9100
              protocol: TCP
            - name: player-tcp
              containerPort: 7000
              protocol: TCP
          startupProbe:
            httpGet: {path: /healthz, port: ops}
            failureThreshold: 30
            periodSeconds: 2
          readinessProbe:
            httpGet: {path: /readyz, port: ops}
            periodSeconds: 5
            timeoutSeconds: 2
            failureThreshold: 3
          livenessProbe:
            httpGet: {path: /healthz, port: ops}
            periodSeconds: 10
            timeoutSeconds: 2
            failureThreshold: 3
          resources:
            requests: {cpu: "250m", memory: "256Mi"}
            limits: {cpu: "2", memory: "2Gi"}
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - {name: config, mountPath: /etc/roost, readOnly: true}
            - {name: tmp, mountPath: /tmp}
            - {name: wal, mountPath: /var/lib/roost/wal}
      volumes:
        - name: config
          secret:
            secretName: planet-game-config
            items:
              - {key: config.yaml, path: config.yaml}
        - name: tmp
          emptyDir: {sizeLimit: 64Mi}
  volumeClaimTemplates:
    - metadata:
        name: wal
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 10Gi
---
apiVersion: v1
kind: Service
metadata:
  name: planet-game
  namespace: roost
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: planet
    app.kubernetes.io/component: game
  ports:
    - {name: ops, port: 9100, targetPort: ops, protocol: TCP}
    - {name: player-tcp, port: 7000, targetPort: player-tcp, protocol: TCP}
