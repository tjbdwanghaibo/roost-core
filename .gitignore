.DS_Store
.idea/*

# Local multi-repo development workspace (see roost-core/docs/DEVELOPMENT_WORKSPACE.md).
# Never commit it: it would silently redirect every consumer to local source.
go.work
go.work.sum
gapmap-report.md

# Local Sync benchmark samples, profiles and test binaries.
/artifacts/perf/sync/

# Local Nest benchmark samples, profiles and test binaries.
/artifacts/perf/nest/

# Local DataEngine pressure samples and profiles.
/artifacts/perf/dataengine/

# Local Remote capacity and soak samples.
/artifacts/perf/remote/
