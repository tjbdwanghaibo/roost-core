// Code generated by tool/dao. DO NOT EDIT.
package db

import (
	"fmt"

	"github.com/tjbdwanghaibo/roost-core/dataengine"
	"github.com/tjbdwanghaibo/roost-core/nest"
	"go.mongodb.org/mongo-driver/v2/bson"
)

// TimerNode is a nested struct with dirty propagation.
//
// The hook is excluded from every codec: it is runtime wiring, not state, and
// an exported embedded struct with no exported fields would otherwise be
// written as an empty "dirtyhook" sub-document (U-0224).
type TimerNode struct {
	dataengine.DirtyHook `bson:"-" json:"-"`
	// dirtyOwner is the one place this value belongs to. Unexported, so no
	// codec sees it, and it is runtime wiring for the same reason the hook is.
	dirtyOwner   daoDirtyOwner
	typeValue    int32
	param1       int64
	param2       int64
	payload      string
	endUnixMilli int64
	delayMillis  int64
}

// bindDirty / unbindDirty are how a parent takes and releases this value.
//
// The parent is identified, not just remembered: a value that already belongs
// somewhere else is refused rather than silently re-pointed, because the
// notification is a single slot and re-pointing it leaves the first place
// stale on disk while memory shows both equal (RR-20260919-02). unbindDirty
// releases only its own binding, so a stale release from a place this value
// has already left cannot silence the place it is in now.
func (s *TimerNode) bindDirty(owner daoDirtyOwner, notify func()) {
	if s == nil {
		return
	}
	daoBindDirty(&s.DirtyHook, &s.dirtyOwner, owner, "TimerNode", notify)
}

func (s *TimerNode) unbindDirty(owner daoDirtyOwner) {
	if s == nil {
		return
	}
	daoUnbindDirty(&s.DirtyHook, &s.dirtyOwner, owner)
}

// --- wire form (persistence, rollback capture, sync) ---
//
// The fields are unexported so every mutation goes through the setters below
// — that is what makes dirty tracking and undo impossible to bypass — and the
// reflection-based codec cannot see unexported fields. TimerNode therefore
// has a WIRE FORM, timerNodeBSONDoc, with exported fields: the
// parent DAO places that into its own document and reflection encodes it
// inline, with no per-value Marshaler round trip (U-0224 and its follow-up:
// the bson.Marshaler route cost about four allocations per nested value).
// MarshalBSON / UnmarshalBSON are kept for a TimerNode encoded on its own.
type timerNodeBSONDoc struct {
	Type         int32  `bson:"type"`
	Param1       int64  `bson:"param1"`
	Param2       int64  `bson:"param2"`
	Payload      string `bson:"payload"`
	EndUnixMilli int64  `bson:"end_unix_milli"`
	DelayMillis  int64  `bson:"delay_millis"`
}

// bsonDoc is the value receiver on purpose: containers hold TimerNode values
// as well as pointers, and a method expression TimerNode.bsonDoc maps over both.
func (s TimerNode) bsonDoc() timerNodeBSONDoc {
	return timerNodeBSONDoc{
		Type:         s.typeValue,
		Param1:       s.param1,
		Param2:       s.param2,
		Payload:      s.payload,
		EndUnixMilli: s.endUnixMilli,
		DelayMillis:  s.delayMillis,
	}
}

func (s *TimerNode) setBSONDoc(doc timerNodeBSONDoc) {
	s.typeValue = doc.Type
	s.param1 = doc.Param1
	s.param2 = doc.Param2
	s.payload = doc.Payload
	s.endUnixMilli = doc.EndUnixMilli
	s.delayMillis = doc.DelayMillis
}

func timerNodeFromBSONDoc(doc timerNodeBSONDoc) TimerNode {
	var s TimerNode
	s.setBSONDoc(doc)
	return s
}

func timerNodePtrBSONDoc(v *TimerNode) *timerNodeBSONDoc {
	if v == nil {
		return nil
	}
	doc := v.bsonDoc()
	return &doc
}

func timerNodePtrFromBSONDoc(doc *timerNodeBSONDoc) *TimerNode {
	if doc == nil {
		return nil
	}
	v := timerNodeFromBSONDoc(*doc)
	return &v
}

func (s TimerNode) MarshalBSON() ([]byte, error) { return bson.Marshal(s.bsonDoc()) }

func (s *TimerNode) UnmarshalBSON(raw []byte) error {
	var doc timerNodeBSONDoc
	if err := bson.Unmarshal(raw, &doc); err != nil {
		return err
	}
	s.setBSONDoc(doc)
	return nil
}

func (s *TimerNode) GetType() int32 { return s.typeValue }

func (s *TimerNode) GetParam1() int64 { return s.param1 }

func (s *TimerNode) GetParam2() int64 { return s.param2 }

func (s *TimerNode) GetPayload() string { return s.payload }

func (s *TimerNode) GetEndUnixMilli() int64 { return s.endUnixMilli }

func (s *TimerNode) GetDelayMillis() int64 { return s.delayMillis }

func (s *TimerNode) SetType(v int32) {
	if tx := nest.CurrentRollbackTx(); tx != nil && tx.Policy() == nest.RollbackUndo {
		old := s.typeValue
		if err := tx.RecordUndo(s, uint64(0), func() error {
			s.typeValue = old
			return nil
		}); err != nil {
			// A mutation without undo coverage silently breaks rollback;
			// failing loudly matches the generated DAO setters.
			panic(fmt.Errorf("TimerNode: record undo: %w", err))
		}
	}
	if s.typeValue != v {
		s.typeValue = v
		s.Mark()
	}
}

func (s *TimerNode) SetParam1(v int64) {
	if tx := nest.CurrentRollbackTx(); tx != nil && tx.Policy() == nest.RollbackUndo {
		old := s.param1
		if err := tx.RecordUndo(s, uint64(1), func() error {
			s.param1 = old
			return nil
		}); err != nil {
			// A mutation without undo coverage silently breaks rollback;
			// failing loudly matches the generated DAO setters.
			panic(fmt.Errorf("TimerNode: record undo: %w", err))
		}
	}
	if s.param1 != v {
		s.param1 = v
		s.Mark()
	}
}

func (s *TimerNode) SetParam2(v int64) {
	if tx := nest.CurrentRollbackTx(); tx != nil && tx.Policy() == nest.RollbackUndo {
		old := s.param2
		if err := tx.RecordUndo(s, uint64(2), func() error {
			s.param2 = old
			return nil
		}); err != nil {
			// A mutation without undo coverage silently breaks rollback;
			// failing loudly matches the generated DAO setters.
			panic(fmt.Errorf("TimerNode: record undo: %w", err))
		}
	}
	if s.param2 != v {
		s.param2 = v
		s.Mark()
	}
}

func (s *TimerNode) SetPayload(v string) {
	if tx := nest.CurrentRollbackTx(); tx != nil && tx.Policy() == nest.RollbackUndo {
		old := s.payload
		if err := tx.RecordUndo(s, uint64(3), func() error {
			s.payload = old
			return nil
		}); err != nil {
			// A mutation without undo coverage silently breaks rollback;
			// failing loudly matches the generated DAO setters.
			panic(fmt.Errorf("TimerNode: record undo: %w", err))
		}
	}
	if s.payload != v {
		s.payload = v
		s.Mark()
	}
}

func (s *TimerNode) SetEndUnixMilli(v int64) {
	if tx := nest.CurrentRollbackTx(); tx != nil && tx.Policy() == nest.RollbackUndo {
		old := s.endUnixMilli
		if err := tx.RecordUndo(s, uint64(4), func() error {
			s.endUnixMilli = old
			return nil
		}); err != nil {
			// A mutation without undo coverage silently breaks rollback;
			// failing loudly matches the generated DAO setters.
			panic(fmt.Errorf("TimerNode: record undo: %w", err))
		}
	}
	if s.endUnixMilli != v {
		s.endUnixMilli = v
		s.Mark()
	}
}

func (s *TimerNode) SetDelayMillis(v int64) {
	if tx := nest.CurrentRollbackTx(); tx != nil && tx.Policy() == nest.RollbackUndo {
		old := s.delayMillis
		if err := tx.RecordUndo(s, uint64(5), func() error {
			s.delayMillis = old
			return nil
		}); err != nil {
			// A mutation without undo coverage silently breaks rollback;
			// failing loudly matches the generated DAO setters.
			panic(fmt.Errorf("TimerNode: record undo: %w", err))
		}
	}
	if s.delayMillis != v {
		s.delayMillis = v
		s.Mark()
	}
}
