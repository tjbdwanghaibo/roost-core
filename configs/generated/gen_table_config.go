// Code generated by tool/tablegen. DO NOT EDIT.
package generated

import (
	"encoding/csv"
	"encoding/json"
	"fmt"
	"io"
	"strconv"
	"strings"
	"time"

	meta0 "example.com/planet/configs/schema"
	meta1 "example.com/planet/configs/schema"
	meta2 "example.com/planet/configs/schema"
	"github.com/tjbdwanghaibo/roost-core/configdata"
)

var (
	_ = csv.NewReader
	_ = json.Unmarshal
	_ = fmt.Errorf
	_ io.Reader
	_ = time.ParseDuration
)

func RegisterGeneratedConfigData(r *configdata.Registry) error {
	if err := configdata.RegisterTable(r, configdata.TableDef[string, meta0.FeatureFlag]{
		Name: configdata.Name("featureflag"),
		File: "feature_flag.json",
		Key:  func(v meta0.FeatureFlag) string { return v.Name },
	}); err != nil {
		return err
	}
	if err := configdata.RegisterTable(r, configdata.TableDef[int64, meta1.Item]{
		Name: configdata.Name("item"),
		File: "item.json",
		Key:  func(v meta1.Item) int64 { return v.ID },
	}); err != nil {
		return err
	}
	if err := configdata.RegisterTable(r, configdata.TableDef[int32, meta2.Spawn]{
		Name: configdata.Name("spawn"),
		File: "spawn.json",
		Key:  func(v meta2.Spawn) int32 { return v.ID },
	}); err != nil {
		return err
	}
	return nil
}

// RegisterConfigData registers the generated definitions on the default
// registry. This is the entry point the generated aggregate in
// internal/registry calls; RegisterGeneratedConfigData stays exported and
// explicit for tests and for services that build their own registry.
//
//roost:register phase=config
func RegisterConfigData() error {
	return RegisterGeneratedConfigData(configdata.DefaultRegistry())
}

type FeatureFlagCfg = meta0.FeatureFlag

func FeatureFlagTableFrom(snap *configdata.Snapshot) (*configdata.Table[string, meta0.FeatureFlag], bool) {
	return configdata.TableFrom[string, meta0.FeatureFlag](snap, configdata.Name("featureflag"))
}

func FeatureFlagByName(id string) (meta0.FeatureFlag, bool) {
	table, ok := FeatureFlagTableFrom(configdata.ActiveSnapshot())
	if !ok {
		var zero meta0.FeatureFlag
		return zero, false
	}
	return table.Get(id)
}

func ConvertFeatureFlagCSV(r io.Reader) ([]meta0.FeatureFlag, error) {
	reader := csv.NewReader(r)
	reader.TrimLeadingSpace = true
	records, err := reader.ReadAll()
	if err != nil {
		var zero []meta0.FeatureFlag
		return zero, err
	}
	rows, err := convertFeatureFlagRecords(records)
	if err != nil {
		var zero []meta0.FeatureFlag
		return zero, err
	}

	return rows, nil

}

func convertFeatureFlagRecords(records [][]string) ([]meta0.FeatureFlag, error) {
	if len(records) == 0 {
		return nil, nil
	}
	header := records[0]
	col := make(map[string]int, len(header))
	for i, name := range header {
		col[name] = i
	}
	start := 1
	if len(records) > start && len(records[start]) > 0 && records[start][0] == "Name" {
		start++
	}
	if len(records) > start && len(records[start]) > 0 && records[start][0] == "string" {
		start++
	}
	if len(records) > start && len(records[start]) > 0 && strings.Contains(records[start][0], "required") {
		start++
	}
	out := make([]meta0.FeatureFlag, 0, len(records)-start)
	for rowIndex := start; rowIndex < len(records); rowIndex++ {
		record := records[rowIndex]
		if tablegenEmptyRecord(record) {
			continue
		}
		var item meta0.FeatureFlag
		if idx, ok := col["name"]; ok && idx < len(record) {
			raw := strings.TrimSpace(record[idx])
			if raw != "" {
				v, err := raw, error(nil)
				if err != nil {
					return nil, fmt.Errorf("row=%d field=Name value=%q: %w", rowIndex+1, raw, err)
				}
				item.Name = v
			}
		}
		if idx, ok := col["enabled"]; ok && idx < len(record) {
			raw := strings.TrimSpace(record[idx])
			if raw != "" {
				v, err := strconv.ParseBool(raw)
				if err != nil {
					return nil, fmt.Errorf("row=%d field=Enabled value=%q: %w", rowIndex+1, raw, err)
				}
				item.Enabled = v
			}
		}
		if idx, ok := col["note"]; ok && idx < len(record) {
			raw := strings.TrimSpace(record[idx])
			if raw != "" {
				v, err := raw, error(nil)
				if err != nil {
					return nil, fmt.Errorf("row=%d field=Note value=%q: %w", rowIndex+1, raw, err)
				}
				item.Note = v
			}
		}
		out = append(out, item)
	}
	return out, nil
}

type ItemCfg = meta1.Item

func ItemTableFrom(snap *configdata.Snapshot) (*configdata.Table[int64, meta1.Item], bool) {
	return configdata.TableFrom[int64, meta1.Item](snap, configdata.Name("item"))
}

func ItemByID(id int64) (meta1.Item, bool) {
	table, ok := ItemTableFrom(configdata.ActiveSnapshot())
	if !ok {
		var zero meta1.Item
		return zero, false
	}
	return table.Get(id)
}

func ConvertItemCSV(r io.Reader) ([]meta1.Item, error) {
	reader := csv.NewReader(r)
	reader.TrimLeadingSpace = true
	records, err := reader.ReadAll()
	if err != nil {
		var zero []meta1.Item
		return zero, err
	}
	rows, err := convertItemRecords(records)
	if err != nil {
		var zero []meta1.Item
		return zero, err
	}

	return rows, nil

}

func convertItemRecords(records [][]string) ([]meta1.Item, error) {
	if len(records) == 0 {
		return nil, nil
	}
	header := records[0]
	col := make(map[string]int, len(header))
	for i, name := range header {
		col[name] = i
	}
	start := 1
	if len(records) > start && len(records[start]) > 0 && records[start][0] == "ID" {
		start++
	}
	if len(records) > start && len(records[start]) > 0 && records[start][0] == "int64" {
		start++
	}
	if len(records) > start && len(records[start]) > 0 && strings.Contains(records[start][0], "required") {
		start++
	}
	out := make([]meta1.Item, 0, len(records)-start)
	for rowIndex := start; rowIndex < len(records); rowIndex++ {
		record := records[rowIndex]
		if tablegenEmptyRecord(record) {
			continue
		}
		var item meta1.Item
		if idx, ok := col["id"]; ok && idx < len(record) {
			raw := strings.TrimSpace(record[idx])
			if raw != "" {
				v, err := strconv.ParseInt(raw, 10, 64)
				if err != nil {
					return nil, fmt.Errorf("row=%d field=ID value=%q: %w", rowIndex+1, raw, err)
				}
				item.ID = v
			}
		}
		if idx, ok := col["name"]; ok && idx < len(record) {
			raw := strings.TrimSpace(record[idx])
			if raw != "" {
				v, err := raw, error(nil)
				if err != nil {
					return nil, fmt.Errorf("row=%d field=Name value=%q: %w", rowIndex+1, raw, err)
				}
				item.Name = v
			}
		}
		if idx, ok := col["max_stack"]; ok && idx < len(record) {
			raw := strings.TrimSpace(record[idx])
			if raw != "" {
				v, err := tablegenParseInt[int32](raw)
				if err != nil {
					return nil, fmt.Errorf("row=%d field=MaxStack value=%q: %w", rowIndex+1, raw, err)
				}
				item.MaxStack = v
			}
		}
		if idx, ok := col["attack"]; ok && idx < len(record) {
			raw := strings.TrimSpace(record[idx])
			if raw != "" {
				v, err := strconv.ParseInt(raw, 10, 64)
				if err != nil {
					return nil, fmt.Errorf("row=%d field=Attack value=%q: %w", rowIndex+1, raw, err)
				}
				item.Attack = v
			}
		}
		if idx, ok := col["hp"]; ok && idx < len(record) {
			raw := strings.TrimSpace(record[idx])
			if raw != "" {
				v, err := strconv.ParseInt(raw, 10, 64)
				if err != nil {
					return nil, fmt.Errorf("row=%d field=HP value=%q: %w", rowIndex+1, raw, err)
				}
				item.HP = v
			}
		}
		out = append(out, item)
	}
	return out, nil
}

type SpawnCfg = meta2.Spawn

func SpawnTableFrom(snap *configdata.Snapshot) (*configdata.Table[int32, meta2.Spawn], bool) {
	return configdata.TableFrom[int32, meta2.Spawn](snap, configdata.Name("spawn"))
}

func SpawnByID(id int32) (meta2.Spawn, bool) {
	table, ok := SpawnTableFrom(configdata.ActiveSnapshot())
	if !ok {
		var zero meta2.Spawn
		return zero, false
	}
	return table.Get(id)
}

func ConvertSpawnCSV(r io.Reader) ([]meta2.Spawn, error) {
	reader := csv.NewReader(r)
	reader.TrimLeadingSpace = true
	records, err := reader.ReadAll()
	if err != nil {
		var zero []meta2.Spawn
		return zero, err
	}
	rows, err := convertSpawnRecords(records)
	if err != nil {
		var zero []meta2.Spawn
		return zero, err
	}

	return rows, nil

}

func convertSpawnRecords(records [][]string) ([]meta2.Spawn, error) {
	if len(records) == 0 {
		return nil, nil
	}
	header := records[0]
	col := make(map[string]int, len(header))
	for i, name := range header {
		col[name] = i
	}
	start := 1
	if len(records) > start && len(records[start]) > 0 && records[start][0] == "ID" {
		start++
	}
	if len(records) > start && len(records[start]) > 0 && records[start][0] == "int32" {
		start++
	}
	if len(records) > start && len(records[start]) > 0 && strings.Contains(records[start][0], "required") {
		start++
	}
	out := make([]meta2.Spawn, 0, len(records)-start)
	for rowIndex := start; rowIndex < len(records); rowIndex++ {
		record := records[rowIndex]
		if tablegenEmptyRecord(record) {
			continue
		}
		var item meta2.Spawn
		if idx, ok := col["id"]; ok && idx < len(record) {
			raw := strings.TrimSpace(record[idx])
			if raw != "" {
				v, err := tablegenParseInt[int32](raw)
				if err != nil {
					return nil, fmt.Errorf("row=%d field=ID value=%q: %w", rowIndex+1, raw, err)
				}
				item.ID = v
			}
		}
		if idx, ok := col["template"]; ok && idx < len(record) {
			raw := strings.TrimSpace(record[idx])
			if raw != "" {
				v, err := tablegenParseInt[int32](raw)
				if err != nil {
					return nil, fmt.Errorf("row=%d field=Template value=%q: %w", rowIndex+1, raw, err)
				}
				item.Template = v
			}
		}
		if idx, ok := col["count"]; ok && idx < len(record) {
			raw := strings.TrimSpace(record[idx])
			if raw != "" {
				v, err := tablegenParseInt[int32](raw)
				if err != nil {
					return nil, fmt.Errorf("row=%d field=Count value=%q: %w", rowIndex+1, raw, err)
				}
				item.Count = v
			}
		}
		if idx, ok := col["hp"]; ok && idx < len(record) {
			raw := strings.TrimSpace(record[idx])
			if raw != "" {
				v, err := strconv.ParseInt(raw, 10, 64)
				if err != nil {
					return nil, fmt.Errorf("row=%d field=HP value=%q: %w", rowIndex+1, raw, err)
				}
				item.HP = v
			}
		}
		if idx, ok := col["center_x"]; ok && idx < len(record) {
			raw := strings.TrimSpace(record[idx])
			if raw != "" {
				v, err := strconv.ParseInt(raw, 10, 64)
				if err != nil {
					return nil, fmt.Errorf("row=%d field=CenterX value=%q: %w", rowIndex+1, raw, err)
				}
				item.CenterX = v
			}
		}
		if idx, ok := col["center_y"]; ok && idx < len(record) {
			raw := strings.TrimSpace(record[idx])
			if raw != "" {
				v, err := strconv.ParseInt(raw, 10, 64)
				if err != nil {
					return nil, fmt.Errorf("row=%d field=CenterY value=%q: %w", rowIndex+1, raw, err)
				}
				item.CenterY = v
			}
		}
		if idx, ok := col["radius"]; ok && idx < len(record) {
			raw := strings.TrimSpace(record[idx])
			if raw != "" {
				v, err := strconv.ParseInt(raw, 10, 64)
				if err != nil {
					return nil, fmt.Errorf("row=%d field=Radius value=%q: %w", rowIndex+1, raw, err)
				}
				item.Radius = v
			}
		}
		if idx, ok := col["respawn_seconds"]; ok && idx < len(record) {
			raw := strings.TrimSpace(record[idx])
			if raw != "" {
				v, err := strconv.ParseInt(raw, 10, 64)
				if err != nil {
					return nil, fmt.Errorf("row=%d field=RespawnSeconds value=%q: %w", rowIndex+1, raw, err)
				}
				item.RespawnSeconds = v
			}
		}
		out = append(out, item)
	}
	return out, nil
}

func tablegenEmptyRecord(record []string) bool {
	for _, cell := range record {
		if strings.TrimSpace(cell) != "" {
			return false
		}
	}
	return true
}

func tablegenParseInt[T ~int | ~int8 | ~int16 | ~int32](raw string) (T, error) {
	v, err := strconv.ParseInt(raw, 10, 64)
	return T(v), err
}

func tablegenParseUint[T ~uint | ~uint8 | ~uint16 | ~uint32](raw string) (T, error) {
	v, err := strconv.ParseUint(raw, 10, 64)
	return T(v), err
}

func tablegenParseFloat[T ~float32](raw string) (T, error) {
	v, err := strconv.ParseFloat(raw, 64)
	return T(v), err
}

func tablegenParseJSON[T any](raw string) (T, error) {
	var v T
	if err := json.Unmarshal([]byte(raw), &v); err != nil {
		return v, err
	}
	return v, nil
}
